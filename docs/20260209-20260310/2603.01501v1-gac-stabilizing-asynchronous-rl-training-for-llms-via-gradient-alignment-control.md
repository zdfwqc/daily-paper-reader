---
title: "GAC: Stabilizing Asynchronous RL Training for LLMs via Gradient Alignment Control"
title_zh: GAC：通过梯度对齐控制稳定大语言模型的异步强化学习训练
authors: "Haofeng Xu, Junwei Su, Yukun Tian, Lansong Diao, Zhengping Qian, Chuan Wu"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01501v1"
tags: ["query:drl"]
score: 9.0
evidence: 提高异步强化学习的训练稳定性
tldr: 本研究针对大语言模型异步强化学习（RL）训练中的不稳定性问题，发现异步更新会导致连续梯度间产生持续的高余弦相似度（陈旧对齐梯度效应），从而引发训练发散。为此，提出了梯度对齐控制（GAC）方法，通过梯度投影调节陈旧方向上的更新进度。GAC 不仅在理论上保证了收敛性，还能在保持高吞吐量的同时，使异步训练达到与同步基准一致的稳定性和性能。
selection_source: fresh_fetch
motivation: 异步 RL 训练中存在的陈旧梯度对齐效应会导致更新过度和训练不稳定，限制了其在大模型上的扩展应用。
method: 提出 GAC 方法，利用梯度投影技术动态调节异步更新，以抑制因梯度高度对齐而产生的发散风险。
result: 实验证明 GAC 能够有效恢复稳定的训练动态，即使在高陈旧度环境下也能匹配同步训练的基准效果。
conclusion: GAC 为大规模异步 RL 提供了一种简单有效的稳定机制，兼顾了训练效率与优化稳定性。
---

## 摘要
异步执行对于将强化学习（RL）扩展到现代大模型工作负载（包括大语言模型和 AI 智能体）至关重要，但它会从根本上改变 RL 的优化行为。尽管先前关于异步 RL 的工作主要集中在训练吞吐量和分布修正上，但我们表明，将异步简单地应用于策略梯度更新会引发性质截然不同的训练动力学，并导致严重的训练不稳定。通过系统的实证和理论分析，我们发现了这种不稳定性的一项关键特征：与同步训练中观察到的近乎正交的更新相比，异步训练在连续的策略梯度之间表现出持续的高余弦相似度。这种陈旧对齐梯度（stale-aligned gradient）效应放大了相关更新，并增加了过度调整（overshooting）和发散的风险。受此观察启发，我们提出了梯度对齐控制（GAC），这是一种简单的动力学感知稳定方法，通过梯度投影来调节异步 RL 在陈旧对齐方向上的进展。我们在有界陈旧度下建立了收敛性保证，并从实证上证明了 GAC 能够恢复稳定的同策略（on-policy）训练动力学，即使在高陈旧度下也能达到与同步基准相当的效果。

## Abstract
Asynchronous execution is essential for scaling reinforcement learning (RL) to modern large model workloads, including large language models and AI agents, but it can fundamentally alter RL optimization behavior. While prior work on asynchronous RL focuses on training throughput and distributional correction, we show that naively applying asynchrony to policy-gradient updates can induce qualitatively different training dynamics and lead to severe training instability. Through systematic empirical and theoretical analysis, we identify a key signature of this instability: asynchronous training exhibits persistently high cosine similarity between consecutive policy gradients, in contrast to the near-orthogonal updates observed under synchronized training. This stale-aligned gradient effect amplifies correlated updates and increases the risk of overshooting and divergence. Motivated by this observation, we propose GRADIENT ALIGNMENT CONTROL(GAC), a simple dynamics-aware stabilization method that regulates asynchronous RL progress along stale-aligned directions via gradient projection. We establish convergence guarantees under bounded staleness and demonstrate empirically that GAC recovers stable, on-policy training dynamics and matches synchronized baselines even at high staleness.

---

## 论文详细总结（自动生成）

### 论文总结：GAC——通过梯度对齐控制稳定大语言模型的异步强化学习训练

#### 1. 核心问题与研究动机
*   **研究背景**：在大语言模型（LLM）的对齐阶段，强化学习（RL）特别是组相对策略优化（GRPO）被广泛用于提升模型的推理能力。为了提高训练吞吐量，现代系统常采用**异步执行**（解耦采样与优化），以减少硬件空闲。
*   **核心问题**：异步训练引入了“陈旧性”（Staleness），即优化器使用的梯度是由旧版本的策略模型生成的。作者发现，简单应用异步更新会导致训练动态发生质变，引发严重的训练不稳定甚至崩溃。
*   **关键发现**：同步训练的连续梯度通常接近正交（余弦相似度接近 0），而异步训练则表现出持续的高余弦相似度（**陈旧对齐梯度效应**）。这种效应会放大相关更新，导致步长过大（Overshooting）和模型发散。

#### 2. 方法论（GAC：Gradient Alignment Control）
*   **核心思想**：GAC 是一种动力学感知的稳定机制，通过监控连续梯度间的余弦相似度 $c_t$，动态调节更新方向，抑制沿陈旧对齐方向的过度进展。
*   **技术细节**：
    *   **梯度分解**：将当前梯度 $\hat{g}_t$ 分解为平行于前一梯度 $\hat{g}_{t-1}$ 的分量 $g_{\parallel}$ 和正交分量 $g_{\perp}$。
    *   **三阶段控制逻辑**：
        1.  **安全状态** ($|c_t| \le c_{low}$)：对齐度低，按原样更新。
        2.  **投影状态** ($c_{low} < |c_t| < c_{high}$)：对齐度偏高，通过缩放因子 $\alpha = c_{low}/|c_t|$ 削减平行分量，保留正交分量，即 $\hat{g}'_t = \alpha g_{\parallel} + g_{\perp}$。
        3.  **违规状态** ($|c_t| \ge c_{high}$)：对齐度极高，可能导致崩溃，直接跳过该步更新。
*   **算法特性**：该方法与具体 RL 算法和分布式系统无关，仅需存储前一步的梯度分片，计算开销极低（常数级）。

#### 3. 实验设计
*   **模型与架构**：Qwen3 系列（1.7B、4B、8B）以及 Llama-3.2-3B-Instruct。
*   **任务场景**：数学推理任务，涵盖 7 个主流 Benchmark（AIME24/25、AMC23/24、Math500、OlympiadBench、MinervaMath）。
*   **对比方法（Baselines）**：
    *   **GRPO-Sync (s=0)**：同步训练基准（上限）。
    *   **GRPO-Async**：原生异步 GRPO。
    *   **M2PO / BAPO**：现有的针对异步/离策（Off-policy）RL 的先进稳定方法。
*   **陈旧度设置**：测试了 $s \in \{4, 8, 16, 32\}$ 等不同延迟步数。

#### 4. 资源与算力
*   **硬件环境**：单台 8-GPU 节点，配备 **NVIDIA H800-80GB** GPU。
*   **互联技术**：400 GB/s NVLink。
*   **软件栈**：基于开源框架 **VERL**，使用 vLLM 进行推理加速，支持 FSDP/DeepSpeed ZeRO 梯度分片。
*   **训练时长**：文中未明确给出总训练小时数，但展示了 125-300 个训练步数的收敛曲线。

#### 5. 实验数量与充分性
*   **实验规模**：作者在 4 种不同规模/架构的模型上进行了验证，并在 7 个不同的数学数据集上报告了准确率。
*   **消融与鲁棒性**：
    *   进行了针对不同陈旧度（最高到 $s=32$）的鲁棒性测试。
    *   对超参数 $c_{low}$ 和 $c_{high}$ 进行了敏感性分析（Grid Sweep），证明了默认参数在不同模型间的通用性。
*   **评价**：实验设计较为充分且客观，通过对比多种 SOTA 异步稳定方法，证明了 GAC 在恢复“同策略动态”方面的独特性。

#### 6. 主要结论与发现
*   **稳定性恢复**：GAC 有效消除了异步训练中的奖励（Reward）骤降和准确率振荡现象。
*   **性能闭环**：在 $s=16$ 的高陈旧度下，原生异步方法性能大幅下滑，而 GAC 能够将性能恢复至接近同步训练（s=0）的水平（例如在 Qwen3-8B 上，GAC 达到 50.2% 准确率，接近同步的 51.0%，远高于基准的 47.1%）。
*   **理论一致性**：理论分析证明，GAC 的投影操作能显著降低由陈旧性引起的梯度偏差（Bias）。

#### 7. 优点与亮点
*   **视角新颖**：不同于以往侧重于“分布修正”的方法，本文从“训练动力学（梯度几何结构）”视角切入，发现了连续梯度对齐这一关键不稳定性指标。
*   **极简且高效**：GAC 不需要复杂的重采样或复杂的损失函数修改，仅通过简单的线性代数投影即可实现，易于集成到现有的大规模训练框架中。
*   **系统友好**：在保持异步训练高吞吐量优势的同时，解决了其固有的稳定性痛点。

#### 8. 不足与局限
*   **任务局限性**：实验主要集中在具有明确验证器（Verifier）的数学推理任务（RLVR），尚未在人类偏好对齐（RLHF）等更具主观性、奖励模型更嘈杂的场景中验证。
*   **超参数依赖**：虽然实验证明 $c_{low}$ 相对鲁棒，但在极端不同的学习率或优化器设置下，是否仍能保持 0.05 的通用性尚待更多验证。
*   **长程依赖**：GAC 目前仅考虑了连续两步之间的梯度对齐，对于更长周期的梯度相关性（如跨越 5-10 步的累积效应）未做深入探讨。

（完）
