---
title: "ExO-PPO: an Extended Off-policy Proximal Policy Optimization Algorithm"
title_zh: ExO-PPO：一种扩展的离策近端策略优化算法
authors: "Hanyong Wang, Menglong Yang"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09726v1"
tags: ["query:drl"]
score: 9.0
evidence: 提高PPO的训练稳定性和样本效率
tldr: 针对PPO算法在保持训练稳定性的同时牺牲了样本效率的问题，本文提出ExO-PPO算法。该方法通过推导广义策略改进下界的期望形式，引入分段指数函数改进裁剪机制，并利用经验回放池存储过去M个策略的轨迹进行离线训练。实验证明，ExO-PPO在多种任务中实现了比PPO及其他变体更优的样本效率与稳定性平衡，为强化学习提供了一种更高效的优化方案。
selection_source: fresh_fetch
motivation: 旨在解决PPO算法因保守的同策略更新导致样本效率低下，以及异策略方法虽高效但方差和偏差较大的矛盾。
method: 提出一种基于广义策略改进下界的扩展异策略目标函数，并结合分段指数裁剪机制和多策略经验回放机制。
result: 在多项实验任务中，ExO-PPO在样本利用率和训练稳定性方面均显著优于PPO及现有的先进变体。
conclusion: ExO-PPO通过有效融合同策略的稳定性和异策略的高效性，成功提升了深度强化学习算法的综合性能。
---

## 摘要
深度强化学习已能成功解决各种任务，然而，由于策略梯度的构造和训练动态，调优深度强化学习模型仍然具有挑战性。作为最成功的深度强化学习算法之一，近端策略优化算法（PPO）将策略梯度裁剪在保守的同策（on-policy）更新范围内，从而确保了可靠且稳定的策略改进。然而，这种训练模式可能会牺牲样本效率。另一方面，离策（off-policy）方法通过样本重用更充分地利用数据，尽管代价是增加了估计方差和偏差。为了结合两者的优点，本文提出了一种新的 PPO 变体，它基于保守同策迭代的稳定性保证，并具有更高效的离策数据利用率。具体而言，我们首先从广义策略改进下界的期望形式推导出一种扩展的离策改进。其次，我们使用分段指数函数扩展了裁剪机制，以获得合适的代理目标函数。第三，由过去 $M$ 个策略生成的轨迹被组织在经验回放池中用于离策训练。我们将此方法称为扩展离策近端策略优化（ExO-PPO）。与 PPO 和其他一些最先进的变体相比，我们在实证实验中证明了 ExO-PPO 在各种任务上具有平衡的样本效率和稳定性，并表现出更优的性能。

## Abstract
Deep reinforcement learning has been able to solve various tasks successfully, however, due to the construction of policy gradient and training dynamics, tuning deep reinforcement learning models remains challenging. As one of the most successful deep reinforcement-learning algorithm, the Proximal Policy Optimization algorithm (PPO) clips the policy gradient within a conservative on-policy updates, which ensures reliable and stable policy improvement. However, this training pattern may sacrifice sample efficiency. On the other hand, off-policy methods make more adequate use of data through sample reuse, though at the cost of increased the estimation variance and bias. To leverage the advantages of both, in this paper, we propose a new PPO variant based on the stability guarantee from conservative on-policy iteration with a more efficient off-policy data utilization. Specifically, we first derive an extended off-policy improvement from an expectation form of generalized policy improvement lower bound. Then, we extend the clipping mechanism with segmented exponential functions for a suitable surrogate objective function. Third, the trajectories generated by the past $M$ policies are organized in the replay buffer for off-policy training. We refer to this method as Extended Off-policy Proximal Policy Optimization (ExO-PPO). Compared with PPO and some other state-of-the-art variants, we demonstrate an improved performance of ExO-PPO with balanced sample efficiency and stability on varied tasks in the empirical experiments.

---

## 论文详细总结（自动生成）

这是一份关于论文《ExO-PPO: an Extended Off-policy Proximal Policy Optimization Algorithm》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
深度强化学习（DRL）在复杂任务中表现优异，但面临**稳定性与样本效率**之间的权衡。
*   **PPO 的局限性**：作为主流的同策（On-policy）算法，PPO 通过裁剪概率比率确保了训练的稳定性，但由于无法有效重用历史数据，导致样本效率低下，通常需要数百万次交互才能收敛。
*   **离策（Off-policy）算法的挑战**：虽然能通过经验回放提高效率，但由于分布偏移（Distribution Shift），容易产生高方差和估计偏差，导致训练不稳定。
*   **研究动机**：本文旨在提出一种新的 PPO 变体，在保留 PPO 稳定性的基础上，通过理论推导和目标函数改进，实现对过去 $M$ 个策略数据的有效利用，从而提升样本效率。

### 2. 方法论
ExO-PPO 的核心由三个关键部分组成：
*   **扩展离策改进下界（Extended Off-Policy Improvement Lower Bound）**：作者从理论上推导了策略改进下界的期望形式。不同于传统 PPO 仅对比当前策略，ExO-PPO 证明了通过最大化当前策略与过去 $M$ 个策略期望之间的差异，依然可以保证策略的单调改进。
*   **扩展比率目标函数（Extended Ratio Objective）**：
    *   **分段指数衰减边缘**：传统 PPO 在裁剪区间 $[1-\epsilon, 1+\epsilon]$ 之外梯度直接归零。ExO-PPO 引入了一个参数化的指数函数 $\alpha$，使超出区间的梯度以指数形式平滑衰减。
    *   **平滑性**：该函数在全域内连续可微，扩大了梯度空间，使模型在处理偏离较大的离策数据时仍能获得有效的优化方向。
*   **扩展离策采样模式**：算法维护一个存储最近 $M$ 个策略轨迹的经验回放池。在训练时，从这 $M$ 个策略中均匀采样。这种设计介于纯同策（$M=1$）和完全离策（大规模 Buffer）之间，通过限制分布偏移的程度来维持稳定。

### 3. 实验设计
*   **实验场景**：
    *   **离散控制**：Atari 游戏（如 Pong-v5, Breakout-v5 等）。
    *   **连续控制**：MuJoCo 环境（Ant-v4, HalfCheetah-v4, Walker2d-v4）。
    *   **离线学习（Offline RL）**：使用 Minari 数据集（包含 D4RL 基准）。
*   **Benchmark 与对比方法**：
    *   **基础/变体 PPO**：原生 PPO, ESPPO, P3O-Scopic。
    *   **经典离策算法**：SAC, TD3。
    *   **混合/改进算法**：GePPO, Off-policy PPO。
    *   **离线算法**：BPPO, CQL, TD3+BC。

### 4. 资源与算力
论文明确列出了实验所用的硬件配置：
*   **操作系统**：Ubuntu 22.04。
*   **CPU**：Intel Core i9-10980XE。
*   **内存**：128 GB RAM。
*   **GPU**：**NVIDIA RTX A6000 (48 GB 显存)**。
*   **软件框架**：Gymnasium, EnvPool (用于加速环境并行采样), Tianshou (天授) 强化学习库。
*   **训练时长**：Atari 任务运行了 12M 步交互。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了离散、连续以及离线三种主流强化学习场景，每项实验均使用了 **4 个随机种子**以减少偶然性。
*   **消融实验**：针对核心超参数 $M$（历史策略数量）和 $\alpha$（边缘衰减率）进行了详细的参数搜索，验证了默认设置（$M=4, \alpha=5$）的合理性。
*   **公平性**：所有对比算法在相同的网络结构、学习率和并行环境数下运行，并确保每个样本被计算的次数一致，体现了实验的客观性。

### 6. 主要结论与发现
*   **性能提升**：ExO-PPO 在大多数 Atari 游戏和 MuJoCo 任务中，无论是学习速度还是最终得分，均优于原生 PPO 及其先进变体。
*   **稳定性验证**：通过监测概率比率的对数绝对值发现，ExO-PPO 的策略更新比 ESPPO 等算法更平滑，证明了指数衰减边缘能有效抑制策略漂移。
*   **离线适应性**：在不进行额外修改的情况下，ExO-PPO 在离线数据集上的表现优于或等同于专门的离线 RL 算法（如 BPPO），展示了其强大的泛化能力。

### 7. 优点
*   **理论与实证结合**：不仅有严谨的数学证明（扩展下界），还有丰富的实验支撑。
*   **平滑的梯度设计**：用指数衰减代替硬裁剪，解决了 PPO 在训练后期可能出现的梯度丢失问题。
*   **高兼容性**：算法逻辑简单，只需对现有 PPO 框架进行微小改动（修改 Buffer 逻辑和 Loss 函数）即可集成。
*   **样本效率高**：通过重用 $M$ 个策略的数据，显著降低了对环境交互次数的需求。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文给出了推荐值，但在不同任务中，$M$ 和 $\alpha$ 的最优选择可能仍需微调。
*   **数学模糊性**：作者在结论中坦言，在连续任务中，关于分布偏移的影响、初始分布配置以及梯度传播效率等方面仍存在数学上的不确定性，需进一步研究。
*   **计算开销**：虽然样本效率提升了，但由于需要处理历史策略数据，单次迭代的计算复杂度略高于原生 PPO。

（完）
