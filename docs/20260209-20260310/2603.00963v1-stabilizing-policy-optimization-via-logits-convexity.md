---
title: Stabilizing Policy Optimization via Logits Convexity
title_zh: 通过 Logits 凸性稳定策略优化
authors: "Hongzhan Chen, Tao Yang, Yuhua Zhu, Shiping Gao, Xiaojun Quan, Ting Yao"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.00963v1"
tags: ["query:drl"]
score: 9.0
evidence: 通过Logits凸性稳定策略优化
tldr: 针对强化学习（RL）在大型语言模型微调中训练不稳定的问题，本文从梯度角度分析了监督微调（SFT）与RL的稳定性差异。研究发现SFT对Logits的凸性是稳定训练的关键，而PPO等算法缺乏此特性。为此，作者提出了Logits凸优化（LCO）框架，通过将策略与RL目标导出的最优目标对齐，模拟Logits级凸性，显著提升了训练稳定性和性能。
selection_source: fresh_fetch
motivation: 探究为何强化学习在微调大模型时比监督微调更不稳定，并试图通过Logits凸性来弥补这一稳定性差距。
method: 提出Logits凸优化（LCO）框架，通过将学习策略与基于原始RL目标计算的最优目标对齐，引入Logits层面的凸性。
result: 在多个模型系列和基准测试上的实验表明，LCO在提高训练稳定性的同时，性能优于传统的强化学习方法。
conclusion: Logits层面的凸性是实现稳定策略优化的关键，LCO为提升大语言模型强化学习的鲁棒性提供了有效途径。
---

## 摘要
尽管强化学习（RL）在大语言模型（LLM）最近的成功中发挥了核心作用，但 RL 优化以其不稳定性而闻名，尤其是与监督微调（SFT）相比。在这项工作中，我们从基于梯度的视角研究了 SFT 与 RL 之间的稳定性差距，并表明 SFT 损失相对于模型 logits 的凸性在实现稳定训练中起着关键作用。我们的理论分析表明，这一特性在优化过程中诱导了有利的梯度方向性。相比之下，近端策略优化（PPO）作为一种广泛采用的、利用截断代理目标的策略梯度算法，缺乏这种稳定特性。受此观察启发，我们提出了 Logits 凸优化（LCO），这是一个简单而有效的策略优化框架，它将学习到的策略与源自原始 RL 目标的最优目标对齐，从而模拟 logits 级凸性的稳定效果。在多个模型系列上的广泛实验表明，我们的 LCO 框架在各种基准测试中一致地提高了训练稳定性，并优于传统的 RL 方法。

## Abstract
While reinforcement learning (RL) has been central to the recent success of large language models (LLMs), RL optimization is notoriously unstable, especially when compared to supervised fine-tuning (SFT). In this work, we investigate the stability gap between SFT and RL from a gradient-based perspective, and show that the convexity of the SFT loss with respect to model logits plays a key role in enabling stable training. Our theoretical analysis demonstrates that this property induces favorable gradient directionality during optimization. In contrast, Proximal Policy Optimization (PPO), a widely adopted policy gradient algorithm utilizing a clipped surrogate objective, lacks this stabilizing property. Motivated by this observation, we propose Logits Convex Optimization (LCO), a simple yet effective policy optimization framework that aligns the learned policy with an optimal target derived from the original RL objective, thereby emulating the stabilizing effects of logits-level convexity. Extensive experiments across multiple model families show that our LCO framework consistently improves training stability and outperforms conventional RL methods on a broad range of benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stabilizing Policy Optimization via Logits Convexity》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大型语言模型（LLM）在强化学习（RL）对齐过程中存在的**训练不稳定性**问题。
*   **研究动机**：尽管 RL（如 PPO）在模型对齐中至关重要，但其训练过程远比监督微调（SFT）脆弱，经常出现梯度爆炸或训练崩溃。
*   **核心发现**：作者从梯度动力学角度分析发现，SFT 的稳定性源于其损失函数相对于模型 **Logits（未归一化的概率输出）具有凸性（Logits Convexity）**。这种凸性保证了参数空间的梯度方向始终指向近优解。而 PPO 等算法的截断代理目标（Clipped Surrogate Objective）在 Logits 空间是非凸的，导致在处理负优势（Negative Advantage）样本时产生剧烈的梯度波动。

### 2. 方法论：Logits 凸优化（LCO）
作者提出了 **Logits Convex Optimization (LCO)** 框架，旨在将 RL 任务重新表述为一个“最优目标匹配”问题，从而继承 SFT 的稳定性。

*   **核心思想**：首先推导出原始 RL 目标（带 KL 惩罚的优势最大化）下的闭式最优策略 $\pi^*$ 和最优 Logits $z^*$。然后，通过最小化当前模型 Logits 与最优目标之间的差异来训练模型。
*   **关键技术细节**：
    *   **最优目标推导**：基于当前行为策略和优势函数，计算出理论上的最优 Logits $z^* = z_{old} + A/\beta$。
    *   **三种损失函数变体**：
        1.  **LCO-MSE**：直接最小化 Logits 之间的均方误差，具有强凸性，收敛最快。
        2.  **LCO-LCH (Log-Cosh)**：对 Logits 差异使用 Log-Cosh 损失，对异常值更鲁棒。
        3.  **LCO-KLD**：最小化最优分布与当前分布之间的前向 KL 散度，确保概率一致性。
    *   **优势估计**：支持稀疏估计（仅采样动作）、密集估计（基于 Log 概率）和基于偏好的估计（DPO 风格）。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：MATH500, AMC23, MinervaMath。
    *   **阅读理解**：QA-Feedback（评估相关性、事实性、完整性）。
    *   **指令遵循**：AlpacaEval 2.0（使用 GPT-4o 作为裁判）。
*   **基准模型**：涵盖 Qwen-2.5 (3B/7B), Qwen-3 (4B/8B), Llama-3.1/3.2 (3B/8B), Mistral-3 (3B/8B) 等多个系列。
*   **对比方法**：
    *   传统 RL：REINFORCE, PPO。
    *   稳定性增强型 RL：GRPO, DAPO, GSPO。
    *   在线蒸馏方法：MiniLLM, GKD。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了序列长度为 2048，采样温度 0.6，Top-p 0.95，学习率为 5e-6，使用 TRL 库进行开发。

### 5. 实验数量与充分性
*   **实验规模**：实验非常充分。作者在 3 个大类任务、5 个以上主流基准测试、4 种不同架构的模型上进行了验证。
*   **消融与分析**：
    *   进行了梯度动力学分析，对比了 PPO 与 LCO 的梯度范数演变。
    *   测试了不同优势估计策略（稀疏 vs 密集）下的表现。
    *   对比了样本效率，证明 LCO-LCH 的收敛速度可达 PPO 的 3 倍。
*   **客观性**：所有实验均报告了四个随机种子的均值和标准差，确保了结果的可靠性和公平性。

### 6. 主要结论与发现
*   **稳定性**：LCO 成功消除了 PPO 中常见的梯度尖峰，训练轨迹极其平滑。
*   **性能优越性**：在数学推理和指令遵循任务中，LCO 一致优于 PPO 和 GRPO，甚至在某些指标上超过了作为奖励模型的 DPO 模型。
*   **收敛速度**：由于 Logits 空间的强凸性，LCO 具有更高的样本效率，能以更少的训练步数达到更高的精度。
*   **理论验证**：证明了 Logits 凸性是防止 RL 训练崩溃的关键几何属性。

### 7. 优点
*   **理论深刻**：不仅提出了新算法，还从数学上解释了为什么传统 RL 在 LLM 上不稳定，填补了该领域的理论空白。
*   **简单有效**：LCO 不需要复杂的截断机制或价值函数估计（Value Function），实现难度低，易于集成到现有框架。
*   **鲁棒性强**：在不同规模、不同任务的模型上表现出极高的一致性。

### 8. 不足与局限
*   **优势函数依赖**：LCO 的效果高度依赖于优势函数 $A$ 的质量。如果奖励模型（RM）本身存在偏差，LCO 可能会快速收敛到错误的方向。
*   **词表规模挑战**：对于 LCO-MSE 等需要遍历整个词表的损失函数，如果词表（Vocabulary）极大，计算开销可能会增加（虽然文中通过采样动作缓解了此问题）。
*   **超参数敏感性**：虽然比 PPO 稳定，但 $\beta$（KL 惩罚系数）的选择仍会对最终性能产生显著影响。

（完）
