---
title: "TempoNet: Slack-Quantized Transformer-Guided Reinforcement Scheduler for Adaptive Deadline-Centric Real-Time Dispatchs"
title_zh: TempoNet：用于自适应以截止日期为中心的实时调度的松弛量化 Transformer 引导强化学习调度器
authors: "Rong Fu, Yibo Meng, Guangzhen Yao, Jiaxuan Lu, Zeyu Zhang, Zhaolu Kang, Ziming Guo, Jia Yee Tan, Xiaojing Du, Simon James Fong"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.18109v1"
tags: ["query:drl-jsp"]
score: 7.0
evidence: 用于实时任务调度的强化学习调度器
tldr: 本研究针对严格计算预算下的实时调度挑战，提出TempoNet框架。该框架结合强化学习与置换不变Transformer，通过紧急度分词器将时间松弛量离散化为可学习嵌入，并利用稀疏注意力机制实现亚毫秒级推理。TempoNet在多核映射和截止日期满足率上表现卓越，在工业级混合关键性任务中展现出优于传统及神经调度器的性能与稳定性，为高吞吐量实时决策提供了实用方案。
selection_source: fresh_fetch
motivation: 实时调度器需要在有限的计算资源内，针对具有紧迫截止日期的复杂任务集做出高效决策。
method: 采用基于Transformer的强化学习架构，通过紧急度分词器量化松弛时间，并结合稀疏注意力机制优化多核任务分配。
result: 实验表明，该方法在截止日期达成率上显著提升，且具备亚毫秒级推理速度和良好的扩展性。
conclusion: TempoNet证明了Transformer在处理高吞吐量实时调度任务中的可行性与优越性，实现了性能与效率的平衡。
---

## 摘要
实时调度器必须在严格的计算预算下处理紧迫的截止日期。我们提出了 TempoNet，这是一种将置换不变 Transformer 与深度 Q 近似相结合的强化学习调度器。紧急度分词器（Urgency Tokenizer）将时间松弛量离散化为可学习的嵌入，从而稳定价值学习并捕捉截止日期的临近程度。具有分块 top-k 选择和局部敏感分块的延迟感知稀疏注意力栈，能够对无序任务集进行全局推理，并实现近线性扩展和亚毫秒级推理。多核映射层通过掩码贪婪选择或可微分匹配，将上下文相关的 Q 分数转换为处理器分配。在工业混合关键性轨迹和大型多处理器设置上的广泛评估表明，与分析调度器和神经基准模型相比，该方法在截止日期达成率方面具有持续提升，并改善了优化稳定性。诊断分析包括松弛量化的敏感性分析、注意力驱动的策略解释、硬件在环和内核微基准测试，以及在压力下通过简单运行时缓解措施表现出的鲁棒性；我们还报告了行为克隆预训练带来的样本效率提升，以及在不改变推理流水线的情况下与 actor-critic 变体的兼容性。这些结果为高吞吐量实时调度中基于 Transformer 的决策建立了一个实用的框架。

## 速览
**TLDR**：本研究针对实时调度中严格截止日期与计算预算的矛盾，提出TempoNet框架。该框架将强化学习与置换不变Transformer结合，通过紧急程度分词器将时间松弛量离散化为可学习嵌入，并利用稀疏注意力机制实现近线性扩展和亚毫秒级推理。TempoNet在工业级混合关键性任务中表现出色，显著提升了截止日期完成率和系统稳定性，为高吞吐量实时调度提供了高效的决策方案。 \
**Motivation**：传统实时调度器难以在严格的计算预算下高效处理具有紧迫截止日期的复杂任务集。 \
**Method**：采用基于Transformer的强化学习架构，结合紧急程度分词器、稀疏注意力机制和多核映射层进行任务调度。 \
**Result**：实验表明，TempoNet在截止日期达成率上优于现有解析和神经调度器，且具备亚毫秒级的推理速度。 \
**Conclusion**：该研究证明了Transformer在处理高吞吐量实时调度决策中的实用性，并展示了其在硬件在环测试中的鲁棒性。

---

## Abstract
Real-time schedulers must reason about tight deadlines under strict compute budgets. We present TempoNet, a reinforcement learning scheduler that pairs a permutation-invariant Transformer with a deep Q-approximation. An Urgency Tokenizer discretizes temporal slack into learnable embeddings, stabilizing value learning and capturing deadline proximity. A latency-aware sparse attention stack with blockwise top-k selection and locality-sensitive chunking enables global reasoning over unordered task sets with near-linear scaling and sub-millisecond inference. A multicore mapping layer converts contextualized Q-scores into processor assignments through masked-greedy selection or differentiable matching. Extensive evaluations on industrial mixed-criticality traces and large multiprocessor settings show consistent gains in deadline fulfillment over analytic schedulers and neural baselines, together with improved optimization stability. Diagnostics include sensitivity analyses for slack quantization, attention-driven policy interpretation, hardware-in-the-loop and kernel micro-benchmarks, and robustness under stress with simple runtime mitigations; we also report sample-efficiency benefits from behavioral-cloning pretraining and compatibility with an actor-critic variant without altering the inference pipeline. These results establish a practical framework for Transformer-based decision making in high-throughput real-time scheduling.

---

## 论文详细总结（自动生成）

### 论文总结：TempoNet —— 用于自适应实时调度的松弛量化 Transformer 引导强化学习调度器

#### 1. 核心问题与整体含义
实时系统（如嵌入式 AI、自动驾驶、工业控制）要求调度器在极短的时间预算（亚毫秒级）内，针对动态变化的负载做出决策，以确保任务在截止日期（Deadline）前完成。
*   **研究动机**：传统的调度算法（如 EDF、RM）在理想假设下表现良好，但在任务过载或执行时间不确定时性能急剧下降。
*   **现有局限**：现有的强化学习（RL）调度器通常依赖固定大小的向量或序列编码，存在顺序依赖性，且难以在严格的实时延迟约束下处理大规模无序任务集。
*   **核心目标**：设计一种既能进行全局推理（Transformer 架构），又能保持极低推理延迟，且对任务顺序不敏感的价值驱动型 RL 调度器。

#### 2. 方法论
TempoNet 结合了 Transformer 的全局表示能力与深度 Q 学习（DQN）的决策框架，其核心组件包括：
*   **紧急度分词器（Urgency Tokenizer, UT）**：这是论文的关键创新。它将连续的任务松弛时间（Slack）通过量化公式 $\tilde{s}_i(t) = \text{clip}(\lfloor s_i(t)/\Delta \rfloor, 0, Q-1)$ 离散化，并映射为可学习的嵌入向量（Embedding）。这稳定了价值函数的学习，并使模型能聚焦于截止日期临近的任务组。
*   **置换不变 Transformer 编码器**：采用不含位置编码的 Transformer 结构，确保对无序任务集的处理具有置换不变性。
*   **延迟感知稀疏注意力（Sparse Attention）**：为了处理大规模任务集（如 600 个任务），采用了分块 Top-k 选择和局部敏感分块技术，将注意力复杂度从 $O(N^2)$ 降低到接近线性的 $O(N^{1.1})$，实现了亚毫秒级推理。
*   **多核映射层**：将 Transformer 输出的 Q 分数通过“掩码贪婪选择”（Masked-Greedy）或“二分匹配”转换为多处理器核心的任务分配。
*   **算法流程**：系统观察状态 -> UT 量化松弛时间 -> Transformer 提取上下文特征 -> 投影得到各任务 Q 值 -> 多核映射执行动作 -> 存入经验回放池并更新网络。

#### 3. 实验设计
论文在多种复杂场景下进行了验证：
*   **数据集/场景**：
    1.  **单处理器周期性调度**：标准任务配置及 200 个随机生成的异构任务集。
    2.  **工业多核工作负载**：包含混合关键性（Mixed-Criticality）任务的工业轨迹。
    3.  **大规模多处理器系统**：任务数量从 100 扩展至 600 个。
*   **Benchmark 与对比方法**：
    *   **经典算法**：RM、EDF、FCFS。
    *   **深度学习基准**：FF-DQN、PPO、A3C、Rainbow DQN、离线 RL。
    *   **前沿学术方法**：基于图神经网络（GNN）的调度器、基于 Transformer 的 DRL 调度器（如 DIOS、MHQISSO）。

#### 4. 资源与算力
*   **硬件设备**：
    *   **微基准测试与训练**：使用了 NVIDIA V100 GPU (CUDA 12.2)。
    *   **硬件在环（HIL）测试**：在嵌入式平台 ARM Cortex-A78 和 NVIDIA Tegra Orin Nano 上进行了实测。
*   **算力细节**：文中未明确给出总训练时长（如总 GPU 小时数），但详细记录了推理延迟。例如，在 Tegra 平台上，处理 600 个任务的端到端延迟约为 375μs，满足 1ms 的实时调度周期要求。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 200 组随机任务集的统计分析、大规模（600 任务）的压力测试。
*   **消融实验**：涵盖了架构深度（层数）、注意力头数、嵌入维度、量化箱（Bin）宽度、量化方案（均匀 vs 对数 vs K-means）等多个维度的消融。
*   **充分性评价**：实验设计非常充分且客观。除了性能指标，还包含了注意力机制的可解释性分析（热力图）、非平稳负载下的鲁棒性测试、以及行为克隆（BC）预训练对样本效率的影响分析。

#### 6. 主要结论与发现
*   **性能提升**：在过载情况下（利用率 > 1.0），TempoNet 的截止日期达成率达到 79%，远高于 EDF 和 RM 的 11.67%，也优于 FF-DQN 的 71.43%。
*   **工业适用性**：在 600 任务的大规模场景下，成功率达到 90.1%，且计算复杂度显著低于同类竞争方法。
*   **稳定性**：紧急度分词器（UT）显著降低了训练过程中的梯度方差，使价值学习更加稳定。
*   **实时性**：通过稀疏注意力优化，模型在嵌入式硬件上实现了亚毫秒级的决策速度。

#### 7. 优点
*   **创新的量化表示**：UT 解决了连续时间特征在 RL 中难以学习的问题。
*   **高效的稀疏架构**：针对调度任务定制的稀疏注意力机制，平衡了全局推理能力与计算开销。
*   **置换不变性**：完美契合调度任务中“任务集无序”的本质属性。
*   **软硬件结合**：不仅有理论推导，还有详细的嵌入式硬件实测数据，证明了实用性。

#### 8. 不足与局限
*   **量化偏差风险**：虽然量化提升了稳定性，但过粗的量化粒度可能导致决策精度损失（论文通过消融实验部分缓解了此疑虑）。
*   **模型复杂度**：尽管推理极快，但相比于简单的启发式规则，Transformer 模型的部署和维护成本更高。
*   **异构硬件支持**：目前的实验主要集中在相同核心（Identical Cores）的处理器上，对于异构多处理器系统（如大小核架构）的适配细节讨论较少。
*   **能耗考虑**：虽然提到了能耗感知奖励，但缺乏与专用低功耗调度算法在实际功耗上的深度对比。

（完）
