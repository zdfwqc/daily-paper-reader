---
title: "It Takes Two to Tango: A Holistic Simulator for Joint Order Scheduling and Multi-Agent Path Finding in Robotic Warehouses"
title_zh: 相辅相成：机器人仓库中订单调度与多智能体路径规划联合优化的整体模拟器
authors: "Haozheng Xu, Wenhao Li, Zifan Wei, Bo Jin, Hongxing Bai, Ben Yang, Xiangfeng Wang"
date: 2026-02-15
pdf: "https://arxiv.org/pdf/2602.13999v1"
tags: ["query:drl-jsp"]
score: 8.0
evidence: 仓库中的联合订单调度与路径规划
tldr: 针对机器人移动履行系统（RMFS）中订单调度（OS）与多智能体路径规划（MAPF）长期解耦导致的效率瓶颈，本文推出了WareRover仿真平台。该平台打破了传统研究中将两者视为孤立子问题的局限，通过统一的闭环优化接口实现了OS与MAPF的紧耦合。WareRover集成了动态订单流、物理感知运动约束及随机故障恢复机制，为下一代仓库协调算法提供了更真实、更具挑战性的测试环境。
selection_source: fresh_fetch
motivation: 现有的RMFS研究通常将订单调度与路径规划解耦，忽略了高层调度与底层拥堵之间的关键依赖关系，且缺乏对真实物理约束的模拟。
method: 提出WareRover仿真平台，通过统一的闭环优化接口将OS与MAPF紧密结合，并引入异构动力学、物理感知运动约束和非标恢复机制。
result: 实验表明，现有的最先进算法在WareRover提供的真实耦合约束下往往表现不佳，暴露出传统解耦方法的局限性。
conclusion: WareRover为开发和评估鲁棒的、下一代仓库协调算法提供了一个必要且具有挑战性的整体仿真基准。
---

## 摘要
机器人移动履行系统（RMFS）中的主流范式通常将订单调度和多智能体路径规划（MAPF）视为孤立的子问题。我们认为这种解耦是一个根本性的瓶颈，掩盖了高层调度与底层拥堵之间的关键依赖关系。现有的模拟器未能弥合这一差距，通常抽象掉了异构运动学和随机执行故障。我们提出了 WareRover，这是一个整体模拟平台，通过统一的闭环优化接口实现了订单调度（OS）与多智能体路径规划（MAPF）之间的紧密耦合。与标准基准测试不同，WareRover 将动态订单流、物理感知运动约束和非标恢复机制集成到单个评估循环中。实验表明，最先进（SOTA）算法在这些现实的耦合约束下往往表现不佳，这证明了 WareRover 为稳健的下一代仓库协调提供了一个必要且具有挑战性的测试平台。项目和视频可在 https://hhh-x.github.io/WareRover/ 获取。

## 速览
**TLDR**：针对机器人移动履行系统（RMFS）中订单调度（OS）与多智能体路径规划（MAPF）长期解耦导致的效率瓶颈，本文推出了WareRover仿真平台。该平台打破了传统研究中将两者视为孤立子问题的局限，通过统一的闭环优化接口实现了OS与MAPF的紧耦合。WareRover集成了动态订单流、物理感知运动约束及随机故障恢复机制，为下一代鲁棒性仓库协调算法提供了更具挑战性且贴近现实的测试环境。 \
**Motivation**：现有的RMFS研究通常将订单调度与路径规划解耦，忽略了高层调度与底层拥堵之间的关键依赖关系，且缺乏对物理约束和随机故障的模拟。 \
**Method**：提出WareRover仿真平台，通过统一的闭环优化接口将OS与MAPF紧密结合，并整合了异构动力学、动态订单流和非标恢复机制。 \
**Result**：实验表明，现有的最先进算法在WareRover提供的现实耦合约束下往往表现不佳，暴露出传统解耦方法的局限性。 \
**Conclusion**：WareRover为开发和评估更具鲁棒性的下一代仓库协调算法提供了一个必要且具有挑战性的综合性实验平台。

---

## Abstract
The prevailing paradigm in Robotic Mobile Fulfillment Systems (RMFS) typically treats order scheduling and multi-agent pathfinding as isolated sub-problems. We argue that this decoupling is a fundamental bottleneck, masking the critical dependencies between high-level dispatching and low-level congestion. Existing simulators fail to bridge this gap, often abstracting away heterogeneous kinematics and stochastic execution failures. We propose WareRover, a holistic simulation platform that enforces a tight coupling between OS and MAPF via a unified, closed-loop optimization interface. Unlike standard benchmarks, WareRover integrates dynamic order streams, physics-aware motion constraints, and non-nominal recovery mechanisms into a single evaluation loop. Experiments reveal that SOTA algorithms often falter under these realistic coupled constraints, demonstrating that WareRover provides a necessary and challenging testbed for robust, next-generation warehouse coordination. The project and video is available at https://hhh-x.github.io/WareRover/.

---

## 论文详细总结（自动生成）

### 论文总结：WareRover —— 机器人仓库中订单调度与路径规划联合优化的整体模拟器

#### 1. 研究动机与背景
在机器人移动履行系统（RMFS）中，传统的做法通常将**订单调度（OS）**和**多智能体路径规划（MAPF）**视为两个独立的子问题。这种“解耦”模式导致了严重的效率瓶颈：高层调度往往忽略了底层AGV（自动导引车）在密集环境下的拥堵情况，而底层的路径规划又缺乏对动态订单流和仓库业务逻辑的理解。此外，现有模拟器要么过于抽象（如网格世界），要么忽略了现实中的异构动力学约束和随机执行故障（如机器人损坏、延迟）。本文旨在通过推出 **WareRover** 平台，弥合高层逻辑与底层执行之间的鸿沟。

#### 2. 核心方法论
WareRover 的核心思想是实现 OS 与 MAPF 的**紧耦合闭环优化**。其架构包含五个关键模块：
*   **仓库环境构建器（Environment Builder）**：支持自定义布局（通道、货架、工作站），并能模拟具有不同物理尺寸、速度和运动约束的异构 AGV 舰队。
*   **任务与订单模块（Task & Order Module）**：模拟真实的电商工作负载（如波次订单、热点 SKU、突发流量），支持多阶段任务（拣选、交付）的分解。
*   **联合优化接口（Joint Optimization）**：在每个仿真步长（Step-level）中，调度器根据全局状态更新任务分配，路径规划器则根据分配生成无碰撞轨迹。两者共享全局信息，允许调度策略显式考虑交通成本。
*   **物理运动执行器（Physical Motion Executor）**：将离散计划转换为满足动力学约束（速度限制、转弯半径）的连续轨迹，并进行连续时间碰撞检测。
*   **故障模拟与恢复（Failure Simulation & Recovery）**：支持随机或按需触发 AGV 故障，并生成“安全走廊”供人工维修，强制其他机器人绕行，测试系统的鲁棒性。

#### 3. 实验设计
*   **场景设置**：
    *   **同构环境（Ho）**：所有 AGV 尺寸相同。
    *   **异构环境（He）**：混合不同尺寸的 AGV 和货物（1x1 和 2x2）。
    *   **容错环境（FT）**：引入 1% 的随机故障概率，故障持续 40 步。
*   **对比方法**：
    *   **调度策略**：随机分配（RD）与订单调度（TA）。
    *   **MAPF 算法**：A*（搜索类）、CBS（基于冲突的搜索）、DHC（分布式强化学习类）。
*   **评估指标**：成功率（SR）、计算时间（CT）、吞吐量（TP）。
*   **基准测试**：在 20x15 的地图上进行，包含 9 台 AGV、32 个货架和 8 个接收区，每组配置重复 100 次。

#### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/仿真所需的总时长。由于该平台主要作为仿真基准，其核心开销在于大规模 MAPF 算法的在线计算和物理引擎的碰撞检测。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单到复杂（同构到异构再到故障）的三个递进场景，并对比了经典搜索算法与现代学习算法。
*   **充分性**：每种配置重复 100 次实验，保证了统计意义上的客观性。
*   **公平性**：通过统一的 WareRover 接口评估不同算法，确保了环境约束（如物理碰撞、任务流）对所有算法是一致的。

#### 6. 主要结论与发现
*   **耦合的重要性**：在异构和故障场景下，简单的解耦方法表现大幅下降。
*   **算法表现差异**：
    *   **CBS** 在异构环境下虽然计算量增加，但通过显式的冲突推理，其成功率远高于 **DHC**。
    *   **DHC**（学习类算法）在处理大型异构智能体时表现不佳，显示出泛化能力的局限。
    *   在**故障环境**下，重规划效率成为关键。A* 因其轻量级的局部重规划在计算时间上占优，而 CBS 在频繁故障下的计算开销剧增。
*   **现实挑战**：实验证明，现有的 SOTA 算法在面对真实物理约束和随机故障时仍有很大提升空间。

#### 7. 优点
*   **高保真度**：相比传统的网格模拟器，WareRover 引入了连续空间、异构动力学和物理碰撞，更接近真实仓库。
*   **闭环设计**：首次在仿真层面强制要求调度与规划的实时交互，为研究“感知拥堵的调度”提供了工具。
*   **鲁棒性测试**：内置的故障恢复机制是现有开源模拟器中较为少见的功能，对工业落地具有重要参考价值。

#### 8. 不足与局限
*   **实验地图规模较小**：论文展示的实验主要在 20x15 的小型地图上进行，虽然声称支持工业级规模，但尚未给出超大规模集群（如数百台机器人）下的性能表现。
*   **算法覆盖有限**：目前仅对比了三种基础 MAPF 算法，未涵盖最新的终身式 MAPF（Lifelong MAPF）或更复杂的混合调度算法。
*   **算力细节缺失**：缺乏对仿真器运行效率（如每秒能模拟多少步）的量化描述，难以评估其在大规模强化学习训练中的实用性。

（完）
