---
title: "Mamba Meets Scheduling: Learning to Solve Flexible Job Shop Scheduling with Efficient Sequence Modeling"
title_zh: Mamba 遇上调度：利用高效序列建模学习求解柔性作业车间调度问题
authors: "Zhi Cao, Cong Zhang, Yaoxin Wu, Yaqing Hou, Hongwei Ge"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21546v1"
tags: ["query:drl-jsp"]
score: 10.0
evidence: 使用 Mamba 架构学习解决柔性作业车间调度问题
tldr: 针对柔性作业车间调度问题（FJSP），本文提出了一种基于 Mamba 状态空间模型的新型架构。针对现有学习方法在捕捉全局依赖关系和计算效率方面的局限，该模型通过双 Mamba 编码器分别提取工序和机器特征，并结合交叉注意力解码器进行交互建模。实验证明，该方法在保持线性计算复杂度的同时，求解速度和性能均优于现有的 SOTA 学习算法。
selection_source: fresh_fetch
motivation: 现有的 FJSP 学习方法多依赖局部特征提取，难以捕捉工序与机器间的全局长程依赖，且计算开销较大。
method: 提出一种包含双 Mamba 编码器（分别处理工序和机器序列）和高效交叉注意力解码器的端到端架构。
result: 实验结果显示，该方法在多个基准测试中实现了更快的求解速度，并超越了现有的最先进学习调度算法。
conclusion: Mamba 模型在处理复杂组合优化问题（如 FJSP）时展现出卓越的序列建模能力和计算效率优势。
---

## 摘要
柔性作业车间调度问题（FJSP）是一个经过深入研究的组合优化问题，在制造和生产调度中具有广泛的应用。它涉及将工件分配给各种机器，以优化诸如最小化总完工时间等准则。该领域目前基于学习的方法通常依赖于局部特征提取模型，限制了它们捕获跨工序和机器的全局依赖关系的能力。本文介绍了一种创新架构，利用具有线性计算复杂度的状态空间模型 Mamba，为 FJSP 量身定制了全面的序列建模。与目前在 FJSP 中计算密集的主流基于图注意力的框架相比，我们展示了我们的模型更加高效。具体而言，所提出的模型包含一个编码器和一个解码器。编码器结合了双 Mamba 模块，分别提取工序和机器特征。此外，我们引入了一个高效的交叉注意力解码器，以学习工序和机器的交互嵌入。我们的实验结果表明，在各种基准测试中，我们的方法实现了更快的求解速度，并超越了目前最先进的基于学习的 FJSP 方法的性能。

## Abstract
The Flexible Job Shop Problem (FJSP) is a well-studied combinatorial optimization problem with extensive applications for manufacturing and production scheduling. It involves assigning jobs to various machines to optimize criteria, such as minimizing total completion time. Current learning-based methods in this domain often rely on localized feature extraction models, limiting their capacity to capture overarching dependencies spanning operations and machines. This paper introduces an innovative architecture that harnesses Mamba, a state-space model with linear computational complexity, to facilitate comprehensive sequence modeling tailored for FJSP. In contrast to prevalent graph-attention-based frameworks that are computationally intensive for FJSP, we show our model is more efficient. Specifically, the proposed model possesses an encoder and a decoder. The encoder incorporates a dual Mamba block to extract operation and machine features separately. Additionally, we introduce an efficient cross-attention decoder to learn interactive embeddings of operations and machines. Our experimental results demonstrate that our method achieves faster solving speed and surpasses the performance of state-of-the-art learning-based methods for FJSP across various benchmarks.

---

## 论文详细总结（自动生成）

这篇论文《Mamba Meets Scheduling: Learning to Solve Flexible Job Shop Scheduling with Efficient Sequence Modeling》探索了将先进的状态空间模型 Mamba 应用于复杂的柔性作业车间调度问题（FJSP）。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：FJSP 是制造和生产调度中的核心 NP-hard 问题，涉及工序排序和机器分配两个维度的决策。
*   **核心痛点**：现有的深度强化学习（DRL）方法多采用图神经网络（GNN）或图注意力网络（GAT）来处理调度状态。这些方法存在两个主要缺陷：
    1.  **计算开销大**：图结构的构建和注意力机制的平方级复杂度限制了其在大规模问题上的应用。
    2.  **局部性限制**：GNN 依赖 K-hop 消息传递，难以捕捉工序与机器序列之间的全局长程依赖关系，且易受过度平滑（Over-smoothing）影响。
*   **研究动机**：验证具有线性复杂度的 Mamba 模型是否能比图模型更高效、更全面地建模调度序列。

### 2. 方法论
论文提出了 **Mamba-CrossAttention (M-CA)** 架构，采用编码器-解码器结构：
*   **MDP 建模**：将 FJSP 建模为马尔可夫决策过程，动作空间为可选的“工序-机器”对，奖励函数基于 Makespan（完工时间）的减量。
*   **双 Mamba 编码器 (Dual Mamba Encoder)**：
    *   分别使用两个独立的 Mamba 模块处理**工序序列**和**机器序列**。
    *   利用 Mamba 的选择性扫描机制（Selective Scan），在保持线性时间复杂度 $O(N)$ 的同时提取全局特征。
*   **交叉注意力解码器 (Cross-Attention Decoder)**：
    *   为了模拟工序与机器之间的双向选择，引入轻量级交叉注意力。
    *   第一层：以机器特征为 Query，工序特征为 Key/Value，学习机器对工序的偏好。
    *   第二层：以工序特征为 Query，融合后的机器特征为 Key/Value，实现特征深度交互。
*   **决策网络**：将提取的嵌入向量与候选动作特征拼接，通过 MLP 输出动作概率分布。
*   **训练算法**：使用 PPO（近端策略优化）算法进行端到端训练。

### 3. 实验设计
*   **数据集**：
    *   **公开基准测试集**：Barnes, Brandimarte, Dauzere, 以及 Hurink 的三个子集（rdata, edata, vdata）。
    *   **合成数据**：从 10×5 到 40×10 的不同规模实例。
    *   **超大规模泛化测试**：从 100×10 到 10,000×10 的极端规模。
*   **对比方法 (Benchmarks)**：
    *   **精确算法**：Google OR-Tools（设置 1800s 时限）。
    *   **启发式规则 (PDRs)**：FIFO, MOPNR, SPT, MWKR。
    *   **学习算法**：HGNN（基于异构图）、DAN（基于双注意力网络）、MLP。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 3090 GPU**。
*   **训练配置**：训练 10,000 个迭代，每个迭代包含 20 条独立轨迹（实例）。
*   **模型参数**：嵌入维度为 128，Mamba 块数量为 1，交叉注意力头数为 8。
*   **系统环境**：测试超大规模问题时使用了 128GB 系统内存。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型（10×5）到超大型（10,000×10）的广泛场景，实验组数非常充分。
*   **消融实验**：对比了纯 Mamba 编码器（DME）、多层 Mamba 堆叠以及纯交叉注意力（CA）的效果，验证了 M-CA 组合的优越性。
*   **泛化性验证**：除了 FJSP，还额外在 JSSP（标准作业车间调度）基准集（Taillard, DMU）上进行了测试，证明了模型的跨问题迁移能力。
*   **客观性**：采用了 Greedy 和 Sampling 两种推理策略，并对比了求解质量（Gap）与求解时间，评价维度全面。

### 6. 主要结论与发现
*   **性能领先**：M-CA 在几乎所有公开基准测试中均优于现有的学习方法（如 DAN 和 HGNN）。
*   **效率优势**：相比基于图注意力的 DAN，M-CA 实现了约 **1.6x 到 2x 的加速**。
*   **超强扩展性**：在 10,000 个工序的超大规模实例上，OR-Tools 崩溃（内存溢出），HGNN 显存溢出，而 M-CA 依然能稳定输出高质量解。
*   **架构发现**：单层 Mamba 块已足够强大，增加层数并未显著提升性能，反而增加了推理延迟。

### 7. 优点
*   **架构创新**：首次将 Mamba 引入离散组合优化调度领域，打破了图模型的统治地位。
*   **线性复杂度**：成功解决了大规模调度场景下的计算瓶颈，使得实时调度成为可能。
*   **全局建模**：通过序列建模而非图邻域建模，更有效地捕捉了工序与机器间的隐式关联。

### 8. 不足与局限
*   **环境交互瓶颈**：虽然模型推理很快，但实验显示 Sampling 策略下的总耗时主要被 Python 环境的串行交互占据，环境模拟器的效率限制了整体速度。
*   **特征依赖**：模型仍依赖于手工设计的原始特征（如剩余工作量、等待时间等），尚未实现完全的端到端原始数据处理。
*   **应用限制**：目前主要针对 Makespan 优化，对于多目标调度或其他复杂约束（如机器故障、动态到达）的验证尚不充分。

（完）
