---
title: Learning Flexible Job Shop Scheduling under Limited Buffers and Material Kitting Constraints
title_zh: 考虑有限缓冲区和物料齐套约束的柔性作业车间调度学习
authors: "Shishun Zhang, Juzhan Xu, Yidan Fan, Chenyang Zhu, Ruizhen Hu, Yongjun Wang, Kai Xu"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.24180v1"
tags: ["query:drl-jsp"]
score: 10.0
evidence: 使用异构图深度强化学习解决柔性作业车间调度
tldr: 针对柔性作业车间调度（FJSP）中常被忽视的有限缓冲区和物料配套约束，本文提出了一种基于深度强化学习（DRL）的调度方法。通过引入异构图网络对机器、工序和缓冲区进行全局建模，优化长序列调度中的托盘更换频率和缓冲区利用率。实验证明，该方法在完工时间和托盘更换次数上优于传统启发式及现有DRL算法，在求解质量与计算效率间取得了良好平衡。
selection_source: fresh_fetch
motivation: 现有的FJSP研究往往忽略了有限缓冲区和物料配套等实际约束，导致生产效率受限且状态建模能力不足。
method: 在深度强化学习框架下利用异构图网络构建机器、工序与缓冲区间的消息传递机制，以实现全局状态建模。
result: 在合成及真实生产线数据集上，该方法在完工时间和托盘更换指标上均优于传统启发式算法和先进的DRL方法。
conclusion: 该研究通过异构图建模有效解决了复杂约束下的调度问题，为提升实际生产线的缓冲区利用率和决策质量提供了高效方案。
---

## 摘要
柔性作业车间调度问题（FJSP）源于真实的生产线，但在目前的 FJSP 研究中，一些实际约束往往被忽略或理想化，其中有限缓冲区问题对生产效率有着特殊的影响。为此，我们研究了一个更接近实际场景的扩展问题——具有有限缓冲区和物料齐套约束的柔性作业车间调度问题。近年来，深度强化学习（DRL）在调度任务中展现出了巨大的潜力。然而，在处理复杂的依赖关系和长期约束时，其状态建模能力仍然有限。为了解决这一问题，我们在 DRL 框架内利用异构图网络来建模全局状态。通过在机器、工序和缓冲区之间构建高效的消息传递，该网络专注于避免在长序列调度过程中可能导致频繁更换托盘的决策，从而有助于提高缓冲区利用率和整体决策质量。在合成数据集和真实生产线数据集上的实验结果表明，所提方法在完工时间（makespan）和托盘更换次数方面优于传统的启发式方法和先进的 DRL 方法，并在求解质量和计算成本之间取得了良好的平衡。此外，还提供了一个补充视频，展示了一个能够有效可视化生产线进展的仿真系统。

## Abstract
The Flexible Job Shop Scheduling Problem (FJSP) originates from real production lines, while some practical constraints are often ignored or idealized in current FJSP studies, among which the limited buffer problem has a particular impact on production efficiency. To this end, we study an extended problem that is closer to practical scenarios--the Flexible Job Shop Scheduling Problem with Limited Buffers and Material Kitting. In recent years, deep reinforcement learning (DRL) has demonstrated considerable potential in scheduling tasks. However, its capacity for state modeling remains limited when handling complex dependencies and long-term constraints. To address this, we leverage a heterogeneous graph network within the DRL framework to model the global state. By constructing efficient message passing among machines, operations, and buffers, the network focuses on avoiding decisions that may cause frequent pallet changes during long-sequence scheduling, thereby helping improve buffer utilization and overall decision quality. Experimental results on both synthetic and real production line datasets show that the proposed method outperforms traditional heuristics and advanced DRL methods in terms of makespan and pallet changes, and also achieves a good balance between solution quality and computational cost. Furthermore, a supplementary video is provided to showcase a simulation system that effectively visualizes the progression of the production line.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Flexible Job Shop Scheduling under Limited Buffers and Material Kitting Constraints》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**带有限缓冲区和物料齐套约束的柔性作业车间调度问题（FJSP-LB-MK）**。
*   **研究动机**：传统的 FJSP 研究往往理想化了资源约束，忽略了现实生产中（如钢板加工、零件分拣）缓冲区有限且必须遵循“物料齐套”规则（即一个托盘只能存放同类零件）的限制。
*   **核心挑战**：当缓冲区（托盘）不足以容纳多样化的零件类型时，会引发频繁的托盘更换（Pallet Changes），导致生产停滞和效率下降。现有的深度强化学习（DRL）方法难以捕捉这种长期的、非局部的资源依赖关系。

### 2. 方法论
论文提出了一种基于 **DRL 和增强型异构图神经网络（HGNN）** 的调度框架：
*   **状态建模（异构图）**：构建了一个包含机器、工序和缓冲区（Buffer）三类节点的有向异构图。
*   **核心思想——成本敏感传播（Cost-sensitive Propagation）**：
    *   **选择性连接**：缓冲区节点仅与“零件分拣工序”节点连接，以减少噪声并突出关键约束。
    *   **动态权重**：引入 `SwEst`（估计托盘更换数）指标。通过 Sigmoid 函数将 `SwEst` 转化为边权重，实现“避灾策略”（Cost-avoiding），使模型在聚合特征时能感知高昂的更换成本。
*   **决策过程**：将工序选择与机器分配整合为复合动作空间。使用 **PPO（近端策略优化）** 算法进行训练。
*   **奖励函数**：设计了双目标奖励函数，同时考虑完工时间（Makespan）的减少量和托盘更换次数的减少量，通过权重因子 $\lambda$ 进行平衡。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：基于 Brandimarte 标准生成，涵盖 6 种规模（从 10任务×5机器 到 40任务×10机器）。
    *   **真实生产线数据集**：采集自 4 条真实的工业钢板加工线，包含实际的机器映射和零件类别分布。
*   **Benchmark（对比方法）**：
    *   **精确算法**：Google OR-Tools（带 1800s 时间限制）。
    *   **启发式规则（PDRs）**：FIFO, MOR, SPT, MWR, LWR。
    *   **先进 DRL 方法**：Song 等人提出的基于 HGNN 的 FJSP 调度算法（2022）。

### 4. 资源与算力
*   **硬件环境**：NVIDIA GeForce RTX 3080 Ti GPU，Intel Core i9-10980XE CPU。
*   **训练细节**：
    *   在 10×5 的小规模合成实例上进行预训练。
    *   在真实数据集上进行微调，并引入 **KL 散度正则化** 以防止策略更新过快导致性能恶化。
*   **时长说明**：文中未明确给出总训练时长，但提到推理时间为秒级。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个合成规模和 4 个真实场景下进行了详尽测试。
*   **消融实验**：针对状态特征（PS, Type, SwEst）、连接策略（全连接 vs. 选择性连接）以及传播策略（避灾 vs. 趋利）进行了对比实验。
*   **充分性评价**：实验设计较为充分，通过合成数据验证了泛化性，通过真实数据验证了实用性。对比了从传统规则到最先进 DRL 的多种方法，结果具有客观性。

### 6. 主要结论与发现
*   **性能优越**：所提方法在所有规模的合成数据集上，Makespan 和托盘更换次数均优于现有 DRL 和启发式方法。
*   **扩展性强**：随着问题规模增大，该方法相对于基准方法的领先优势更加明显。
*   **真实场景表现**：在真实生产线 C 上，该方法在极短时间内达到了优于 OR-Tools（1800s 搜索）的完工时间。
*   **权衡关系**：实验发现“避灾策略”优于“趋利策略”，因为前者能更好地从全局角度缓解瓶颈，而非仅仅追求局部最优。

### 7. 优点
*   **实际约束建模**：首次在 DRL 框架下系统性地解决了带缓冲区和齐套约束的 FJSP 问题。
*   **异构图创新**：通过选择性连接和成本敏感的加权机制，巧妙地将复杂的全局约束分解为图上的局部可学习信号。
*   **效率与质量平衡**：在保持秒级决策速度的同时，提供了接近甚至超越精确求解器的方案质量。

### 8. 不足与局限
*   **理想化假设**：实验未考虑机器故障（Breakdowns）或动态订单插入等随机干扰，与完全真实的动态生产环境仍有差距。
*   **多目标冲突**：在某些真实数据集上，为了优化 Makespan，托盘更换次数并未达到最低，说明在极端拥堵环境下，多目标平衡仍有优化空间。
*   **超参数敏感性**：奖励函数中的 $\lambda$ 和权重缩放因子 $\alpha$ 可能需要针对不同工业场景进行精细调优。

（完）
