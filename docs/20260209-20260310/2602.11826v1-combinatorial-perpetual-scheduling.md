---
title: Combinatorial Perpetual Scheduling
title_zh: 组合永续调度
authors: "Mirabel Mendoza-Cadena, Arturo Merino, Mads Anker Nielsen, Kevin Schewior"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.11826v1"
tags: ["query:drl-jsp"]
score: 6.0
evidence: 永续调度问题的组合框架
tldr: "本文研究了组合永续调度问题，提出了一种基于集合系统 $(E, \\mathcal{I})$ 的通用框架。研究重点是“组合竹园修剪”问题，即在每个时间步选择一个独立集，使元素高度增长率与调度频率平衡，目标是最小化元素的最大高度。研究证明了在拟阵约束下，最大高度可控制在2以内（最优），并为特定拟阵提供了高效算法；而对于一般集合系统，最优高度为 $\\Theta(\\log |E|)$。"
selection_source: fresh_fetch
motivation: 旨在解决在复杂组合约束下，如何通过周期性调度来平衡各元素的出现频率并最小化累积负荷。
method: 利用拟阵交集多面体的整性证明存在性，并针对不同拟阵类别设计了高效的调度算法。
result: 证明了拟阵约束下的最优高度为2，并为图形拟阵和层级拟阵实现了高度为4的算法，同时确定了一般系统的对数级界限。
conclusion: 该框架不仅优化了竹园修剪问题，还为风车调度等问题的密度边界提供了新的理论支撑。
---

## 摘要
本文介绍了一个用于永续调度问题组合变体的框架。给定一个集合系统 $(E,\mathcal{I})$，调度由每个时间步 $t \in \mathbb{N}$ 的一个独立集 $I_t \in \mathcal{I}$ 组成，目标是满足 $E$ 中元素出现的频率要求。我们特别关注组合竹园修剪问题（combinatorial bamboo garden trimming），其中元素 $e \in E$ 以增长率 $g(e)$ 累积高度（$g(e)$ 给定为 $\mathcal{I}$ 中独立集关联向量的凸组合），并在被调度时重置为零，目标是最小化任何元素达到的最大高度。利用拟阵交多面体（matroid-intersection polytope）的整数性，我们证明了当 $(E,\mathcal{I})$ 是一个拟阵时，可以保证最大高度至多为 2，这是最优的。我们通过针对特定拟阵类的高效算法补充了这一存在性结果，对于均匀拟阵（uniform matroids）和划分拟阵（partition matroids）实现了 2 的最大高度，对于图形拟阵（graphic matroids）和层级拟阵（laminar matroids）实现了 4 的最大高度。相比之下，我们表明对于一般集合系统，最优保证高度为 $\Theta(\log |E|)$，且可以通过高效算法实现。对于组合风车调度（combinatorial pinwheel scheduling），即每个元素 $e\in E$ 需要在调度中至少每 $a_e \in \mathbb{N}$ 个时间步出现一次，我们的结果给出了足以保证可调度性的密度界限。

## 速览
**TLDR**：本文研究了组合永续调度问题，提出了一种基于集合系统 $(E, \mathcal{I})$ 的通用框架。研究重点是“组合竹园修剪”问题，即在每个时间步选择一个独立集，使元素高度增长率与调度频率平衡，目标是最小化元素的最大高度。研究证明了在拟阵约束下，最大高度可控制在2以内（最优），并为特定拟阵提供了高效算法；而对于一般集合系统，最优高度为 $\Theta(\log |E|)$。 \
**Motivation**：旨在解决在复杂组合约束下，如何通过周期性调度来平衡各元素的出现频率并最小化其累积延迟或高度。 \
**Method**：利用拟阵交集多面体的整性证明存在性，并针对不同拟阵类别及一般集合系统设计了高效的调度算法。 \
**Result**：证明了拟阵约束下的最优高度为2，并为图形和层级拟阵提供了高度为4的算法，同时确定了一般集合系统的对数级高度界限。 \
**Conclusion**：该研究为组合约束下的永续调度提供了理论最优界限，并为风车调度等实际问题提供了密度保证。

---

## Abstract
This paper introduces a framework for combinatorial variants of perpetual-scheduling problems. Given a set system $(E,\mathcal{I})$, a schedule consists of an independent set $I_t \in \mathcal{I}$ for every time step $t \in \mathbb{N}$, with the objective of fulfilling frequency requirements on the occurrence of elements in $E$. We focus specifically on combinatorial bamboo garden trimming, where elements accumulate height at growth rates $g(e)$ for $e \in E$ given as a convex combination of incidence vectors of $\mathcal{I}$ and are reset to zero when scheduled, with the goal of minimizing the maximum height attained by any element.   Using the integrality of the matroid-intersection polytope, we prove that, when $(E,\mathcal{I})$ is a matroid, it is possible to guarantee a maximum height of at most 2, which is optimal. We complement this existential result with efficient algorithms for specific matroid classes, achieving a maximum height of 2 for uniform and partition matroids, and 4 for graphic and laminar matroids. In contrast, we show that for general set systems, the optimal guaranteed height is $Θ(\log |E|)$ and can be achieved by an efficient algorithm. For combinatorial pinwheel scheduling, where each element $e\in E$ needs to occur in the schedule at least every $a_e \in \mathbb{N}$ time steps, our results imply bounds on the density sufficient for schedulability.