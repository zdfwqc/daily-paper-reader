---
title: "RESCHED: Rethinking Flexible Job Shop Scheduling from a Transformer-based Architecture with Simplified States"
title_zh: RESCHED：基于简化状态的 Transformer 架构对柔性作业车间调度问题的重新思考
authors: "Xiangjie Xiao, Cong Zhang, Wen Song, Zhiguang Cao"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07020v1"
tags: ["query:drl-jsp"]
score: 10.0
evidence: 用于柔性作业车间调度的极简深度强化学习框架
tldr: 本研究针对柔性作业车间调度问题（FJSP），提出了一种名为ReSched的极简深度强化学习框架。针对现有方法依赖复杂人工特征和图神经网络架构的问题，ReSched通过重新审视马尔可夫决策过程，将状态空间简化为仅4个核心特征，并采用改进的Transformer架构。实验证明，该方法在FJSP上优于现有SOTA方法，且在JSSP和FFSP等变体任务中展现出极强的泛化能力，显著降低了建模复杂度。
selection_source: fresh_fetch
motivation: 现有FJSP调度方法过度依赖复杂的人工特征工程和特定的图神经网络架构，导致建模复杂度高且泛化性受限。
method: 重新定义MDP模型将状态简化为4个核心特征，并结合针对调度任务优化的Transformer架构进行决策。
result: 在FJSP基准测试中超越了传统规则和最先进的强化学习方法，并在JSSP和FFSP任务中表现出优异的跨任务泛化性。
conclusion: ReSched证明了通过简化状态表示和采用通用的Transformer架构，可以实现更高效、更具泛化性的工业调度方案。
---

## 摘要
近年来，针对柔性作业车间调度问题（FJSP）的神经方法，特别是基于深度强化学习（DRL）的方法，受到了越来越多的关注。然而，现有方法依赖于复杂的特征工程状态表示（即通常需要 20 多个手工设计的特征）和偏向图结构的神经架构。为了降低建模复杂度并推进更具泛化性的 FJSP 框架，我们提出了 ReSched，这是一个极简的 DRL 框架，它重新思考了调度公式和模型设计。首先，通过重新审视 FJSP 的马尔可夫决策过程（MDP）公式，我们将状态空间压缩为仅四个核心特征，并通过基于子问题的视角消除了历史依赖。其次，我们采用了带有点积注意力的 Transformer 块，并辅以针对调度任务定制的三个轻量级且有效的架构改进。大量实验表明，ReSched 在 FJSP 上优于经典的调度规则和最先进的 DRL 方法。此外，ReSched 还能很好地泛化到作业车间调度问题（JSSP）和柔性流水车间调度问题（FFSP），在与专门为这些变体设计的神经基准模型的对比中取得了具有竞争力的性能。

## 速览
**TLDR**：针对柔性作业车间调度问题（FJSP），现有深度强化学习方法通常依赖复杂的手工特征和图神经网络。本文提出ReSched框架，通过重新审视MDP模型将状态空间简化为仅4个核心特征，并采用改进的Transformer架构。实验证明ReSched在FJSP上超越了现有SOTA方法，且在JSSP和FFSP等调度变体中也展现出极强的泛化能力和竞争优势。 \
**Motivation**：解决现有FJSP神经调度方法中状态表示过于复杂（依赖20多个手工特征）以及模型架构过于依赖图神经网络的问题。 \
**Method**：通过子问题视角将状态空间压缩至4个核心特征，并利用带有轻量级调度优化设计的Transformer架构进行建模。 \
**Result**：ReSched在FJSP任务上优于传统启发式规则和最先进的DRL方法，并能有效泛化至JSSP和FFSP等其他调度场景。 \
**Conclusion**：研究表明，极简的状态表示结合通用的Transformer架构能够显著提升柔性调度问题的建模效率和跨任务泛化能力。

---

## Abstract
Neural approaches to the Flexible Job Shop Scheduling Problem (FJSP), particularly those based on deep reinforcement learning (DRL), have gained growing attention in recent years. However, existing methods rely on complex feature-engineered state representations (i.e., often requiring more than 20 handcrafted features) and graph-biased neural architectures. To reduce modeling complexity and advance a more generalizable framework for FJSP, we introduce \textsc{ReSched}, a minimalist DRL framework that rethinks both the scheduling formulation and model design. First, by revisiting the Markov Decision Process (MDP) formulation of FJSP, we condense the state space to just four essential features, eliminating historical dependencies through a subproblem-based perspective. Second, we employ Transformer blocks with dot-product attention, augmented by three lightweight but effective architectural modifications tailored to scheduling tasks. Extensive experiments show that \textsc{ReSched} outperforms classical dispatching rules and state-of-the-art DRL methods on FJSP. Moreover, \textsc{ReSched} also generalizes well to the Job Shop Scheduling Problem (JSSP) and the Flexible Flow Shop Scheduling Problem (FFSP), achieving competitive performance against neural baselines specifically designed for these variants.

---

## 论文详细总结（自动生成）

这篇论文由新加坡管理大学、南洋理工大学和山东大学的研究团队合作完成，发表于 ICLR 2026。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：针对柔性作业车间调度问题（FJSP），现有的深度强化学习（DRL）方法普遍存在**状态表示过于复杂**（依赖 20 多个手工特征）和**神经架构归纳偏置过强**（过度依赖图神经网络 GNN）的问题。
*   **研究动机**：复杂的特征工程增加了建模难度且存在冗余，而 GNN 在捕捉长程依赖和处理复杂的非局部交互时存在局限。作者旨在探索：能否通过**极简的马尔可夫状态**和**通用的 Transformer 架构**，构建一个性能更强且能跨任务泛化的调度框架？

### 2. 方法论
*   **核心思想**：将调度过程建模为基于子问题的 MDP，通过简化状态特征并利用 Transformer 的表达能力来替代复杂的图结构偏置。
*   **关键技术细节**：
    *   **极简状态表示**：仅保留 4 个核心特征：1) 操作可用时间；2) 机器可用时间；3) 加工耗时（Duration）；4) 最小加工耗时。通过“相对时间”消除绝对数值影响，并利用“后向边”图结构消除历史依赖。
    *   **双分支 Transformer 架构**：
        *   **操作分支（Operation Branch）**：引入 **RoPE（旋转位置编码）** 来显式建模作业内部的操作顺序，无需额外参数即可捕捉工序约束。
        *   **机器分支（Machine Branch）**：采用 **Edge-in Attention**，将边特征（加工时间）直接嵌入注意力机制；并设计 **Self-based Cross-attention**，让机器节点在聚合操作信息时关注自身，缓解操作与机器数量不平衡导致的表示稀释。
    *   **算法流程**：采用构造式调度，每步选择一个“操作-机器”对。使用 **REINFORCE** 算法进行训练（附录中也提供了 PPO 版本），奖励函数基于估计的完工时间（Makespan）下界变化。

### 3. 实验设计
*   **数据集/场景**：
    *   **FJSP**：合成数据集 SD1、SD2；标准基准集 Brandimarte、Hurink。
    *   **JSSP（作业车间调度）**：Taillard、DMU 基准集。
    *   **FFSP（柔性流水车间调度）**：MatNet 论文中的合成数据集。
*   **对比方法（Benchmarks）**：
    *   **传统规则**：FIFO, SPT, MWKR 等。
    *   **SOTA DRL 方法**：HGNN, DANIEL, DOAGNN (FJSP); L2D, RL-GNN (JSSP); MatNet (FFSP)。
    *   **精确求解器/启发式**：OR-Tools (CP-SAT), 2SGA。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX A40 GPU** 进行训练和评估。
*   **训练时长**：在 SD1 (10x5) 设置下，REINFORCE 版本约需 30 分钟，PPO 版本约需 20 分钟。
*   **超参数**：隐藏层维度 128，Transformer 块 2 层，8 头注意力，训练 2000 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 10x5 到 100x20 等多种规模的测试，总计评估了数百个实例。
*   **充分性**：
    *   **跨任务验证**：不仅在 FJSP 上测试，还直接在 JSSP 和 FFSP 上验证了泛化性。
    *   **消融实验**：针对 RoPE、Edge-in Attention、相对时间等 6 个关键组件进行了详尽的消融分析。
    *   **鲁棒性**：进行了 4 个不同随机种子的重复实验，结果波动小于 1%，证明了结论的客观性。
    *   **公平性**：在相同训练预算下对比了 DANIEL 等 SOTA 方法，确保了对比的公正。

### 6. 主要结论与发现
*   **性能领先**：ReSched 在 FJSP 的所有基准测试中均达到了 SOTA 水平，尤其在复杂数据集 SD2 上，比之前的最佳方法 DANIEL 减少了约 30% 的差距。
*   **极强的泛化性**：仅在 10x10 规模训练的模型，在 100x20 的 JSSP 任务上依然优于专门为 JSSP 设计的 DRL 模型。
*   **特征冗余性**：实验证明，移除 DANIEL 等方法中一半以上的特征并不会降低性能，验证了“极简状态”的有效性。

### 7. 优点（亮点）
*   **大道至简**：打破了“特征越多越好”的迷思，证明了 4 个核心特征足以描述复杂的调度状态。
*   **架构通用**：通过对 Transformer 的轻量级改造（RoPE, Edge-in），使其能完美适配具有严格工序约束的调度任务。
*   **零样本迁移**：展示了从 FJSP 到 JSSP/FFSP 的无缝迁移能力，体现了框架的通用性。

### 8. 不足与局限
*   **反馈机制缺失**：目前仅建模了“操作到机器”的交互，缺乏“机器到操作”的显式反馈。
*   **推理加速潜力未完全释放**：虽然理论上支持 KV Cache 加速（可提速 10-40 倍），但论文实验部分尚未正式实现该优化。
*   **复杂约束覆盖**：尚未探讨带有机器故障、物料搬运时间等更复杂现实约束的场景。

（完）
