---
title: Next Embedding Prediction Makes World Models Stronger
title_zh: 下个嵌入预测让世界模型更强大
authors: "George Bredis, Nikita Balagansky, Daniil Gavrilov, Ruslan Rakhimov"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02765v1"
tags: ["query:drl"]
score: 8.0
evidence: 最先进的基于模型的强化学习算法
tldr: 本研究针对部分可观测高维环境中的模型强化学习，提出了 NE-Dreamer 框架。该方法摒弃了传统的解码器结构，利用时间 Transformer 直接在表示空间中预测下一时刻的编码器嵌入，从而优化时间预测一致性。NE-Dreamer 无需重构损失或辅助监督即可学习到连贯的预测性状态表示。实验表明，该方法在 DeepMind Control Suite 和 DMLab 任务中表现优异，尤其在处理记忆和空间推理方面具有显著优势。
selection_source: fresh_fetch
motivation: 在部分可观测的高维领域中，捕捉时间依赖性对于基于模型的强化学习至关重要。
method: 提出 NE-Dreamer，一种无解码器的智能体，通过时间 Transformer 预测下一时刻的编码器嵌入以优化表示空间的一致性。
result: 在 DeepMind Control Suite 上性能媲美 DreamerV3，并在涉及记忆和空间推理的 DMLab 任务中取得大幅领先。
conclusion: 基于时间 Transformer 的下一嵌入预测为复杂环境下的模型强化学习提供了一个高效且可扩展的框架。
---

## 摘要
在部分可观测的高维领域中，捕捉时间依赖性对于基于模型的强化学习（MBRL）至关重要。我们提出了 NE-Dreamer，这是一种无解码器（decoder-free）的 MBRL 智能体，它利用时间 Transformer 从潜状态序列中预测下一步的编码器嵌入，从而直接优化表示空间中的时间预测对齐。这种方法使 NE-Dreamer 能够在无需重构损失或辅助监督的情况下，学习到连贯且具有预测性的状态表示。在 DeepMind Control Suite 上，NE-Dreamer 的性能达到或超过了 DreamerV3 以及领先的无解码器智能体。在涉及记忆和空间推理的 DMLab 任务挑战性子集中，NE-Dreamer 取得了显著的提升。这些结果表明，结合时间 Transformer 的下个嵌入预测是复杂、部分可观测环境下 MBRL 的一种有效且可扩展的框架。

## Abstract
Capturing temporal dependencies is critical for model-based reinforcement learning (MBRL) in partially observable, high-dimensional domains. We introduce NE-Dreamer, a decoder-free MBRL agent that leverages a temporal transformer to predict next-step encoder embeddings from latent state sequences, directly optimizing temporal predictive alignment in representation space. This approach enables NE-Dreamer to learn coherent, predictive state representations without reconstruction losses or auxiliary supervision. On the DeepMind Control Suite, NE-Dreamer matches or exceeds the performance of DreamerV3 and leading decoder-free agents. On a challenging subset of DMLab tasks involving memory and spatial reasoning, NE-Dreamer achieves substantial gains. These results establish next-embedding prediction with temporal transformers as an effective, scalable framework for MBRL in complex, partially observable environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Next Embedding Prediction Makes World Models Stronger》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在基于模型的强化学习（MBRL）中，智能体需要从高维像素观测中学习紧凑的潜状态表示。传统方法（如 Dreamer 系列）依赖**像素解码器**进行重构，但这会带来巨大的计算开销，且模型容易过度关注与任务无关的视觉细节。
*   **核心问题**：现有的“无解码器”（Decoder-free）方法虽然提高了效率，但大多只关注同一时刻的表示对齐，在处理**部分可观测性（Partial Observability）**、需要长期记忆和空间推理的复杂任务时表现乏力。
*   **整体含义**：论文探讨了如何通过“预测未来”而非“重构当前”来学习更强大的世界模型表示，旨在证明**时间预测能力**是构建强健世界模型的关键。

### 2. 方法论
*   **核心思想**：提出 **NE-Dreamer** 框架，摒弃像素解码器，引入一个轻量级的**因果时间 Transformer**，通过预测“下一时刻的编码器嵌入”来训练世界模型。
*   **关键技术细节**：
    *   **RSSM 架构**：保留了 Dreamer 的循环状态空间模型，包含确定性状态 $h_t$ 和随机潜变量 $z_t$。
    *   **下一嵌入预测（NEP）**：使用因果 Transformer $T_\theta$ 接收历史序列信息 $(h_{\le t}, z_{\le t}, a_{\le t})$，输出对下一时刻编码器嵌入的预测值 $\hat{e}_{t+1}$。
    *   **Barlow Twins 对齐**：为了防止表示崩溃（Collapse），使用 Barlow Twins 损失函数将预测值 $\hat{e}_{t+1}$ 与真实的下一时刻编码器嵌入 $e^*_{t+1}$（通过停止梯度算子处理）进行对齐。该损失通过互相关矩阵鼓励表示的不变性并减少冗余。
    *   **Actor-Critic 学习**：在学习到的潜空间中进行“想象”训练，通过预测奖励和价值函数来优化策略。

### 3. 实验设计
*   **实验场景**：
    1.  **DeepMind Lab (DMLab) Rooms**：包含四个极具挑战性的任务（Collect, Exploit, Select, Watermaze），侧重考察智能体的长期记忆、导航和空间推理能力。
    2.  **DeepMind Control Suite (DMC)**：包含 20 个标准的连续控制任务，用于验证基础控制性能。
*   **Benchmark 与对比方法**：
    *   **DreamerV3**：经典的基于重构的世界模型。
    *   **R2-Dreamer**：无解码器基准，采用同一时刻的冗余减少对齐。
    *   **DreamerPro**：无解码器基准，依赖强数据增强（随机偏移）。
    *   **DrQv2**：强力的无模型（Model-free）强化学习基准。
    *   **Dreamer (无重构版)**：仅靠奖励和 KL 散度约束的消融基准。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **实验配置**：提到所有方法都在统一的 PyTorch 代码库下运行，模型参数量统一控制在 **12M**（对应 Dreamer-S 架构），以确保对比的公平性。DMLab 任务训练步数为 50M steps，DMC 任务为 1M steps。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 24 个不同的环境任务（4 DMLab + 20 DMC）。
*   **充分性与客观性**：
    *   每个实验均使用 **5 个随机种子**并报告均值与标准差，具有统计学意义。
    *   **消融实验**非常深入：分别验证了“Transformer 结构”、“下一时刻偏移（Next-step shift）”以及“投影头（Projector）”的作用，明确了性能提升源于时间预测机制而非架构堆叠。
    *   **表示诊断**：通过训练后验解码器可视化发现，NE-Dreamer 学习到的表示在时间连贯性上显著优于基准方法。

### 6. 主要结论与发现
*   **性能飞跃**：在 DMLab Rooms 任务中，NE-Dreamer 显著超过了 DreamerV3 和所有无解码器基准，证明了时间预测对处理部分可观测环境的优越性。
*   **无损替代**：在 DMC 连续控制任务中，NE-Dreamer 匹配或略微超过了 SOTA 水平，说明移除解码器并不会损害基础控制能力。
*   **预测的重要性**：消融实验显示，如果将“预测下一时刻”改为“对齐当前时刻”，性能会大幅下降，证明了**前向预测**是学习高质量表示的核心。

### 7. 优点
*   **架构简洁高效**：去除了复杂的像素解码器，降低了计算负担，同时避免了模型浪费容量去建模无关的视觉细节。
*   **时间一致性强**：通过 Transformer 捕捉长程依赖，使潜状态在长时间序列中保持稳定，非常适合需要记忆的任务。
*   **无需重度增强**：相比 DreamerPro 等方法，NE-Dreamer 不依赖复杂的数据增强技术即可实现稳定训练。

### 8. 不足与局限
*   **视觉精细度风险**：在某些极度依赖微小视觉变化（如纹理细节）而非宏观结构的任务中，无解码器方法可能存在信息丢失的风险。
*   **超参数敏感性**：虽然论文使用了标准参数，但 Transformer 的引入增加了超参数空间（如层数、头数），在不同领域的泛化性仍需更多验证。
*   **算力开销细节**：虽然模型参数量一致，但 Transformer 在处理长序列时的推理/训练耗时与传统 CNN 解码器的具体对比数据缺失。

（完）
