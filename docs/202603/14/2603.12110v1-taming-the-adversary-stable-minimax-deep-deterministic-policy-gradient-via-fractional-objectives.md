---
title: "Taming the Adversary: Stable Minimax Deep Deterministic Policy Gradient via Fractional Objectives"
title_zh: 驯服对抗者：通过分式目标函数实现稳定的极大极小深度确定性策略梯度
authors: "Taeho Lee, Donghwan Lee"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12110v1"
tags: ["query:drl"]
score: 9.0
evidence: 通过极大极小优化提高深度强化学习的稳定性
tldr: 针对强化学习在外部扰动和模型不确定性下表现不稳定的问题，本文提出了极小极大深度确定性策略梯度（MMDDPG）框架。该框架通过用户策略与对抗性扰动策略之间的博弈来学习鲁棒策略，并创新性地引入分式目标函数来平衡任务性能与扰动强度，从而稳定训练过程。实验证明，该方法在MuJoCo环境中能有效应对外部力和参数变化，显著提升了系统的鲁棒性。
selection_source: fresh_fetch
motivation: 强化学习智能体在面临外部扰动和模型不确定性时，往往会出现性能不稳定或严重退化的问题。
method: 提出MMDDPG框架，通过极小极大优化博弈并引入分式目标函数来平衡任务表现与扰动强度。
result: 在MuJoCo环境中的实验结果显示，该方法在应对外部力扰动和模型参数变化方面均表现出显著的鲁棒性。
conclusion: 通过分式目标函数稳定对抗训练过程，MMDDPG为实现连续控制任务中的高鲁棒性策略提供了有效途径。
---

## 摘要
强化学习（RL）在广泛的控制和决策任务中取得了显著成功。然而，当部署在存在意外外部扰动和模型不确定性的环境中时，强化学习智能体往往表现出不稳定或性能下降。因此，确保在这些条件下的可靠性能仍然是一个关键挑战。在本文中，我们提出了极大极小深度确定性策略梯度（MMDDPG），这是一个在连续控制任务中学习抗扰动策略的框架。训练过程被表述为用户策略与对抗性扰动策略之间的极大极小优化问题。在该问题中，用户学习一个最小化目标函数的鲁棒策略，而对抗者则生成最大化该目标函数的扰动。为了稳定这种交互，我们引入了一个平衡任务性能和扰动强度的分式目标。该目标防止了过度激进的扰动，并促进了鲁棒学习。在 MuJoCo 环境中的实验评估表明，所提出的 MMDDPG 在应对外部力扰动和模型参数变化方面均实现了显著提高的鲁棒性。

## Abstract
Reinforcement learning (RL) has achieved remarkable success in a wide range of control and decision-making tasks. However, RL agents often exhibit unstable or degraded performance when deployed in environments subject to unexpected external disturbances and model uncertainties. Consequently, ensuring reliable performance under such conditions remains a critical challenge. In this paper, we propose minimax deep deterministic policy gradient (MMDDPG), a framework for learning disturbance-resilient policies in continuous control tasks. The training process is formulated as a minimax optimization problem between a user policy and an adversarial disturbance policy. In this problem, the user learns a robust policy that minimizes the objective function, while the adversary generates disturbances that maximize it. To stabilize this interaction, we introduce a fractional objective that balances task performance and disturbance magnitude. This objective prevents excessively aggressive disturbances and promotes robust learning. Experimental evaluations in MuJoCo environments demonstrate that the proposed MMDDPG achieves significantly improved robustness against both external force perturbations and model parameter variations.

---

## 论文详细总结（自动生成）

以下是对论文《Taming the Adversary: Stable Minimax Deep Deterministic Policy Gradient via Fractional Objectives》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：深度强化学习（RL）在复杂任务中表现优异，但在面对外部扰动、传感器噪声或模型参数变化（如摩擦力、电机常数变化）时，策略往往表现出极大的脆弱性。
*   **核心问题**：现有的鲁棒强化学习（RRL）方法，特别是对抗性强化学习（如 RARL），通常将问题建模为零和博弈。然而，直接进行极大极小（Minimax）优化往往极不稳定：对抗者（Adversary）可能会生成过度激进、不切实际的扰动，导致训练崩溃或无法收敛到有意义的解。
*   **研究目标**：开发一种稳定的对抗性训练框架，在保证策略鲁棒性的同时，防止对抗者过度主导优化过程。

### 2. 论文提出的方法论
*   **核心思想**：引入一种**分式目标函数（Fractional Objective）**。该函数将“任务性能（累积代价）”作为分子，将“扰动强度（累积扰动范数）”作为分母。
*   **关键技术细节**：
    *   **分式建模**：目标函数定义为 $J = J_{performance} / J_{disturbance}$。这种结构模仿了控制理论中的 $H_\infty$ 范数，即衡量扰动到输出的最大增益。
    *   **对数转换**：为了简化梯度计算，对目标函数取对数，将其转化为差值形式：$\ln(J) = \ln(J_{performance}) - \ln(J_{disturbance})$。这使得优化过程在数学上更易处理且更稳定。
    *   **双评论家-双演员架构**：
        *   **两个 Critic**：$Q_{\psi1}$ 估计累积任务代价，$Q_{\psi2}$ 估计累积扰动范数。
        *   **两个 Actor**：用户策略 $\pi_\theta$ 试图最小化对数目标，对抗策略 $\mu_\phi$ 试图最大化它。
    *   **梯度归一化**：在更新 Actor 时，使用 Batch Mean 对 Q 值进行归一化，防止梯度消失或爆炸，并维持用户与对抗者之间的平衡竞争。
*   **算法流程**：基于 DDPG 框架，通过从经验回放池采样，同时更新两个 Critic 网络和两个 Actor 网络，并使用软更新（Soft Update）维持目标网络的稳定性。

### 3. 实验设计
*   **实验场景**：使用了 MuJoCo 物理引擎中的两个连续控制基准任务：
    1.  **Reacher（到达器）**：2 自由度机械臂，目标是触碰随机位置。
    2.  **Pusher（推物器）**：更复杂的 7 自由度机械臂，目标是将物体推向目标点。
*   **对比方法（Benchmarks）**：
    *   **DDPG**：标准的非鲁棒基准。
    *   **RARL**：经典的对抗鲁棒强化学习。
    *   **PR-DDPG & NR-DDPG**：动作鲁棒（Action-Robust）变体，通过在动作空间注入噪声来提高鲁棒性。
*   **测试维度**：
    *   **外部扰动**：在机械臂末端施加不同强度的随机高斯力。
    *   **模型不确定性**：改变关节阻尼（Damping）和齿轮比（Gear Scale）参数，模拟模型失配。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **训练规模**：每个实验运行了 100,000 个步长（100k steps），并使用了 10 个独立的随机种子以确保统计显著性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对外部扰动，测试了多种均值和标准差组合的扰动分布。
    *   针对模型参数，构建了 5x5 的参数网格（共 25 种配置）生成性能热力图。
*   **充分性评价**：实验设计较为充分。通过 10 个随机种子的均值和方差分析，证明了结果的可靠性。对比了从标准 RL 到对抗 RL 再到动作鲁棒 RL 的多种主流方法，覆盖了外部环境变化和内部参数漂移两种关键鲁棒性维度。

### 6. 论文的主要结论与发现
*   **稳定性提升**：相比于 RARL，MMDDPG 的分式目标函数能有效“驯服”对抗者，防止其产生破坏性的极端扰动，从而使训练过程更加平滑。
*   **鲁棒性增强**：在 Pusher 等复杂任务中，MMDDPG 的表现显著优于所有基准方法，不仅平均代价最低，且性能波动（方差）最小。
*   **泛化能力**：MMDDPG 学习到的策略在未见过的参数配置（如过阻尼或欠阻尼环境）下表现出极强的泛化能力，而其他方法在参数偏离名义值时性能大幅下降。

### 7. 优点
*   **理论联系紧密**：将控制理论中的 $H_\infty$ 思想巧妙地引入到深度强化学习的目标函数设计中。
*   **无需精细调参**：相比于需要手动设置扰动约束或噪声水平的方法，MMDDPG 通过分式结构自发地平衡了性能与扰动，减少了超参数敏感性。
*   **离线策略效率**：基于 DDPG（Off-policy），比许多基于 On-policy 的对抗算法具有更高的样本效率。

### 8. 不足与局限
*   **正值假设**：对数转换要求代价函数 $J_1$ 必须始终为正。在某些奖励函数（Reward）为负或包含正负交替的任务中，需要手动添加偏移量（Offset），这可能影响收敛特性。
*   **计算开销**：由于需要维护两套 Critic 和两套 Actor 网络，其内存占用和单步计算耗时比标准 DDPG 高出一倍左右。
*   **环境覆盖面**：实验仅限于 MuJoCo 的两个任务，尚未在更复杂的视觉输入任务或真实机器人硬件上进行验证。

（完）
