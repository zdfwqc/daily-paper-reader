---
title: Actor-Accelerated Policy Dual Averaging for Reinforcement Learning in Continuous Action Spaces
title_zh: 连续动作空间强化学习中的 Actor 加速策略对偶平均
authors: "Ji Gao, Caleb Ju, Guanghui Lan, Zhaohui Tong"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10199v1"
tags: ["query:drl"]
score: 9.0
evidence: 强化学习中策略对偶平均的数学基础
tldr: 本研究针对策略对偶平均（PDA）在连续动作空间中计算开销大的问题，提出了“演员加速PDA”算法。PDA虽能有效结合价值函数逼近并保证收敛，但在连续空间中每步需解优化子问题。该方法通过引入策略网络来近似求解这些子问题，在保持收敛保证的同时显著提升了运行效率。理论分析量化了近似误差的影响，实验证明其在机器人和控制任务中优于PPO等基准算法，成功将PDA推向实际应用。
selection_source: fresh_fetch
motivation: 解决策略对偶平均（PDA）在连续动作空间中因每步需求解复杂的优化子问题而导致的计算效率低下和难以实际部署的问题。
method: 提出演员加速的PDA框架，利用学习到的策略网络来近似替代每步的优化子问题求解过程，并提供收敛性理论分析。
result: 在机器人、控制和运筹学等多个基准测试中，该方法在性能和运行速度上均优于PPO等流行的在线策略强化学习算法。
conclusion: 该研究通过引入策略网络加速，成功弥合了PDA算法的理论优势与其在连续动作空间函数逼近任务中实际应用之间的鸿沟。
---

## 摘要
策略对偶平均 (PDA) 提供了一个有原则的策略镜像下降 (PMD) 框架，相比标准的 PMD，它能更自然地容纳价值函数近似，从而在保留强收敛保证的同时，允许使用近似优势（或 Q-）函数。然而，在连续状态和动作空间中应用 PDA 仍然具有计算挑战性，因为动作选择涉及在每个决策步骤求解一个优化子问题。在本文中，我们提出了 Actor 加速的 PDA，它使用学习到的策略网络来近似优化子问题的解，在保持收敛保证的同时实现了更快的运行速度。我们提供了理论分析，量化了在适当假设下 Actor 近似误差如何影响 PDA 的收敛性。随后，我们在机器人、控制和运筹学问题的多个基准测试中评估了其性能。与近端策略优化 (PPO) 等流行的同策略基准算法相比，Actor 加速的 PDA 取得了更优越的性能。总的来说，我们的研究结果弥合了 PDA 的理论优势与其在带有函数近似的连续动作问题中的实际部署之间的差距。

## Abstract
Policy Dual Averaging (PDA) offers a principled Policy Mirror Descent (PMD) framework that more naturally admits value function approximation than standard PMD, enabling the use of approximate advantage (or Q-) functions while retaining strong convergence guarantees. However, applying PDA in continuous state and action spaces remains computationally challenging, since action selection involves solving an optimization sub-problem at each decision step. In this paper, we propose \textit{actor-accelerated PDA}, which uses a learned policy network to approximate the solution of the optimization sub-problems, yielding faster runtimes while maintaining convergence guarantees. We provide a theoretical analysis that quantifies how actor approximation error impacts the convergence of PDA under suitable assumptions. We then evaluate its performance on several benchmarks in robotics, control, and operations research problems. Actor-accelerated PDA achieves superior performance compared to popular on-policy baselines such as Proximal Policy Optimization (PPO). Overall, our results bridge the gap between the theoretical advantages of PDA and its practical deployment in continuous-action problems with function approximation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Actor-Accelerated Policy Dual Averaging for Reinforcement Learning in Continuous Action Spaces》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在强化学习（RL）中，策略镜像下降（PMD）是许多算法的理论基石。策略对偶平均（PDA）作为 PMD 的一种变体，在处理价值函数近似时比标准 PMD 更具理论优势。
*   **核心痛点**：PDA 在连续动作空间中面临巨大的计算挑战。它要求在每一个决策步都求解一个复杂的优化子问题（寻找使累积优势函数最小的动作），这在实际应用中（尤其是深度 RL）速度极慢，难以部署。
*   **整体含义**：本文旨在通过引入一个“Actor（演员）”网络来学习并近似求解这些优化子问题，从而在保留 PDA 理论收敛保证的同时，大幅提升其在连续动作空间中的运行效率和实用性。

### 2. 方法论
*   **核心思想**：将原本需要通过数值优化求解的子问题，转化为由策略网络（Actor）直接预测最优动作的过程。
*   **关键技术细节**：
    *   **累积优势网络**：算法不直接存储历史梯度，而是维护一个参数化网络来近似“加权累积优势函数”。
    *   **Actor 加速更新**：Actor 网络的目标是最小化 PDA 的正则化目标函数，即：$\min_{a} [ \text{累积优势} + \text{正则项} ]$。其中正则项采用 Bregman 散度（通常为欧几里得距离平方），约束策略不要偏离初始策略 $\pi_0$ 太远。
    *   **递归缩放方案**：为了数值稳定性，实现中采用了缩放后的累积优势更新公式，避免了数值爆炸。
    *   **收敛性分析**：论文在理论上量化了 Actor 的近似误差（Optimality Gap）如何影响最终的收敛速度，证明了在存在函数近似误差的情况下，算法仍能收敛到全局最优解的邻域。

### 3. 实验设计
*   **实验场景**：
    *   **连续控制基准**：MuJoCo（如 Humanoid, Ant, Hopper 等高维任务）和 Box2D（LunarLander, BipedalWalker）。
    *   **运筹学（OR）基准**：OR-Gym 套件，包括报童问题（Newsvendor）、投资组合优化（PortfolioOpt）和供应链库存管理（InvManagement）。
*   **Benchmark 与对比方法**：
    *   主要对比了流行的同策略（On-policy）算法：**PPO**（近端策略优化）、**TRPO**（信任区域策略优化）和 **NPG**（自然策略梯度）。
    *   在运筹学任务中，还对比了传统的数学规划方法（如 SHLP, MIP）和 Oracle（先知）模型。

### 4. 资源与算力
*   **硬件环境**：文中提到在 Intel i7-14700K CPU 上进行了运行时间测试。
*   **算力细节**：
    *   使用了 **SOAP 优化器**，其训练墙钟速度（Wall-clock speed）大约是 Adam 优化器的两倍。
    *   **运行时间**：在 MuJoCo 环境中，100 万步训练耗时约 400 到 1500 秒不等（取决于任务复杂度）。
    *   **GPU 使用**：虽然未明确标注 GPU 型号，但基于 Tianshou 框架和深度神经网络的使用，推测使用了主流单卡 GPU（如 RTX 3090/4090 级别）进行加速。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了超过 15 个不同的实验环境，每个环境运行了 10 个随机种子，每个种子包含多次测试，数据量充足。
*   **消融与敏感性分析**：
    *   对探索噪声 $\sigma_0$ 和步长 $\lambda$ 进行了详细的网格搜索和敏感性研究。
    *   对比了不同优化器（SOAP vs Adam）对性能的影响。
    *   探讨了累积优势更新中“理论加权”与“指数平滑”的效果差异。
*   **客观性**：使用了 Tianshou 库中调优过的 PPO 作为基准，而非原始论文版本，这使得对比更加严谨和公平。

### 6. 主要结论与发现
*   **性能卓越**：Actor-accelerated PDA 在大多数 MuJoCo 任务中优于 PPO，尤其是在复杂的 Humanoid（人形机器人）任务中表现突出。
*   **运筹学潜力**：在供应链管理等任务中，PDA 达到了与传统数学规划方法相当的性能，且标准差更小，鲁棒性更强。
*   **效率提升**：通过 Actor 加速，PDA 的运行速度达到了与 PPO 相当的水平，克服了原生 PDA 无法处理连续空间的缺陷。
*   **理论验证**：实验观察到的 Actor 误差保持稳定，验证了论文中关于“近似误差界限”的理论假设。

### 7. 优点
*   **理论完备性**：不仅提出了算法，还提供了在函数近似和 Actor 误差存在下的收敛性证明，弥补了经验算法缺乏理论支撑的不足。
*   **超参直观**：相比 PPO 复杂的剪切机制，PDA 的超参数（正则化强度 $\lambda$ 和探索噪声 $\sigma_0$）具有更明确的物理和数学含义。
*   **跨领域适用**：在机器人控制和运筹学决策两个截然不同的领域均表现出强竞争力。

### 8. 不足与局限
*   **非凸性风险**：虽然理论上讨论了弱凸情况，但在极度复杂的非凸动作空间中，Actor 网络可能陷入局部最优，导致无法准确近似 PDA 的理论更新。
*   **超参敏感度**：实验显示，稳定型任务（如 Ant）和平衡型任务（如 Hopper）对探索噪声的需求截然不同，仍需针对任务类型进行一定的调优。
*   **收敛结果的解释**：对于非凸情况下的收敛结论（Theorem 3.5），其在实际物理意义上的指导作用仍有待进一步深入挖掘。

（完）
