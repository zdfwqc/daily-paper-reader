---
title: Wasserstein Proximal Policy Gradient
title_zh: Wasserstein 近端策略梯度
authors: "Zhaoyu Zhu, Shuhan Zhang, Rui Gao, Shuang Li"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02576v1"
tags: ["query:drl"]
score: 9.0
evidence: 基于 Wasserstein 几何的策略梯度数学基础
tldr: 本研究针对连续动作熵正则化强化学习，提出了一种基于 Wasserstein 几何的近端策略梯度方法（WPPG）。该方法通过算子分裂方案，交替进行最优传输更新与高斯卷积热步，有效避免了对策略对数密度及其梯度的直接计算。这使得 WPPG 能够直接应用于由前推映射定义的隐式随机策略。研究不仅证明了 WPPG 在精确评估和 Actor-Critic 架构下的全局线性收敛性，还通过实验验证了其在标准连续控制任务中的优异性能。
selection_source: fresh_fetch
motivation: 旨在克服传统策略梯度方法在处理复杂隐式策略时需计算对数密度梯度的局限性。
method: 提出 WPPG 算法，利用 Wasserstein 近端算子分裂技术，将策略更新分解为最优传输和高斯卷积步骤。
result: 建立了算法的全局线性收敛理论，并在多个连续控制基准任务上取得了领先的实验结果。
conclusion: WPPG 为基于 Wasserstein 几何的策略优化提供了新视角，是处理高维连续动作空间隐式策略的有效工具。
---

## 摘要
我们从 Wasserstein 几何的角度研究了针对连续动作、熵正则化强化学习的策略梯度方法。从 Wasserstein 近端更新出发，我们通过一种算子分裂方案推导出了 Wasserstein 近端策略梯度 (WPPG)，该方案交替进行最优传输更新和由高斯卷积实现的热步（heat step）。这种表述避免了评估策略的对数密度及其梯度，使得该方法可以直接应用于以推前映射（pushforward maps）形式指定的具有强表达能力的隐式随机策略。我们为 WPPG 建立了全局线性收敛率，涵盖了精确策略评估以及具有受控近似误差的 Actor-Critic 实现。实验表明，WPPG 易于实现，并在标准连续控制基准测试中取得了具有竞争力的性能。

## Abstract
We study policy gradient methods for continuous-action, entropy-regularized reinforcement learning through the lens of Wasserstein geometry. Starting from a Wasserstein proximal update, we derive Wasserstein Proximal Policy Gradient (WPPG) via an operator-splitting scheme that alternates an optimal transport update with a heat step implemented by Gaussian convolution. This formulation avoids evaluating the policy's log density or its gradient, making the method directly applicable to expressive implicit stochastic policies specified as pushforward maps. We establish a global linear convergence rate for WPPG, covering both exact policy evaluation and actor-critic implementations with controlled approximation error. Empirically, WPPG is simple to implement and attains competitive performance on standard continuous-control benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Wasserstein Proximal Policy Gradient》（WPPG）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的策略梯度（PG）方法（如 PPO、TRPO）主要基于欧几里得几何或信息几何（KL 散度）。这些方法通常要求策略具有显式的概率密度函数（log-density），这限制了**隐式策略（Implicit Policies）**的使用。隐式策略通过复杂的推前映射生成动作，表达能力强但密度函数难以计算。
*   **研究动机**：Wasserstein 距离能够捕捉动作空间本身的几何结构（即动作之间的物理距离），在处理连续动作空间时比 KL 散度更具优势。然而，现有的基于 Wasserstein 的方法缺乏在连续动作空间下的全局收敛性理论保证，且往往仍需依赖策略密度。
*   **整体含义**：本文旨在提出一种既能利用 Wasserstein 几何优势、又不需要策略密度信息的算法，并为其提供坚实的数学收敛证明。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将策略更新建模为 Wasserstein 空间中的近端优化问题（Proximal Update），并引入熵正则化以促进探索。
*   **关键技术细节**：
    *   **算子分裂方案（Operator-Splitting）**：将复杂的 Wasserstein 近端更新分解为两个交替步骤：
        1.  **Wasserstein 传输步**：根据动作价值函数（Q 函数）的梯度移动动作样本，使策略向高奖励区域“漂移”。
        2.  **热流步（Heat Step）**：通过注入高斯噪声来实现熵正则化。这在数学上等价于在概率空间中进行热方程演化。
    *   **隐式策略支持**：由于更新过程仅依赖于 $Q$ 函数对动作的梯度（$\nabla_a Q$）和噪声注入，算法完全避开了对策略对数密度（$\log \pi$）及其梯度的计算，因此可以直接训练由神经网络定义的隐式生成模型。
    *   **参数化实现**：在实际应用中，通过最小化动作样本位移与目标方向（由 $Q$ 梯度和噪声组成）之间的差异来更新策略网络参数。

### 3. 实验设计
*   **实验场景**：使用了标准的 MuJoCo 连续控制基准测试集。
*   **具体任务**：包括 Hopper-v5, Walker2d-v5, HalfCheetah-v5, Reacher-v5, Swimmer-v5 和高维的 Humanoid-v5。
*   **对比方法（Benchmarks）**：
    *   **PPO**：基于 KL 散度的近端策略优化。
    *   **SAC**：基于最大熵框架的 Off-policy 算法（使用显式高斯策略）。
    *   **WPO**：现有的基于 Wasserstein 几何的 Actor-Critic 方法（通常需要 KL 投影步）。
*   **变体对比**：对比了使用显式高斯策略的 **WPPG** 和使用隐式策略的 **WPPG-I**。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：所有 Off-policy 模型训练了 $1 \times 10^6$ 个时间步，每 2000 步进行一次评估。使用了 Adam 优化器，学习率为 $3 \times 10^{-4}$，Batch Size 为 256。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同的 MuJoCo 环境上进行了测试，每个环境运行了 10 个独立随机种子以确保结果的统计显著性。
*   **消融实验**：非常充分。作者针对熵系数 $\tau$、隐式策略的隐变量维度、Double-Q 技术的作用、步长 $\eta$ 以及动作采样数量等关键超参数进行了详细的消融研究。
*   **客观性**：基线方法的超参数均取自权威开源库（如 RL Zoo）或原始论文，且在对比时关闭了 SAC 的自动熵调整功能，以确保在固定熵系数下的公平比较。

### 6. 主要结论与发现
*   **性能优异**：WPPG 在所有任务中均表现出与 SAC 相当或更优的性能。特别是 **WPPG-I（隐式策略版）** 在高维任务（如 Humanoid）中显著超过了所有基线方法。
*   **理论突破**：在数学上证明了 WPPG 在熵正则化设置下具有**全局线性收敛率**。即使在 Q 函数存在近似误差的情况下，算法依然能收敛到最优策略的邻域。
*   **隐式策略的潜力**：实验证明，隐式策略能够捕捉比简单高斯分布更复杂的动作分布，从而在复杂任务中获得更高的回报。

### 7. 优点
*   **理论与实践结合**：不仅提出了高性能算法，还填补了 Wasserstein 策略梯度在连续空间收敛性分析上的空白。
*   **架构灵活性**：摆脱了对策略概率密度的依赖，为未来使用更复杂的生成模型（如扩散模型、流匹配模型）作为策略网络铺平了道路。
*   **实现简单**：算子分裂方案将复杂的几何优化简化为“梯度漂移 + 噪声注入”，易于在现有深度强化学习框架中实现。

### 8. 不足与局限
*   **超参数敏感性**：虽然有消融实验，但 Wasserstein 步长 $\eta$ 和噪声尺度 $\tau$ 仍需针对不同任务进行一定程度的调优。
*   **计算开销**：WPPG-I 在更新时通常需要多个动作样本（如 32 个）来估计更新方向，相比单样本方法，单次迭代的计算量略大。
*   **应用范围限制**：目前主要在连续控制任务上验证，尚未在离散动作空间或基于视觉的超高维观测任务中进行大规模验证。

（完）
