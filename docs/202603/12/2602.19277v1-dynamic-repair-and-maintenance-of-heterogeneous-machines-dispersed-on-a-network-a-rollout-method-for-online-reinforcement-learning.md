---
title: "Dynamic Repair and Maintenance of Heterogeneous Machines Dispersed on a Network: A Rollout Method for Online Reinforcement Learning"
title_zh: 分布在网络上的异构机器的动态维修与保养：一种用于在线强化学习的展开方法
authors: "Dongnuan Tian, Rob Shone"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19277v1"
tags: ["query:drl-jsp"]
score: 8.0
evidence: 用于机器维修和维护调度的在线强化学习
tldr: 本研究针对分布在网络中的异构机器动态维修问题，提出了一种在线强化学习方法。研究将问题建模为马尔可夫决策过程（MDP），旨在最小化长期平均成本。通过开发一种索引启发式策略作为基础，并结合基于Rollout的在线策略改进（OPI）技术，实现了在随机维修和切换时间下的高效决策。实验证明，该方法在快速变化的系统中表现接近最优，具有极高的实时处理能力。
selection_source: carryover_cache
motivation: 旨在优化单个维修人员在网络中维护多台地理分散且具有随机退化特性的异构机器的决策过程。
method: 构建MDP模型并提出索引启发式策略，随后利用基于Rollout的在线策略改进（OPI）方法进行实时决策优化。
result: 实验结果显示，该OPI算法在状态转移频率高达每秒100次的复杂动态系统中，依然能取得接近最优的成本控制效果。
conclusion: 结合索引启发式与Rollout技术的在线强化学习框架为大规模网络化机器维护问题提供了一种高效且实用的实时解决方案。
---

## 摘要
我们研究了一个由单名维修人员负责维护和修理分布在由节点和边构成的网络中不同位置的一组机器的问题。机器根据随机过程发生劣化，且随着接近完全失效，产生的成本不断增加。执行修理所需的时间以及维修人员在不同机器之间切换所需的时间都是随机的，且取决于具体的机器。该问题被建模为一个马尔可夫决策过程（MDP），其目标是最小化长期平均成本。我们证明了基于奖励的另一种等效表述方式，并据此开发了一种指数启发式策略，该策略在某些特殊情况下被证明是最优的。随后，我们利用基于展开（rollout-based）的强化学习技术开发了一种新型在线策略改进（OPI）方法，该方法将指数启发式策略作为基础策略，并在无法以足够置信度选择最佳动作的决策时刻将其作为一种保险选项。涉及随机生成的网络布局和参数值的大量数值实验结果表明，OPI 启发式算法在状态转移频率达每秒 100 次的快速变化系统中能够实现接近最优的性能，这表明该方法适用于在线实施。

## Abstract
We consider a problem in which a single repairer is responsible for the maintenance and repair of a collection of machines, positioned at different locations on a network of nodes and edges. Machines deteriorate according to stochastic processes and incur increasing costs as they approach complete failure. The times needed for repairs to be performed, and the amounts of time needed for the repairer to switch between different machines, are random and machine-dependent. The problem is formulated as a Markov decision process (MDP) in which the objective is to minimize long-run average costs. We prove the equivalence of an alternative formulation based on rewards and use this to develop an index heuristic policy, which is shown to be optimal in certain special cases. We then use rollout-based reinforcement learning techniques to develop a novel online policy improvement (OPI) approach, which uses the index heuristic as a base policy and also as an insurance option at decision epochs where the best action cannot be selected with sufficient confidence. Results from extensive numerical experiments, involving randomly-generated network layouts and parameter values, show that the OPI heuristic is able to achieve close-to-optimal performance in fast-changing systems with state transitions occurring 100 times per second, suggesting that it is suitable for online implementation.

---

## 论文详细总结（自动生成）

这篇论文针对分布在网络中的异构机器动态维修问题，提出了一种结合索引启发式算法与在线强化学习（Rollout方法）的决策框架。以下是对该论文的详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的工业维护、网络安全和医疗调度中，维修人员往往需要负责分布在不同地理位置（网络节点）的多台机器。这些机器具有**异构性**（退化率、修理难度、重要程度不同），且其退化过程是**随机的**。
*   **研究动机**：传统的维修调度模型往往忽略了维修人员在机器间切换的“切换时间”，或者假设切换不可中断。
*   **核心问题**：如何在考虑随机切换时间、随机修理时间以及机器多级退化状态的情况下，制定一个长期平均成本最低的动态调度策略。
*   **关键特性**：模型允许**修理和切换过程随时中断**（例如，在前往机器A的途中，机器B突然发生严重故障，维修人员可以立即改道）。

### 2. 提出的方法论
论文将问题建模为连续时间马尔可夫决策过程（MDP），并通过“一致化”（Uniformization）技术将其转化为离散时间模型。
*   **成本-奖励等效性证明**：论文首先证明了最小化长期平均成本等效于最大化一种特定的“修理奖励”。这简化了启发式指标的计算。
*   **索引启发式策略 ($\theta_{IND}$)**：
    *   为每个动作（留在原地修理或移动到其他节点）计算一个“索引值”（Index），代表单位时间内的预期奖励率。
    *   该策略在每个时间步重新计算，从而支持“中断”决策。
    *   论文证明了在特定简化场景（如齐次机器、二元状态、完全图或星型网络）下，该策略是理论最优的。
*   **在线策略改进（OPI）方法**：
    *   **核心思想**：利用强化学习中的 **Rollout（展开）** 技术。以索引策略为“基础策略”，通过模拟未来的演化轨迹来评估当前不同动作的优劣。
    *   **安全机制（可靠性权重）**：引入置信区间。只有当模拟结果以足够高的置信度表明某个动作优于基础策略时，才执行改进动作；否则，回退到基础策略以保证安全性。
    *   **嵌套模拟**：在系统状态转移的极短间隔内（如0.01秒），利用算力进行并行模拟，实时更新价值函数估计。

### 3. 实验设计
*   **场景设置**：基于 5x5 整数格点网络随机生成 1,000 个测试实例，机器数量 $m$ 从 2 到 8 不等。
*   **对比方法（Benchmarks）**：
    1.  **最优策略**：通过动态规划（策略迭代）计算，仅用于小规模实例（$m \le 4$）。
    2.  **轮询系统启发式 ($\theta_{POL}$)**：按照固定循环顺序访问机器。
    3.  **索引启发式 ($\theta_{IND}$)**：论文提出的基础索引策略。
*   **变量控制**：测试了不同的交通强度（$\rho$）、切换速度（$\eta$）、成本函数类型（线性、二次、分段线性）以及最大退化状态数（$K$）。

### 4. 资源与算力
*   **硬件**：Intel Core i5-6500T 桌面 CPU，8GB RAM。
*   **软件**：Python 3.9.10，使用 **PyPy** 即时编译器加速计算。
*   **算力说明**：文中未提到使用 GPU。在线决策阶段，算法被限制在 **0.01 秒** 内必须做出决策，以模拟高频动态环境。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 1,000 组随机实例实验。
*   **充分性**：实验覆盖了从低负载到高负载（$\rho$ 从 0.1 到 1.5）的各种情况，并对小规模问题进行了最优解对比。
*   **客观性**：使用了“公共随机数”方法确保不同策略在相同的随机序列下进行对比，保证了实验的公平性。

### 6. 主要结论与发现
*   **性能优异**：OPI 方法在所有测试中表现最佳。在已知最优解的小规模实例中，其成本仅比最优解高出约 **2.51%**（按奖励衡量仅高出 **1.02%**）。
*   **实时性强**：即使在状态每秒转移 100 次的极快系统中，OPI 也能在规定的 0.01 秒内完成计算并提供高质量决策。
*   **索引策略的局限**：虽然索引策略在某些特殊情况下最优，但在复杂网络拓扑中具有“近视”缺陷，而 OPI 通过 Rollout 成功克服了这一问题。

### 7. 优点
*   **理论与实践结合**：既有严谨的 MDP 理论证明（等效性、特殊情况最优性），又考虑了工程实现的实时性约束。
*   **鲁棒的安全机制**：通过可靠性权重防止了因模拟样本不足导致的错误决策，这在强化学习落地中非常重要。
*   **高度灵活**：网络拓扑、成本函数和退化过程均可自定义，适用范围广。

### 8. 不足与局限
*   **单维修人员限制**：目前模型仅支持单个维修人员，无法直接应用于需要多员协作的大型系统。
*   **完全可观测性假设**：假设维修人员随时知道所有机器的精确状态，但在现实中，机器状态往往需要通过检查（Inspection）才能获知（即 POMDP 问题）。
*   **网络规模较小**：实验主要集中在 5x5 的小型网络，对于超大规模节点网络的扩展性有待进一步验证。

（完）
