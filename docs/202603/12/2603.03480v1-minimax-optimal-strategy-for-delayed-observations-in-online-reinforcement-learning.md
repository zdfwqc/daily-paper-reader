---
title: Minimax Optimal Strategy for Delayed Observations in Online Reinforcement Learning
title_zh: 在线强化学习中延迟观测的极小极大最优策略
authors: "Harin Lee, Kevin Jamieson"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03480v1"
tags: ["query:drl"]
score: 8.0
evidence: 延迟观测 MDP 的数学基础和遗憾界分析
tldr: 本研究针对在线强化学习中状态观测存在随机延迟的问题，提出了一种结合状态增强法与置信上限（UCB）策略的新算法。在表格马尔可夫决策过程（MDP）下，该算法实现了与延迟上限相关的极小极大最优遗憾界。研究还将该问题推广至一类具有特定分解结构的MDP通用框架，并证明了其理论最优性，为处理延迟观测环境下的强化学习提供了严谨的理论基础。
selection_source: fresh_fetch
motivation: 旨在解决强化学习中智能体无法即时观测当前状态，且观测延迟具有随机性时的最优策略设计与理论界限问题。
method: 提出一种将状态增强技术与上置信界算法相结合的方法，并将其建模为具有已知和未知分解结构的特殊MDP进行分析。
result: 在表格MDP中推导出与延迟上限相关的遗憾界，并提供了匹配的下界证明，证明了该算法在极小极大意义下的最优性。
conclusion: 该研究确立了延迟观测强化学习的理论性能极限，并为具有结构化转移特性的MDP提供了一个具有独立研究价值的通用分析框架。
---

## 摘要
我们研究了具有延迟状态观测的强化学习，其中智能体在随机步数之后才能观测到当前状态。我们提出了一种结合了状态增强方法和置信上限（UCB）方法的算法。对于表格型马尔可夫决策过程（MDPs），我们推导出了 $\tilde{\mathcal{O}}(H \sqrt{D_{\max} SAK})$ 的累积遗憾界，其中 $S$ 和 $A$ 分别是状态空间和动作空间的基数，$H$ 是时间跨度，$K$ 是回合数，$D_{\max}$ 是最大延迟长度。我们还提供了一个在对数因子内匹配的下界，证明了我们方法的最优性。我们的分析框架将该问题公式化为一类更广泛 MDP 的特例，其转移动态可分解为一个已知部分和一个未知但具有结构的部分。我们为这种抽象设定建立了通用结果，这些结果可能具有独立的学术价值。

## Abstract
We study reinforcement learning with delayed state observation, where the agent observes the current state after some random number of time steps. We propose an algorithm that combines the augmentation method and the upper confidence bound approach. For tabular Markov decision processes (MDPs), we derive a regret bound of $\tilde{\mathcal{O}}(H \sqrt{D_{\max} SAK})$, where $S$ and $A$ are the cardinalities of the state and action spaces, $H$ is the time horizon, $K$ is the number of episodes, and $D_{\max}$ is the maximum length of the delay. We also provide a matching lower bound up to logarithmic factors, showing the optimality of our approach. Our analytical framework formulates this problem as a special case of a broader class of MDPs, where their transition dynamics decompose into a known component and an unknown but structured component. We establish general results for this abstract setting, which may be of independent interest.

---

## 论文详细总结（自动生成）

这篇论文针对在线强化学习中“状态观测延迟”这一核心挑战，从理论层面给出了极小极大（Minimax）最优的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
在标准的强化学习（RL）框架中，智能体通常假设能立即观测到当前状态。然而在现实场景（如机器人控制、自动驾驶、在线广告投放）中，由于传感器处理、数据传输或计算开销，观测往往存在延迟。
*   **核心挑战**：延迟迫使智能体必须预先规划一系列动作，而动作序列的数量随延迟长度呈指数级增长。
*   **研究空白**：现有研究虽然证明了多项式样本复杂度的可能性，但其遗憾界（Regret Bound）非常松散，且尚未明确延迟长度（$D_{\max}$）如何具体影响学习的统计复杂度。
*   **目标**：推导出延迟观测下表格型 MDP 的最优遗憾界，并提供匹配的理论下界。

### 2. 方法论：核心思想与算法流程
论文提出了一种名为 **MVP-Delayed** 的算法，其核心逻辑分为两步：

*   **状态增强（State Augmentation）**：
    将具有延迟的原始 MDP 转换为一个等效的、无延迟的“增强 MDP”。增强状态定义为三元组：$(s_{t_h}, \mathbf{a}, e\Delta)$。
    *   $s_{t_h}$：最后一次观测到的状态。
    *   $\mathbf{a}$：尚未观测到结果的动作队列。
    *   $e\Delta$：自上次观测以来经过的时间步数。
*   **部分已知动力学的分解（Partially Known Dynamics）**：
    这是本文降低遗憾界的关键。作者发现增强 MDP 的转移概率可以分解为：
    1.  **已知部分**：动作队列的移动（进队/出队）和时间步的增加是确定的。
    2.  **未知部分**：仅包含原始 MDP 的状态转移 $P$ 和延迟分布 $P_{delay}$。
    通过只学习这些“结构化”的未知部分，算法避免了在指数级规模的增强状态空间上直接进行探索。
*   **算法实现**：
    基于 **MVP**（一种带有 Bernstein 类型奖励修正的 UCBVI 算法）进行改进。算法在计算 Q 值时，利用已知结构共享不同增强状态间的转移信息，从而实现高效学习。

### 3. 实验设计
**注意：本论文是一篇纯理论研究论文，未包含基于真实数据集或仿真环境的数值实验。**
*   **理论验证场景**：作者设计了一个名为 **CodeMDP** 的特殊硬实例（Hard Instance）来证明下界。
*   **Benchmark（基准）**：对比对象是 Chen et al. (2023) 提出的遗憾界。
*   **对比维度**：主要对比遗憾界对 $H$（时间跨度）、$D_{\max}$（最大延迟）、$S$（状态数）、$A$（动作数）和 $K$（回合数）的依赖关系。

### 4. 资源与算力
*   论文中**未提及**任何 GPU 型号、算力资源或训练时长。
*   由于是纯数学证明和算法复杂度分析，其核心贡献在于理论推导而非实验性能。

### 5. 实验数量与充分性
*   **理论充分性**：作者提供了详尽的数学证明（附录长达 20 余页），涵盖了上界推导、下界构造以及计算复杂度的硬度分析。
*   **客观性**：通过构造匹配的上下界，客观地确定了该问题的统计难度。
*   **局限性**：缺乏数值模拟来验证在有限样本下的实际表现。

### 6. 主要结论与发现
*   **最优遗憾界**：证明了在表格 MDP 下，延迟观测的遗憾界为 $\tilde{\mathcal{O}}(H \sqrt{D_{\max} SAK})$。
*   **改进幅度**：相比前人最好结果，在延迟因子 $D_{\max}$ 上实现了从 $D_{\max}^{5/2}$ 到 $D_{\max}^{1/2}$ 的显著优化。
*   **极小极大最优性**：通过构造下界 $\Omega(H \sqrt{D_{\max} SAK} / \sqrt{\log D_{\max}})$，证明了该算法在统计学上是近乎最优的。
*   **计算硬度**：证明了求解延迟 MDP 是 **NP-hard** 的（通过归约 3-SAT 问题），这意味着虽然统计上可以高效学习，但计算上的指数级开销（随 $D_{\max}$）可能是无法避免的。

### 7. 优点
*   **理论严谨性**：首次给出了延迟观测 RL 的匹配上下界，填补了该领域的理论空白。
*   **通用框架**：提出了“部分已知动力学 MDP”的抽象模型，该框架不仅适用于延迟观测，也可推广到其他具有已知结构特征的 RL 问题。
*   **依赖项优化**：大幅提升了遗憾界对延迟长度和时间跨度的依赖精度。

### 8. 不足与局限
*   **计算效率**：算法的时间复杂度随延迟长度 $D_{\max}$ 呈指数级增长。虽然论文证明了这是 NP-hard 的，但在实际应用中（当延迟很大时）该算法难以运行。
*   **表格法限制**：目前仅限于表格型（Tabular）设置，尚未扩展到函数逼近（如深度强化学习）或连续状态空间。
*   **缺乏实证**：没有在实际环境或模拟器中验证算法的鲁棒性。

（完）
