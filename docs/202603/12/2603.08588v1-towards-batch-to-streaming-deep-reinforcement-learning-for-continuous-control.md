---
title: Towards Batch-to-Streaming Deep Reinforcement Learning for Continuous Control
title_zh: 迈向连续控制的批处理到流式深度强化学习
authors: "Riccardo De Monte, Matteo Cederle, Gian Antonio Susto"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08588v1"
tags: ["query:drl"]
score: 9.0
evidence: 用于连续控制的新型流式深度强化学习算法
tldr: 本研究针对深度强化学习在资源受限硬件上计算复杂度高的问题，提出了 S2AC 和 SDAC 两种流式深度强化学习算法。这些算法通过纯在线更新取代了传统的经验回放和批处理，显著降低了内存和计算开销。该方法特别强调与现有批处理 RL 方法的兼容性，非常适合 Sim2Real 迁移等设备端微调应用。实验证明，新算法在标准基准上表现优异且无需复杂调参，并为批处理到流式学习的过渡提供了实用策略。
selection_source: fresh_fetch
motivation: 传统深度强化学习依赖经验回放和批处理，导致其计算复杂度难以满足资源受限硬件的实时控制需求。
method: 提出了 S2AC 和 SDAC 算法，通过纯在线更新机制实现与主流批处理强化学习方法的无缝兼容。
result: 在标准连续控制基准测试中，所提算法在无需繁琐调参的情况下达到了与现有流式 RL 基准相当的性能。
conclusion: 该研究成功解决了从批处理向流式学习过渡的实际挑战，为机器人 Sim2Real 等设备端微调任务提供了高效方案。
---

## 摘要
尖端的深度强化学习（RL）方法在连续控制任务中取得了显著性能，但由于依赖经验回放池（replay buffers）、批更新（batch updates）和目标网络（target networks），其计算复杂度往往与资源受限硬件的约束不相容。新兴的流式深度强化学习范式通过纯在线更新解决了这一局限，在标准基准测试中取得了强劲的经验性能。在这项工作中，我们提出了两种新型流式深度强化学习算法：流式软 Actor-Critic（S2AC）和流式确定性 Actor-Critic（SDAC），它们被明确设计为与尖端的批处理强化学习方法兼容，使其特别适用于设备端微调应用，如 Sim2Real 迁移。两种算法在标准基准测试中均达到了与尖端流式基准相当的性能，且无需繁琐的超参数调优。最后，我们进一步研究了微调过程中从批处理学习过渡到流式学习的实际挑战，并提出了应对这些挑战的具体策略。

## Abstract
State-of-the-art deep reinforcement learning (RL) methods have achieved remarkable performance in continuous control tasks, yet their computational complexity is often incompatible with the constraints of resource-limited hardware, due to their reliance on replay buffers, batch updates, and target networks. The emerging paradigm of streaming deep RL addresses this limitation through purely online updates, achieving strong empirical performance on standard benchmarks. In this work, we propose two novel streaming deep RL algorithms, Streaming Soft Actor-Critic (S2AC) and Streaming Deterministic Actor-Critic (SDAC), explicitly designed to be compatible with state-of-the-art batch RL methods, making them particularly suitable for on-device finetuning applications such as Sim2Real transfer. Both algorithms achieve performance comparable to state-of-the-art streaming baselines on standard benchmarks without requiring tedious hyperparameter tuning. Finally, we further investigate the practical challenges of transitioning from batch to streaming learning during finetuning and propose concrete strategies to tackle them.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Batch-to-Streaming Deep Reinforcement Learning for Continuous Control》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现代深度强化学习（DRL）算法（如 SAC、TD3）虽然性能强大，但高度依赖**经验回放池（Replay Buffer）**、**批处理更新（Batch Updates）**和**目标网络（Target Networks）**。这些机制带来了巨大的内存消耗和计算开销，使其难以部署在资源受限的边缘设备（如微型机器人）上。
*   **核心问题**：现有的“流式（Streaming）”RL 算法虽然解决了计算开销问题，但与主流的“批处理（Batch）”算法在架构上不兼容。这导致开发者无法先在模拟器中用批处理算法训练，再在真实设备上用流式算法进行微调（Sim2Real）。
*   **整体含义**：本文旨在打破批处理学习与流式学习之间的隔阂，提出了一套兼容主流架构的流式 RL 框架，支持从离线/模拟训练到在线/设备端微调的平滑过渡。

### 2. 方法论
论文提出了两种新算法：**S2AC**（流式软 Actor-Critic）和 **SDAC**（流式确定性 Actor-Critic）。

*   **核心思想**：
    *   **纯在线更新**：完全舍弃回放池和目标网络，每个时间步仅利用当前样本进行更新。
    *   **资格迹（Eligibility Traces）**：引入 $\lambda$ 参数来平衡偏差和方差，增强信用分配能力。
    *   **ObGD 优化器**：使用“过冲限制梯度下降（Overshooting-bounded Gradient Descent）”来稳定在线更新，防止因单个样本噪声导致的参数剧烈波动。
*   **关键技术细节**：
    *   **S2AC 的动态熵系数**：由于流式学习中使用了奖励缩放（Reward Scaling），固定的温度参数 $\alpha$ 会失效。S2AC 提出将 $\alpha$ 除以奖励的标准差 $\sigma_r$，以保持奖励与熵之间的相对平衡。
    *   **SDAC 的目标噪声**：在计算 Q 值目标时引入高斯噪声，防止 Critic 网络对特定动作过拟合，提高学习稳定性。
    *   **Batch-to-Stream 过渡策略**：发现 Adam 优化器在预训练阶段会产生巨大的权重范数，降低网络塑性。论文建议在批处理预训练阶段使用 **SGDC（带裁剪的随机梯度下降）** 替代 Adam，以保证后续流式微调的成功。

### 3. 实验设计
*   **实验场景**：
    *   **MuJoCo Gym**：包括 Humanoid, Hopper, Ant, Walker2d 等经典连续控制任务。
    *   **DM Control Suite**：包括 Dog-walk, Walker-run, Quadruped-run 等更复杂的机器人仿真环境。
*   **Benchmark（基准）**：
    *   **Stream AC($\lambda$)**：目前最先进的流式 DRL 算法。
    *   **AVG**：另一种基于 Q 值的流式算法。
*   **对比方法**：
    *   标准的批处理算法（SAC, TD3）作为性能上限参考。
    *   针对微调场景，对比了直接切换算法与使用论文建议策略后的性能差异。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或数量。
*   **训练规模**：实验涵盖了从 300 万步（3M）到 2000 万步（20M）不等的训练时长。考虑到流式算法不需要维护回放池，其内存占用极低，理论上可以在 CPU 或低端边缘计算模块上运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   所有实验均使用了 **10 个不同的随机种子**，保证了结果的统计显著性。
    *   涵盖了超过 10 种不同的连续控制环境。
*   **充分性与客观性**：
    *   **消融实验**：详细测试了动态 $\alpha$、目标噪声、优化器选择对性能的影响。
    *   **微调实验**：模拟了环境扰动（如改变关节刚度、控制增益）来验证 Sim2Real 的适应能力。
    *   **公平性**：对比基准 Stream AC($\lambda$) 使用了原作者推荐的最优参数，实验设计较为客观。

### 6. 主要结论与发现
*   **性能达标**：S2AC 和 SDAC 在无需针对每个环境单独调参的情况下，达到了与 SOTA 流式算法相当的水平。
*   **归一化的重要性**：即使是传统的批处理算法（SAC/TD3），引入流式学习中的状态归一化和奖励缩放也能显著提升性能。
*   **优化器陷阱**：Adam 优化器在预训练中积累的“权重硬化”是导致流式微调失败的主因，改用 SGDC 可显著改善过渡效果。
*   **微调潜力**：通过预训练初始化，流式算法可以用极少的样本超越从零开始训练的效果。

### 7. 优点
*   **实用导向**：直接解决了机器人领域 Sim2Real 迁移中“算力不对等”的痛点。
*   **架构兼容**：S2AC/SDAC 与 SAC/TD3 结构高度一致，降低了工程实现难度。
*   **鲁棒性**：算法对超参数不敏感，具有较强的通用性。

### 8. 不足与局限
*   **复杂任务挑战**：在极高维或极复杂的任务（如 Quadruped-run）中，从批处理到流式的过渡仍会出现明显的性能下降，尚未完全实现“无缝”切换。
*   **S2AC 微调不成熟**：论文提到 S2AC 在微调阶段的表现不如 SDAC 稳定，熵系数的调节在分布偏移（Distribution Shift）下依然敏感。
*   **应用限制**：目前仅验证了仿真环境，尚未在真实的物理硬件上进行最终测试。

（完）
