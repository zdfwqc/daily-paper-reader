---
title: "Learning to Score: Tuning Cluster Schedulers through Reinforcement Learning"
title_zh: 学习评分：通过强化学习调优集群调度器
authors: "Martin Asenov, Qiwen Deng, Gingfung Yeung, Adam Barker"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10545v1"
tags: ["query:drl-jsp"]
score: 9.0
evidence: 用于学习调度器评分算法权重的强化学习方法
tldr: "本研究针对大规模集群调度中评分函数权重难以手动优化的问题，提出了一种基于强化学习（RL）的自动调优方法。该方法通过引入百分比改进奖励、帧堆叠技术以及限制领域信息，实现了对调度器评分权重的动态优化，以适应不同工作负载。实验表明，该方法在无服务器场景下显著提升了作业的端到端性能，相比固定权重平均提升了33%，证明了RL在提升集群资源利用率和调度效率方面的巨大潜力。"
selection_source: fresh_fetch
motivation: 传统的集群调度器通常对评分函数采用固定权重，难以适应多样化的工作负载且手动调优成本极高。
method: 提出一种利用强化学习动态调整评分权重的方法，结合了百分比改进奖励、帧堆叠和限制领域信息等技术。
result: "在实验中，该方法相比固定权重方案性能提升了33%，相比表现最好的基准算法提升了12%。"
conclusion: 通过强化学习自动优化调度评分权重，可以有效提升集群性能并具备在不同集群和负载下的泛化能力。
---

## 摘要
在大规模集群中高效地将传入作业分配给节点，可以显著提高集群利用率和作业性能。为了分配传入作业，集群调度器通常依赖一组评分函数来对可行节点进行排名。各个评分函数的结果通常被赋予相等的权重，这可能导致次优部署，因为这种“一刀切”的方案没有考虑到每个工作负载的特征。然而，调整评分函数的权重需要专家知识，且计算成本高昂。本文提出了一种强化学习方法，用于学习调度器评分算法中的权重，其总体目标是提高给定集群中作业的端到端性能。我们的方法基于百分比改进奖励、帧堆叠和限制领域信息。我们提出了百分比改进奖励来解决多步参数调优的目标。引入帧堆叠允许在整个优化实验中传递信息。限制领域信息可以防止过拟合，并提高在未知集群和工作负载中的性能。该策略在不同的工作负载和集群设置组合上进行训练。我们证明，在实验室环境下的无服务器场景中，所提方法与固定权重相比平均性能提升了 33%，与表现最好的基准方法相比提升了 12%。

## Abstract
Efficiently allocating incoming jobs to nodes in large-scale clusters can lead to substantial improvements in both cluster utilization and job performance. In order to allocate incoming jobs, cluster schedulers usually rely on a set of scoring functions to rank feasible nodes. Results from individual scoring functions are usually weighted equally, which could lead to sub-optimal deployments as the one-size-fits-all solution does not take into account the characteristics of each workload. Tuning the weights of scoring functions, however, requires expert knowledge and is computationally expensive.   This paper proposes a reinforcement learning approach for learning the weights in scheduler scoring algorithms with the overall objective of improving the end-to-end performance of jobs for a given cluster. Our approach is based on percentage improvement reward, frame-stacking, and limiting domain information. We propose a percentage improvement reward to address the objective of multi-step parameter tuning. The inclusion of frame-stacking allows for carrying information across an optimization experiment. Limiting domain information prevents overfitting and improves performance in unseen clusters and workloads. The policy is trained on different combinations of workloads and cluster setups. We demonstrate the proposed approach improves performance on average by 33\% compared to fixed weights and 12\% compared to the best-performing baseline in a lab-based serverless scenario.

---

## 论文详细总结（自动生成）

这篇论文《Learning to Score: Tuning Cluster Schedulers through Reinforcement Learning》由华为爱丁堡研究所的研究团队撰写，提出了一种利用强化学习（RL）自动调优集群调度器评分函数权重的方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现代集群调度器（如 Kubernetes、Borg）通常采用“过滤+评分”两步走策略。在评分阶段，多个评分函数（如资源利用率、数据本地性等）通常被赋予相等的固定权重。
*   **核心挑战**：由于不同工作负载（如无服务器函数、批处理、AI 训练）对资源的需求差异巨大，这种“一刀切”的权重配置往往导致性能次优。而手动调优权重需要极高的专家知识，且搜索空间巨大、计算成本高昂。
*   **研究目标**：开发一种能够根据集群配置和工作负载特征，自动、动态调整评分权重的智能代理。

### 2. 方法论
*   **核心思想**：将多步参数调优任务建模为一个强化学习问题，通过学习历史调优经验来建立更强的采样偏置，从而比传统黑盒优化（如贝叶斯优化）更高效地找到最优权重。
*   **关键技术细节**：
    *   **状态空间（State）**：包含静态信息（集群规模、节点类型、负载类型）和动态信息（已尝试的权重及其对应的奖励值）。
    *   **动作空间（Action）**：输出一组评分函数的权重值。
    *   **百分比改进奖励（Percentage Improvement Reward）**：定义为当前实验中最佳结果相对于默认固定权重配置的提升比例。这种设计使奖励在不同实验间归一化，专注于优化提升率。
    *   **帧堆叠（Frame-stacking）**：通过堆叠历史动作-奖励对，使 RL 代理能够感知之前的尝试，从而在单次优化实验的多步迭代中做出更好决策。
    *   **限制领域信息（Limiting Domain Information）**：为了防止代理过拟合于特定的集群配置，故意只提供粗粒度的领域信息，以增强其在未知环境下的泛化能力。
*   **算法流程**：使用 **Soft Actor-Critic (SAC)** 算法进行训练，利用其最大熵特性鼓励探索，防止陷入局部最优。

### 3. 实验设计
*   **实验场景**：基于 FaaS（无服务器计算）场景，使用高保真模拟器 `faas-sim` 进行大规模实验。
*   **数据集/配置**：
    *   **集群**：8 种异构集群配置（包括云端 CPU/GPU、边缘设备如树莓派、Jetson 等）和 2 种网络拓扑（互联网型、城市网络型）。
    *   **负载**：包含 ResNet 推理/预处理、MobileNet、语音识别等 8 种典型函数。
*   **Benchmark 与对比方法**：
    *   **Fixed**：固定等权重（类似 Kubernetes 默认配置）。
    *   **RS**：随机搜索（Random Search）。
    *   **BO**：贝叶斯优化（Bayesian Optimization）。
    *   **TPE**：树状 Parzen 估计器（Tree-Structured Parzen Estimator）。

### 4. 资源与算力
*   **模型架构**：使用了 Stable Baselines3 库，策略网络和 Q 网络均为 512x512x512 的多层感知机（MLP），采用 ReLU 激活函数。
*   **算力说明**：文中提到使用了多个并行环境进行训练以提高效率，但**未明确指出具体的 GPU 型号、数量或具体的训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **训练集**：仅在 3 种集群配置上进行训练。
    *   **测试集**：在 10 组“相似配置”和 10 组“全新配置”（未见过的集群、负载和调度选项）上进行评估。
*   **充分性评价**：实验设计较为充分，特别强调了**泛化性测试**。通过在完全未见的异构硬件组合和网络拓扑上进行测试，客观地验证了 RL 代理的鲁棒性，而非仅仅是记忆训练数据。

### 6. 主要结论与发现
*   **性能提升**：在相似场景下，RL 方法比固定权重提升了 **33%**，比表现最好的基准方法（TPE/BO）提升了约 **20%**。
*   **泛化能力**：在全新（未见过）的场景下，RL 方法依然保持领先，比固定权重提升 **20%**，比最强基准提升 **6%**。
*   **权重偏好**：RL 代理学会了识别哪些评分函数是关键的（例如在某些场景下“能力标签”比“本地性”更重要），并能根据环境动态调整这些偏好。

### 7. 优点
*   **工程易实现性**：该方法不需要修改现有的调度器核心逻辑，只需调整权重参数，非常适合集成到 Kubernetes 等现有系统中。
*   **高效采样**：通过 RL 学习到的偏置，在极少的尝试次数（如 5 次采样）内就能找到优于传统优化算法的结果。
*   **创新的奖励设计**：百分比改进奖励解决了不同基准线下奖励值难以统一的问题。

### 8. 不足与局限
*   **场景局限性**：实验主要集中在 FaaS/Serverless 负载，对于长时运行的批处理任务或复杂的微服务链条，其表现尚待验证。
*   **搜索深度限制**：实验中每次优化仅允许 5 次采样，虽然证明了高效性，但未探讨在更多采样次数下，RL 是否会被更成熟的 BO 算法超越。
*   **模拟器依赖**：所有结果均来自模拟环境 `faas-sim`，虽然该模拟器考虑了网络和硬件异构性，但与真实生产环境中的系统抖动和复杂干扰仍可能存在偏差。

（完）
