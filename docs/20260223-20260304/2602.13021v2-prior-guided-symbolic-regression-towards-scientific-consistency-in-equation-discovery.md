---
title: "Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery"
title_zh: 先验引导的符号回归：迈向方程发现中的科学一致性
authors: "Jing Xiao, Xinhai Chen, Jiaming Peng, Qinglin Wang, Menghan Jia, Zhiquan Lai, Guangping Yu, Dongsheng Li, Tiejun Li, Jie Liu"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13021v2"
tags: ["query:sr"]
score: 10.0
evidence: 先验引导的符号回归，确保科学一致性和物理约束
tldr: 针对符号回归中方程虽拟合数据但违背科学原理的“伪方程陷阱”，本文提出PG-SR框架。该框架通过预热、演化和精炼三阶段流程，引入先验约束检查器和先验退火约束评估机制（PACE），将领域先验转化为可执行约束，引导搜索过程。理论证明其降低了假设空间复杂度，实验显示其在多领域优于现有方法，且对噪声和数据稀缺具有鲁棒性。
selection_source: fresh_fetch
motivation: 现有的符号回归方法过度依赖经验风险最小化，导致发现的方程往往符合数据观测却违背基本的科学原理。
method: 提出PG-SR框架，利用先验约束检查器将领域知识编码为约束，并通过先验退火约束评估机制（PACE）在演化过程中引导方程发现。
result: 实验表明PG-SR在多个领域均优于最先进的基准方法，并在面对低质量先验、噪声数据和数据稀缺时表现出极强的鲁棒性。
conclusion: 该研究通过引入先验引导机制，有效解决了符号回归中的科学一致性问题，为发现具有物理意义的科学方程提供了理论保障和实践路径。
---

## 摘要
符号回归 (SR) 旨在从观测数据中发现可解释的方程，具有揭示自然现象背后潜在原理的潜力。然而，现有方法往往会陷入“伪方程陷阱”：生成的方程虽然能很好地拟合观测数据，但与基本科学原理不一致。一个关键原因是这些方法由经验风险最小化主导，缺乏确保科学一致性的显式约束。为了弥补这一差距，我们提出了 PG-SR，这是一个基于预热、进化和精炼三阶段流水线构建的先验引导符号回归框架。在整个流水线中，PG-SR 引入了一个先验约束检查器，将领域先验显式编码为可执行的约束程序，并在进化阶段采用先验退火约束评估 (PACE) 机制，逐步引导发现过程转向符合科学一致性的区域。在理论上，我们证明了 PG-SR 降低了假设空间的 Rademacher 复杂度，从而产生了更紧致的泛化界限，并为防止伪方程提供了保证。在实验上，PG-SR 在多个领域均优于最先进的基准方法，并在面对不同的先验质量、噪声数据和数据稀缺性时保持了鲁棒性。

## Abstract
Symbolic Regression (SR) aims to discover interpretable equations from observational data, with the potential to reveal underlying principles behind natural phenomena. However, existing approaches often fall into the Pseudo-Equation Trap: producing equations that fit observations well but remain inconsistent with fundamental scientific principles. A key reason is that these approaches are dominated by empirical risk minimization, lacking explicit constraints to ensure scientific consistency. To bridge this gap, we propose PG-SR, a prior-guided SR framework built upon a three-stage pipeline consisting of warm-up, evolution, and refinement. Throughout the pipeline, PG-SR introduces a prior constraint checker that explicitly encodes domain priors as executable constraint programs, and employs a Prior Annealing Constrained Evaluation (PACE) mechanism during the evolution stage to progressively steer discovery toward scientifically consistent regions. Theoretically, we prove that PG-SR reduces the Rademacher complexity of the hypothesis space, yielding tighter generalization bounds and establishing a guarantee against pseudo-equations. Experimentally, PG-SR outperforms state-of-the-art baselines across diverse domains, maintaining robustness to varying prior quality, noisy data, and data scarcity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
符号回归（SR）旨在从观测数据中发现可解释的数学方程。然而，现有方法普遍面临**“伪方程陷阱”（Pseudo-Equation Trap）**：即方程在训练集上拟合精度极高（MSE极低），但其数学结构违背了基本的科学原理（如物理守恒、生物单峰响应等），导致在分布外（OOD）数据上表现糟糕。
*   **研究动机**：现有的搜索算法、Transformer模型或大语言模型（LLM）驱动的SR方法大多由“经验风险最小化”主导，缺乏显式的科学一致性约束。
*   **核心目标**：引入领域先验知识作为显式约束，引导搜索过程向符合科学逻辑的区域收敛，从而发现真正具有科学意义的方程。

### 2. 方法论：核心思想与技术细节
论文提出了 **PG-SR** 框架，其核心由一个三阶段流水线和两个关键机制组成：
*   **三阶段流水线**：
    1.  **预热（Warm-up）**：利用数据统计分析引导LLM生成初步的方程骨架，初始化经验池。
    2.  **演化（Evolution）**：在多个并行“岛屿”中进行迭代，LLM根据历史高分方程和成功/失败的见解（Insights）生成新假设。
    3.  **精炼（Refinement）**：针对残差进行统计分析，利用反射机制（Reflection）对最优候选方程进行结构微调。
*   **关键机制**：
    1.  **先验约束检查器（Prior Constraint Checker）**：通过LLM辅助、人工校验的方式，将领域知识（如单调性、边界条件）编码为可执行的Python程序。
    2.  **先验退火约束评估（PACE）**：这是本文的核心创新。它不直接剔除违背先验的方程，而是采用一种“退火”策略：在搜索初期允许一定的违背以保持多样性，随着迭代进行，逐步加大对违背先验方程的惩罚，最终强制收敛到科学一致的空间。
*   **理论支撑**：论文通过统计学习理论证明，引入先验约束能有效降低假设空间的 **Rademacher 复杂度**，从而获得更紧致的泛化界限。

### 3. 实验设计
*   **数据集/场景**：涵盖了生物、材料、化学和物理四大领域：
    *   大肠杆菌（E. coli）生长动力学。
    *   铝合金 6061-T651 的**真实实验应力-应变数据**。
    *   化学反应动力学（CRK）。
    *   两种非线性振荡器（时齐与非时齐）。
*   **对比基准（Baselines）**：共对比了 13 种最先进的方法，分为三类：
    *   **搜索类**：PySR, GPLearn, DSR, uDSR, Operon, GP-GOMEA。
    *   **Transformer类**：TPSR, E2E, NeSymReS, PhyE2E。
    *   **LLM类**：LLM-SR, LaSR, DrSR。
*   **评估指标**：主要使用归一化均方误差（NMSE），重点关注 **OOD（分布外）泛化性能**。

### 4. 资源与算力
论文在附录 I 中明确说明了实验环境：
*   **硬件**：Intel Core i9-14900HX CPU，单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **耗时**：在一个标准基准测试（如振荡器）上运行一次完整流程约需 **10 小时**。
*   **成本**：使用 `gpt-4o-mini` 作为后端，单次运行消耗约 1785 万个 Token，折合商业 API 成本约 **4.70 美元**。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的任务上进行了全面测试，每个任务均包含 ID（分布内）和 OOD（分布外）评估。
*   **消融实验**：针对预热阶段、PACE 机制和精炼阶段分别做了剔除实验，验证了各组件的必要性。
*   **鲁棒性测试**：
    *   **先验质量**：测试了无先验、弱先验、错误先验和全先验下的表现，证明 PACE 机制能防止模型被错误先验误导。
    *   **噪声干扰**：在不同高斯噪声水平下验证了稳定性。
    *   **数据稀缺性**：测试了从 100% 到 5% 数据量下的表现，验证了先验在小样本下的引导作用。
*   **客观性**：实验采用了 LLMSR-Bench 这一新基准以防止 LLM 记忆效应，并对比了不同 LLM 后端（GPT-4o-mini 和 Llama-3.3-70B），结果具有较强的说服力。

### 6. 主要结论与发现
*   **解决伪方程问题**：PG-SR 在所有任务中均取得了最低的 OOD NMSE，证明了显式先验约束是解决“拟合好但物理错”问题的关键。
*   **科学发现能力**：在振荡器任务中，PG-SR 几乎完美还原了真实的物理方程结构和系数。
*   **PACE 的有效性**：相比于硬性过滤约束，PACE 的退火策略能更好地平衡搜索空间的探索（Exploration）与利用（Exploitation）。
*   **理论与实践一致**：实验结果验证了降低 Rademacher 复杂度确实能提升模型的泛化能力。

### 7. 优点
*   **理论严谨**：不仅有工程实现，还提供了详细的统计学习理论证明。
*   **机制创新**：PACE 机制巧妙地解决了约束优化中的梯度消失/搜索停滞问题。
*   **现实意义强**：在真实世界的材料实验数据上表现优异，证明了其处理噪声和复杂现实系统的潜力。
*   **鲁棒性高**：即使先验知识存在偏差，模型也不会崩溃，具有较好的容错性。

### 8. 不足与局限
*   **人工干预**：虽然使用了 LLM 辅助，但在构建“可执行先验约束程序”时仍需要一定程度的人工校验和领域专家介入，尚未实现完全自动化。
*   **推理成本**：相比于传统的遗传算法（如 PySR），基于 LLM 的迭代过程在时间和金钱成本上较高。
*   **应用限制**：对于完全缺乏先验知识的“黑盒”系统，该框架的优势将难以发挥。

（完）
