---
title: An Empirical Investigation of Neural ODEs and Symbolic Regression for Dynamical Systems
title_zh: 神经常微分方程与符号回归在动力系统中的实证研究
authors: "Panayiotis Ioannou, Pietro Liò, Pietro Cicuta"
date: 2026-01-28
pdf: "https://arxiv.org/pdf/2601.20637v1"
tags: ["query:sr"]
score: 10.0
evidence: 研究符号回归用于恢复控制方程
tldr: 本研究探讨了利用神经常微分方程（NODEs）和符号回归（SR）建模复杂动力系统并发现其控制方程的方法。通过对阻尼振荡系统的实验，研究发现NODEs在动力学相似的情况下具有良好的外推能力，而SR能有效从噪声数据中恢复方程。最重要的是，即使在训练数据极少的情况下，利用NODE生成的数据也能辅助SR推断出物理定律，为科学发现提供了新路径。
selection_source: fresh_fetch
motivation: 准确建模复杂动力系统并发现其背后的控制微分方程是加速科学发现的关键任务。
method: 通过阻尼振荡系统的噪声合成数据，评估了神经常微分方程的外推性能以及符号回归恢复底层方程的能力。
result: "NODEs在相似动力学条件下外推效果显著，且SR能从仅基于10%原始数据训练的NODE所生成的数据中成功恢复大部分方程。"
conclusion: 结合NODEs丰富有限数据与符号回归推断物理定律的方法，为科学探索和规律发现提供了一种极具前景的新策略。
---

## 摘要
准确建模复杂系统的动力学并发现其控制微分方程是加速科学发现的关键任务。利用来自两个阻尼振荡系统的带噪声合成数据，我们探索了神经常微分方程（NODEs）的外推能力以及符号回归（SR）恢复底层方程的能力。我们的研究得出了三个关键见解。首先，我们证明了只要生成的轨迹与训练数据具有动力学相似性，NODEs 就能有效地外推到新的边界条件。其次，SR 成功地从带噪声的真实数据中恢复了方程，尽管其性能取决于输入变量的正确选择。最后，我们发现，当使用仅在完整模拟数据的 10% 上训练的 NODE 所生成的数据时，SR 恢复了三个控制方程中的两个，并对第三个方程给出了良好的近似。虽然最后这一发现指出了未来工作的方向，但我们的结果表明，利用 NODEs 丰富有限的数据并使符号回归能够推断物理定律，代表了一种极具前景的科学发现新方法。

## Abstract
Accurately modelling the dynamics of complex systems and discovering their governing differential equations are critical tasks for accelerating scientific discovery. Using noisy, synthetic data from two damped oscillatory systems, we explore the extrapolation capabilities of Neural Ordinary Differential Equations (NODEs) and the ability of Symbolic Regression (SR) to recover the underlying equations. Our study yields three key insights. First, we demonstrate that NODEs can extrapolate effectively to new boundary conditions, provided the resulting trajectories share dynamic similarity with the training data. Second, SR successfully recovers the equations from noisy ground-truth data, though its performance is contingent on the correct selection of input variables. Finally, we find that SR recovers two out of the three governing equations, along with a good approximation for the third, when using data generated by a NODE trained on just 10% of the full simulation. While this last finding highlights an area for future work, our results suggest that using NODEs to enrich limited data and enable symbolic regression to infer physical laws represents a promising new approach for scientific discovery.

---

## 论文详细总结（自动生成）

这是一份关于论文《An Empirical Investigation of Neural ODEs and Symbolic Regression for Dynamical Systems》的结构化深入总结：

### 1. 论文的核心问题与整体含义
本文探讨了在科学发现中如何从有限且带噪声的实验数据中推断复杂系统的动力学方程。研究的核心动机在于结合两种技术的优势：**神经常微分方程（NODEs）** 擅长处理连续时间动力学建模但属于“黑盒”模型，缺乏可解释性；而**符号回归（SR）** 能够发现显式的物理定律，但通常需要大量高质量数据。论文提出并验证了一个协同工作流：利用 NODE 学习有限数据的底层动力学，并将其作为数据增强工具，为 SR 生成更丰富、去噪后的数据，从而在数据稀缺的情况下恢复系统的控制方程。

### 2. 论文提出的方法论
核心思想是构建一个 **“NODE 增强的符号回归”** 流。
*   **数据生成与预处理**：使用数值积分器（SciPy）生成两个动力系统（倒立摆和生物模型）的合成轨迹，并加入 5% 的均匀分布噪声。
*   **NODE 建模**：采用基于 JAX 的 Diffrax 库构建 NODE。模型使用多层感知器（MLP）近似导数函数 $f(x, t, \theta)$。
*   **数据增强（Pipeline）**：在仅 10% 的原始模拟数据上训练 NODE。训练完成后，利用 NODE 生成完整的、连续的时间序列数据。
*   **符号回归（SR）**：使用 PySR 库对 NODE 生成的数据进行处理。SR 的目标是寻找能够最小化均方误差（MSE）且复杂度适中的数学表达式。
*   **关键技术细节**：
    *   NODE 充当了**去噪过滤器**，因为它在拟合过程中倾向于学习平滑的动力学轨迹，而非噪声。
    *   SR 过程中引入了梯度计算（NumPy gradient）来获取状态变量的时间导数作为目标值。

### 3. 实验设计
研究设计了两个具有挑战性的阻尼振荡场景：
*   **场景 A：小车倒立摆（Cart-pole）**：经典的非线性物理系统，用于测试 NODE 在不同初始条件下的插值与外推能力。
*   **场景 B：生物模型（Bio-model）**：模拟大肠杆菌对营养环境变化的适应过程。该系统包含三个状态变量（氨基酸比例、核糖体质量分数、基因调节函数），具有高度非线性和复杂的有理项。
*   **Benchmark 与对比方法**：
    *   **基准 1**：直接在无噪声的 Ground Truth（GT）数据上运行 SR。
    *   **基准 2**：在带 5% 噪声的 GT 数据上运行 SR。
    *   **实验组**：在由 NODE（基于 10% 噪声数据训练）生成的增强数据上运行 SR。
    *   **消融实验**：测试了不同的采样频率（每小时 5 到 100 个数据点）对 NODE 长期预测性能的影响。

### 4. 资源与算力
论文**未明确说明**具体的硬件型号（如 GPU 类型）或总算力消耗。
*   **软件框架**：使用了 JAX, Diffrax, PySR, Optax 和 SciPy。
*   **训练规模**：NODE 模型训练了 100,000 到 150,000 次迭代。考虑到 MLP 规模较小（2 层，每层 20 个神经元），预计在单张商用 GPU 或高性能 CPU 上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对倒立摆系统，通过热图分析了大量初始条件下的 MSE。
    *   针对生物模型，进行了 10 次独立运行以计算平均 MSE 和标准差。
    *   测试了 19 种不同的营养转换（Shift）场景。
*   **充分性评价**：实验设计较为客观，涵盖了物理和生物两个跨学科领域。通过改变采样频率和初始条件，验证了模型的鲁棒性。然而，SR 的测试主要集中在单一转换场景，若能增加更多工况下的 SR 稳定性测试会更具说服力。

### 6. 论文的主要结论与发现
*   **NODE 的外推逻辑**：NODE 的外推能力并不取决于初始条件的接近程度，而是取决于新轨迹与训练轨迹在**相空间（Phase Space）中的动力学相似性**。
*   **数据效率**：即使采样频率极低（如每小时仅 6 个点），NODE 仍能保持稳定的长期预测性能（8 小时 MSE 保持一致）。
*   **SR 的敏感性**：SR 的成功高度依赖于输入变量的选择。例如，在生物模型中，如果不显式提供中间变量 $\lambda$，SR 很难发现复杂的有理项。
*   **NODE 作为去噪器**：在带 5% 噪声的原始数据上 SR 失败了，但在 NODE 生成的数据上，SR 成功恢复了 3 个方程中的 2 个。这证明 NODE 能有效提取信号并抑制噪声。

### 7. 优点
*   **跨学科验证**：同时在物理和生物系统上验证，增强了结论的普适性。
*   **创新的 Pipeline**：提出了一种在数据稀缺和高噪声环境下，利用深度学习辅助符号发现的实用流程。
*   **深入的洞察**：明确了 NODE 外推能力的本质是“动力学相似性”，而非简单的空间距离。

### 8. 不足与局限
*   **变量依赖性**：SR 仍然高度依赖于人类对输入变量（如 $\lambda$）的预先筛选，自动化程度有待提高。
*   **复杂项丢失**：在生物模型中，SR 无法完全恢复包含微弱信号项的方程（如方程 2 中的 $-\lambda \psi_A$ 项），而是将其合并到了常数项中。
*   **合成数据局限**：所有实验均基于合成数据，尚未在真实的、具有非平稳噪声或系统偏差的实验数据上进行验证。
*   **物理先验缺失**：SR 搜索过程中未引入单位匹配或物理约束，导致生成的方程可能在物理意义上不严谨。

（完）
