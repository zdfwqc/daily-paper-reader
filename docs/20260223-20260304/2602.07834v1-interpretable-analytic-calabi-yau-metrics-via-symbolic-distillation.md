---
title: Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation
title_zh: 通过符号蒸馏获得的可解释解析卡拉比-丘度规
authors: D Yang Eng
date: 2026-02-08
pdf: "https://arxiv.org/pdf/2602.07834v1"
tags: ["query:sr"]
score: 10.0
evidence: 符号回归用于物理学中神经近似的蒸馏
tldr: 本研究针对弦理论中难以计算的卡拉比-丘流形度规，提出了一种基于符号回归的蒸馏方法。通过将复杂的神经网络近似模型转化为简洁的解析公式，研究者仅用五个项就实现了与神经网络相当的精度（R²=0.9994），且参数量减少了3000倍。该公式不仅在不同模空间范围内保持稳定，还能准确复现体积积分和汤川耦合等物理观测值，为获取可解释的几何物理模型提供了新途径。
selection_source: fresh_fetch
motivation: 求解弦理论中至关重要的卡拉比-丘流形度规极其困难，且现有的神经网络近似方法缺乏可解释性。
method: 利用符号回归技术将预训练的神经网络度规模型蒸馏为由幂和与对称多项式组成的简洁解析表达式。
result: 得到的五项解析式在参数量大幅缩减的同时保持了极高精度，并成功复现了体积积分等关键物理量。
conclusion: 符号蒸馏证明了可以从黑盒模型中提取出紧凑且具有物理意义的解析模型，为复杂几何问题的解析求解提供了新思路。
---

## 摘要
卡拉比-丘流形对弦理论至关重要，但其度规的计算往往难以处理。本文展示了符号回归可以将神经网络近似蒸馏为简单且可解释的公式。我们的五项表达式在参数量减少 3000 倍的情况下，达到了与神经网络相当的精度（$R^2 = 0.9994$）。多种子验证确认了几何约束选择了关键特征（特别是幂和与对称多项式），同时允许结构上的多样性。在所研究的模空间范围（$ψ \in [0, 0.8]$）内，该函数形式保持不变且系数平滑变化；我们将这些趋势解释为在局部训练的教师模型精度范围内（当 $ψ \neq 0$ 时，$σ \approx 8-9\%$）的经验假设。该公式能够重现物理可观测物理量——体积积分和汤川耦合——从而验证了符号蒸馏可以为此前仅能由黑盒网络计算的物理量恢复出紧凑且可解释的模型。

## Abstract
Calabi--Yau manifolds are essential for string theory but require computing intractable metrics. Here we show that symbolic regression can distill neural approximations into simple, interpretable formulas. Our five-term expression matches neural accuracy ($R^2 = 0.9994$) with 3,000-fold fewer parameters. Multi-seed validation confirms that geometric constraints select essential features, specifically power sums and symmetric polynomials, while permitting structural diversity. The functional form can be maintained across the studied moduli range ($ψ\in [0, 0.8]$) with coefficients varying smoothly; we interpret these trends as empirical hypotheses within the accuracy regime of the locally-trained teachers ($σ\approx 8-9\%$ at $ψ\neq 0$). The formula reproduces physical observables -- volume integrals and Yukawa couplings -- validating that symbolic distillation recovers compact, interpretable models for quantities previously accessible only to black-box networks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种通过符号回归（Symbolic Regression）从神经网络模型中蒸馏出解析公式的方法，旨在解决弦理论中卡拉比-丘（Calabi-Yau, CY）流形度规计算的复杂性与不可解释性问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在弦理论中，计算卡拉比-丘三维倍体的里奇平坦（Ricci-flat）度规对于推导粒子物理观测值（如汤川耦合）至关重要。然而，求解定义度规的 Monge-Ampère 方程在计算上极其困难。
*   **核心矛盾**：传统的数值方法（如 Donaldson 算法）耗时极长；近年来的神经网络（NN）代理模型虽然计算速度快，但属于“黑盒”，缺乏物理直觉和解析表达。
*   **核心问题**：能否找到一种既能保持神经网络精度，又具有紧凑解析形式且可解释的度规表达式？

### 2. 方法论
*   **核心思想（符号蒸馏）**：采用“教师-学生”架构。首先训练一个高精度的神经网络或代数度规作为“教师”，然后利用符号回归（PySR）作为“学生”去拟合教师模型的输出，从而提取出简洁的数学公式。
*   **关键技术细节**：
    *   **回归目标**：拟合代数度规与 Fubini-Study 度规行列式的比值对数：$y = \log(\det g_{alg} / \det g_{FS})$。
    *   **特征工程**：利用规范不变的几何不变量作为输入特征，包括二阶幂和 $p_2 = \sum |z_i|^4$ 和三阶初等对称多项式 $\sigma_3$。
    *   **算法流程**：
        1. 在不同的模参数 $\psi$ 下训练 Donaldson 教师模型（$k=10$ 阶多项式）。
        2. 生成 $10^5$ 个采样点的数据集。
        3. 运行 PySR 进行符号搜索，通过 Pareto 准则平衡复杂度和准确性。
        4. 验证公式在物理观测值（体积积分、汤川耦合）上的表现。

### 3. 实验设计
*   **数据集/场景**：研究对象为 Dwork 家族的五次超曲面（Quintic Hypersurfaces），模空间范围设定为 $\psi \in [0, 0.8]$。
*   **Benchmark（基准）**：
    *   **Donaldson H-matrix**：传统的代数度规方法（参数量 875）。
    *   **神经网络代理模型**：如 Ashmore (2021) 提出的模型（参数量约 15,000）。
*   **对比维度**：拟合优度（$R^2$）、均方根误差（RMSE）、里奇平坦度指标（$\sigma$）、参数压缩率、推理速度。

### 4. 资源与算力
*   **硬件使用**：论文明确提到符号回归搜索是在 **NVIDIA A100 GPU**（Google Colab Pro 环境）上运行的。
*   **训练时长**：PySR 运行了 200 次迭代；Donaldson 教师模型在每个 $\psi$ 点训练约 15 次迭代。具体的总耗时未详细列出，但强调了符号公式在推理阶段比 H-matrix 快 10,000 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 **10 个随机种子** 进行独立运行，以验证特征发现的稳健性。
    *   进行了 **特征消融实验**，证明了 $\sigma_3$ 对捕捉各向异性的必要性。
    *   跨模空间验证：在 $\psi = 0.0, 0.2, \dots, 0.8$ 多个点独立训练并观察系数演化。
    *   跨阶数验证：对比了 $k=6, 8, 10$ 不同精度的教师模型。
*   **充分性评价**：实验设计较为充分，通过多种子验证和物理基准测试（体积和耦合常数）确保了结果不是偶然的拟合，而是捕捉到了底层的几何结构。

### 6. 主要结论与发现
*   **极高的压缩比**：发现了一个仅含 **5 个项** 的解析公式，其精度（$R^2 = 0.9994$）与拥有 15,000 个参数的神经网络相当，参数量减少了 **3,000 倍**。
*   **系数演化规律**：在模空间内，函数形式保持稳定，但系数随 $\psi$ 平滑变化。观察到“分层调制”现象：奇异项发生符号反转，而对称项随 $\psi$ 增大而单调增强。
*   **物理有效性**：该公式计算的体积积分与文献值的误差在 5.5% 以内，汤川耦合计算达到精确匹配（误差 < 0.001%），证明了其在唯象学研究中的实用价值。

### 7. 优点（亮点）
*   **可解释性**：将黑盒模型转化为可理解的几何不变量组合，揭示了 Monge-Ampère 方程解的潜在结构。
*   **因式分解原理**：提出了“PDE 约束控制函数形式，模参数控制系数”的设计原则，这为其他物理领域的 AI 应用提供了范式。
*   **计算效率**：极大地提升了度规评估速度，使得大规模扫描模空间以寻找符合物理要求的真空点成为可能。

### 8. 不足与局限
*   **教师模型限制**：在 $\psi \neq 0$ 的变形区域，教师模型本身的里奇误差较高（8-9%），这限制了符号公式最终能达到的绝对几何精度。
*   **覆盖范围**：研究仅限于 Dwork 五次超曲面这一特定家族，尚未验证其在更高维模空间或其他流形家族（如双三次元）上的普适性。
*   **奇异性处理**：虽然发现了奇异项（$1/p_2^2$），但其数学上的严格渐近形式仍需进一步的理论推导支持，目前更多是经验性的发现。
*   **度规完整性**：目前主要针对度规行列式比值进行建模，若要计算完整的度规张量，可能需要更复杂的符号表达式。

（完）
