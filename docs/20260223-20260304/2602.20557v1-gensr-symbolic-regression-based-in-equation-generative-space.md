---
title: "GENSR: Symbolic Regression Based in Equation Generative Space"
title_zh: GenSR：基于方程生成空间的符号回归
authors: "Qian Li, Yuxiao Hu, Juncheng Liu, Yuntian Chen"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20557v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于生成潜空间的符号回归框架
tldr: 针对传统符号回归在离散空间搜索导致反馈噪声大的问题，本文提出 GenSR 框架。它通过预训练 CVAE 构建具有数值平滑性的生成潜空间，将搜索过程分为地图构建、粗略定位与精细搜索三个阶段。该方法利用 CMA-ES 在连续潜空间中高效寻优，在保证预测精度和表达式简洁性的同时，显著提升了计算效率和抗噪能力。
selection_source: fresh_fetch
motivation: 传统符号回归在离散空间搜索，方程结构的微小改变往往导致数值行为剧变，使得误差反馈难以指导搜索。
method: 采用双分支 CVAE 预训练生成潜空间以实现方程的平滑重参数化，并结合 CMA-ES 算法进行区域定位与精细搜索。
result: 实验证明该方法在预测准确性、公式简洁度及计算效率上均优于现有方法，且在噪声环境下表现稳健。
conclusion: GenSR 从贝叶斯视角重新定义了符号回归任务，为在连续生成空间中解决离散方程搜索问题提供了理论支撑和有效方案。
---

## 摘要
符号回归 (SR) 旨在揭示观测数据背后隐藏的方程。然而，大多数方法在离散的方程空间中进行搜索，其中方程的结构修改很少与其数值行为保持一致，导致拟合误差反馈过于嘈杂，难以引导探索。为了应对这一挑战，我们提出了 GenSR，这是一个基于生成潜空间的符号回归框架，遵循“地图构建 -> 粗略定位 -> 精细搜索”的范式。具体而言，GenSR 首先预训练一个双分支条件变分自编码器 (CVAE)，将符号方程重新参数化为一个具有符号连续性和局部数值平滑性的生成潜空间。该空间可以被视为方程空间中结构良好的“地图”，为搜索提供方向性信号。在推理阶段，CVAE 将输入数据粗略定位到潜空间中有潜力的区域。随后，改进的 CMA-ES 利用平滑的潜梯度对候选区域进行精细化搜索。从贝叶斯角度来看，GenSR 将符号回归任务重构为最大化条件分布 $p(\mathrm{Equ.} \mid \mathrm{Num.})$，而 CVAE 的训练通过证据下界 (ELBO) 实现了这一目标。这一新视角为 GenSR 的有效性提供了理论保证。大量实验表明，GenSR 在保持对噪声鲁棒性的同时，共同优化了预测准确性、表达式简洁性和计算效率。

## Abstract
Symbolic Regression (SR) tries to reveal the hidden equations behind observed data. However, most methods search within a discrete equation space, where the structural modifications of equations rarely align with their numerical behavior, leaving fitting error feedback too noisy to guide exploration. To address this challenge, we propose GenSR, a generative latent space-based SR framework following the `map construction -> coarse localization -> fine search'' paradigm. Specifically, GenSR first pretrains a dual-branch Conditional Variational Autoencoder (CVAE) to reparameterize symbolic equations into a generative latent space with symbolic continuity and local numerical smoothness. This space can be regarded as a well-structured `map'' of the equation space, providing directional signals for search. At inference, the CVAE coarsely localizes the input data to promising regions in the latent space. Then, a modified CMA-ES refines the candidate region, leveraging smooth latent gradients. From a Bayesian perspective, GenSR reframes the SR task as maximizing the conditional distribution $p(\mathrm{Equ.} \mid \mathrm{Num.})$, with CVAE training achieving this objective through the Evidence Lower Bound (ELBO). This new perspective provides a theoretical guarantee for the effectiveness of GenSR. Extensive experiments show that GenSR jointly optimizes predictive accuracy, expression simplicity, and computational efficiency, while remaining robust under noise.

---

## 论文详细总结（自动生成）

这是一份关于论文《GENSR: Symbolic Regression Based in Equation Generative Space》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
符号回归（SR）的核心挑战在于如何在庞大的方程空间中找到既能拟合数据又具有可解释性的数学表达式。
*   **研究动机**：传统方法（如遗传算法、MCTS）多在**离散方程空间**中搜索。这种空间的缺点是方程结构的微小改变（编辑距离小）往往导致数值行为的剧变，使得误差反馈信号充满噪声，无法有效引导搜索方向。
*   **核心目标**：将离散的方程空间重参数化为**连续的生成潜空间**。通过构建一个具有“符号连续性”和“局部数值平滑性”的“方程地图”，将离散的组合优化问题转化为连续空间的搜索问题，从而提高搜索效率和鲁棒性。

### 2. 方法论：核心思想与技术细节
GenSR 遵循“地图构建 → 粗略定位 → 精细搜索”的范式：
*   **双分支 CVAE 预训练（地图构建）**：
    *   **后验分支（Posterior Branch）**：输入数值样本和符号方程，学习方程结构的分布，确保潜空间具备符号连续性（相似结构在空间中靠近）。
    *   **先验分支（Prior Branch）**：仅输入数值样本，学习数值特征的分布。
    *   **联合优化**：通过最小化重构损失（保证能解码出有效方程）和 KL 散度（对齐先验与后验，实现数值平滑性），构建生成潜空间。
*   **贝叶斯视角**：论文将 SR 任务形式化为最大化条件分布 $p(\text{Equ.} \mid \text{Num.})$，证明了 CVAE 的训练本质上是在优化证据下界（ELBO）。
*   **推理与搜索流程**：
    1.  **粗略定位**：利用先验分支将输入数据映射到潜空间的高概率区域。
    2.  **精细搜索（改进的 CMA-ES）**：在潜空间中运行协方差矩阵自适应进化策略。为了提升效率，GenSR 引入了**对角协方差假设**和 **Top-k 维度更新**，仅优化最相关的潜维度。
    3.  **常数优化**：解码出方程结构后，使用 BFGS 算法对其中的常数进行精细拟合。

### 3. 实验设计
*   **数据集与场景**：
    *   **SRBench**：包含 119 个 Feynman 物理方程、14 个 Strogatz 非线性动力学挑战。
    *   **Black-box 任务**：57 个来自 PMLB 的真实/合成黑盒回归任务（目标函数未知）。
    *   **噪声环境**：在 Strogatz 数据集上测试了不同噪声水平（0.0 到 0.1）下的表现。
*   **对比方法（18 种基线）**：
    *   **启发式/遗传算法**：GPlearn, Operon, GP-GOMEA, AFP 等。
    *   **深度学习/强化学习**：DSR, E2ESR, SNIP, TPSR, RAG-SR, MDLformer 等。
    *   **物理/概率方法**：AI Feynman 2.0, BSR。
*   **评估指标**：拟合优度 $R^2$、方程复杂度（Token 数量）、计算时间（秒）。

### 4. 资源与算力
*   **硬件**：使用了 2 张 **NVIDIA H800 GPU** (80GB 显存)。
*   **训练时长**：预训练阶段共 200 个 epoch，每个 epoch 耗时约 1 小时（总计约 200 小时）。
*   **模型规模**：模型包含约 1.62 亿（162.1M）个可训练参数。
*   **推理效率**：在 Feynman 数据集上平均搜索时间约为 192.3 秒。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的基准测试，涵盖了数百个不同的回归任务。
*   **消融实验**：非常充分。作者研究了潜空间维度（64 到 768）、CMA-ES 的种群大小、步长、Top-k 更新维度以及适应度权重对结果的影响。
*   **可视化分析**：通过 t-SNE 降维展示了潜空间的结构分离性和数值连续性，并进行了潜空间插值实验，验证了空间的生成能力。
*   **公平性**：使用了标准化的 SRBench 框架，并与当前最先进的（SOTA）预训练模型和启发式算法进行了直接对比，实验设计客观。

### 6. 主要结论与发现
*   **Pareto 最优**：GenSR 在准确性、方程简洁度和计算效率三个维度上均处于 Pareto 前沿的领先位置。
*   **抗噪性强**：在噪声环境下，$R^2$ 分数下降最慢，且保持了较低的方差，证明了连续潜空间对数据扰动的容忍度更高。
*   **生成空间优势**：相比于 SNIP 等判别式潜空间方法，GenSR 的生成空间几乎没有“死区”，插值生成的方程均符合语法且物理意义连贯。
*   **迁移潜力**：预训练的编码器在方程分类任务中也表现优异（准确率 0.849），说明其捕捉到了深层的函数家族特征。

### 7. 优点（亮点）
*   **范式创新**：提出了“地图构建 → 粗略定位 → 精细搜索”的新范式，解决了离散搜索中反馈信号缺失的问题。
*   **理论完备**：从贝叶斯推断和 ELBO 优化的角度为框架提供了坚实的数学支撑。
*   **平滑性设计**：成功在潜空间中平衡了符号结构（全局）和数值行为（局部）的连续性，使得连续优化算法（CMA-ES）能有效应用于符号回归。
*   **效率平衡**：通过改进 CMA-ES，在保证搜索精度的前提下，大幅降低了高维潜空间的搜索耗时。

### 8. 不足与局限
*   **预训练依赖**：模型性能高度依赖于预训练数据集的覆盖范围，对于完全超出预训练分布的极端复杂方程，可能难以重构。
*   **计算开销**：虽然推理较快，但预训练过程需要高性能 GPU（H800）和较长时间，门槛较高。
*   **常数优化瓶颈**：尽管使用了 BFGS，但在处理包含大量嵌套常数的极复杂方程时，常数优化仍可能陷入局部最优。
*   **应用限制**：目前主要针对常微分方程和代数方程，对于偏微分方程（PDE）的直接支持尚需进一步扩展。

（完）
