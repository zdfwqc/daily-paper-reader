---
title: An information-based model selection criterion for data-driven model discovery
title_zh: 一种用于数据驱动模型发现的基于信息的模型选择准则
authors: "Michael C Chung, Alen Zacharia, Juan Guan"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21393v1"
tags: ["query:sr"]
score: 9.0
evidence: 利用稀疏回归和信息准则从数据中提取可解释的符号模型
tldr: 本研究针对数据驱动模型发现中难以平衡拟合精度与模型稀疏性的挑战，提出了一种新型信息准则SLIC。该准则配合自适应候选模型生成算法，能够自动从数据中识别出最优的物理模型。实验证明，SLIC在处理非线性微分方程及流体力学等领域的实验数据时，其准确性和鲁棒性显著优于传统方法，为科学发现提供了强有力的自动化工具。
selection_source: fresh_fetch
motivation: 传统数据驱动模型发现方法在平衡模型复杂度和拟合精度时过度依赖人工调参，且容易出现过拟合问题。
method: 开发了一种自适应生成候选模型的稀疏回归算法，并引入样本长度缩放对数信息准则（SLIC）来自动识别最佳模型。
result: 在非线性常微分和偏微分方程以及流体力学、纳米技术实验数据中，SLIC提取正确模型的表现均优于其他流行信息准则。
conclusion: SLIC为从复杂实验数据中自动发现具有可解释性和预测能力的物理模型提供了一种稳健且高效的准则。
---

## 摘要
数据驱动的模型发现 (DDMD) 算法是从数据中提取可解释符号模型的强大工具。然而，识别出能够最佳平衡拟合优度与稀疏性的模型通常是一个费力的过程，需要用户进行微调，且容易出现过拟合，其结果可能会因模型初始化和特定训练过程的不同而产生显著差异。在本文中，我们提出了一种稀疏回归算法，该算法能够自动且自适应地生成候选模型，并使用一种新型的样本长度缩放对数信息准则 (SLIC) 从这些候选模型中识别出最佳模型。我们证明了在从多个非线性常微分方程和偏微分方程的数据中提取正确模型方面，SLIC 的表现显著优于其他流行的信息准则。随后，我们展示了 SLIC 从流体动力学和纳米技术的实验数据集中发现可解释模型的能力，这些模型能够产生新的可测试预测。

## Abstract
Data-driven model discovery (DDMD) algorithms are powerful tools for extracting interpretable symbolic models from data. However, identifying the model that best balances goodness-of-fit and sparsity is often a laborious process requiring user fine-tuning, is prone to overfitting, and results may significantly vary depending on model initialization and specific training procedure. Here, we present a sparse regression algorithm that automatically and adaptively generates candidate models, and uses a novel sample-length-scaling logarithmic information criterion (SLIC) to identify the best model from these candidates. We demonstrate that SLIC greatly outperforms other popular information criteria in extracting the correct model from the data of several nonlinear ordinary and partial differential equations. Then, we demonstrate SLIC's ability to discover interpretable models from experimental datasets in fluid dynamics and nanotechnology that generate new testable predictions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SLIC**（Sample-Length-scaling logarithmic Information Criterion，样本长度缩放对数信息准则）的新型模型选择方法，旨在解决数据驱动模型发现（DDMD）中的过拟合与参数调优难题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在从数据中提取物理方程（如 SINDy 算法）时，核心挑战在于如何平衡**拟合精度**与**模型稀疏性**。
*   **现有痛点**：
    *   传统的模型选择准则（如 AIC、BIC）在数据量增大时，拟合项的权重会远超惩罚项，导致算法倾向于包含无意义的杂项（过拟合）。
    *   现有的稀疏回归方法通常需要人工精细调节阈值参数（$\lambda$），难以实现完全自动化。
*   **核心目标**：开发一种无需人工干预、对数据量具有缩放不变性、且能稳健识别真实物理规律的准则。

### 2. 方法论
*   **核心思想**：基于对称性原理和缩放一致性，设计一个让拟合误差和模型复杂度在数学形式上地位平等的评分函数。
*   **关键公式**：
    $$SLIC(\hat{\epsilon}, k) = n \log(\hat{\epsilon} \cdot k)$$
    其中，$n$ 是样本数量，$\hat{\epsilon}$ 是均方误差（MSE），$k$ 是模型参数数量（复杂度）。
*   **技术细节**：
    *   **SLIC 准则**：与 AIC/BIC 不同，SLIC 的惩罚项随 $n$ 线性缩放，确保了在大数据集下的稀疏性约束依然有效。
    *   **自适应剪枝算法**：算法自动从初始最小二乘估计中提取系数大小作为候选阈值，通过迭代循环生成一系列候选模型，并利用 SLIC 评分选出最优者。
    *   **弱形式（Weak Form）处理**：利用 Galerkin 投影将微分方程转化为积分形式，从而绕过对噪声数据直接求导的难题。
    *   **去噪预处理**：集成了快速 Whittaker-Henderson 滤波器进行数据平滑。

### 3. 实验设计
*   **数据集与场景**：
    *   **常微分方程 (ODEs)**：Lorenz 系统、Rössler 吸引子、Lotka-Volterra 模型、Brusselator、Van der Pol 振子、非线性摆。
    *   **偏微分方程 (PDEs)**：Burger's 方程、KdV 方程、Kuramoto-Sivashinsky (KS) 方程、非线性薛定谔方程 (NLS)、Sine-Gordon 方程。
    *   **真实实验数据**：流体晃动坦克（Sloshing Tank）的质心动力学、RNA-脂质体纳米颗粒的聚合动力学。
*   **Benchmark（对比方法）**：AIC, AICc, BIC, HQIC, KIC, Bridge Criterion (BC)。
*   **评价指标**：项识别准确率（Accuracy）、误报率（FPR）、系数误差（SMAE）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU/CPU 型号或具体的训练时长。
*   **实现工具**：算法基于 **Julia** 语言开发，利用了 `DifferentialEquations.jl` 进行仿真，`DiffEqFlux.jl` 进行参数估计，以及 `Optimization.jl` 进行非线性优化。
*   **计算开销**：由于采用的是稀疏回归而非深度学习，其计算成本相对较低，主要集中在矩阵伪逆运算和迭代剪枝过程。

### 5. 实验数量与充分性
*   **实验规模**：针对每个系统，在不同的噪声水平（最高达 40%）下进行了 **25 次独立随机实验**并取平均值。
*   **充分性评价**：实验设计非常全面。不仅覆盖了从简单到复杂的多种数学模型，还进行了关于采样频率、轨迹长度、参数变化以及数据下采样的消融实验。
*   **客观性**：通过与 6 种主流信息准则在相同框架下对比，证明了 SLIC 在高噪声和大数据量下的显著优势。

### 6. 主要结论与发现
*   **性能优越**：SLIC 在所有测试的 ODE 和 PDE 系统中，识别正确项的准确率均大幅领先，且误报率（FPR）极低。
*   **解决过拟合**：SLIC 成功解决了传统准则随数据量增加而倾向于过拟合的“悖论”。
*   **真实预测力**：
    *   在流体实验中，仅用无迫振动数据训练的模型，成功预测了受迫振动下的滞后现象和不稳定区间。
    *   在纳米技术实验中，SLIC 识别出的“常数核”模型揭示了扩散主导的聚合机制，并准确预测了稀释后的颗粒生长速率。

### 7. 优点
*   **理论优雅**：SLIC 的推导基于简单的对称性假设，却在实践中与最小化模型方差（Bias-Variance Tradeoff）相契合。
*   **高度自动化**：消除了 DDMD 中最麻烦的超参数调优步骤。
*   **鲁棒性强**：对高噪声数据和不同的采样策略表现出极强的适应性。

### 8. 不足与局限
*   **基函数库依赖**：作为一种符号回归方法，它仍然依赖于用户预定义的候选函数库。如果真实的物理项不在库中，算法无法凭空创造。
*   **噪声分布假设**：SLIC 的推导基于高斯噪声假设，虽然实验显示其鲁棒性好，但在极端非高斯噪声环境下的表现尚待验证。
*   **计算扩展性**：对于具有成千上万个候选项的超大规模搜索空间，迭代剪枝的效率可能会受到挑战。

（完）
