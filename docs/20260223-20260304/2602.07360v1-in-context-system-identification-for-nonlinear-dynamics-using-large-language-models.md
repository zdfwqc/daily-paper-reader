---
title: In-Context System Identification for Nonlinear Dynamics Using Large Language Models
title_zh: 基于大语言模型的非线性动力学上下文系统辨识
authors: Linyu Lin
date: 2026-02-07
pdf: "https://arxiv.org/pdf/2602.07360v1"
tags: ["query:sr"]
score: 10.0
evidence: 用于符号方程优化的LLM辅助SINDy流水线
tldr: 本研究针对非线性动力学系统识别中SINDy方法依赖专家调优候选库的局限，提出了一种LLM辅助的SINDy流水线。该方法利用大语言模型的上下文学习能力，通过迭代循环不断优化候选方程。在每一轮迭代中，LLM根据当前误差和约束建议新的方程结构，并结合仿真误差进行评估。实验涵盖了63个动力系统数据集及复杂的核反应堆模型，结果显示该方法在符号恢复的结构相似性和预测精度上均显著优于传统SINDy，为自动化发现可解释的物理规律提供了新途径。
selection_source: fresh_fetch
motivation: 传统的SINDy方法在发现非线性动力学方程时高度依赖专家对候选库的预设和调优，难以处理复杂的未知动力学系统。
method: 提出一种将LLM引入循环的SINDy流水线，通过上下文学习迭代优化方程结构，并利用仿真误差和结构相似性作为反馈进行精炼。
result: 在63个动力系统数据集及核反应堆模型上的测试表明，该方法在符号恢复准确性和测试RMSE方面均优于基准SINDy。
conclusion: 研究证明了LLM能有效引导SINDy在方程空间中的搜索，将数据驱动的误差反馈与符号推理相结合，实现了更准确且具解释性的系统识别。
---

## 摘要
非线性动力学稀疏辨识（SINDy）是从数据中发现简洁控制方程的强大方法，但通常需要专家对候选库进行调优。我们提出了一种 LLM 辅助的 SINDy 流水线，通过上下文学习（in-context learning）将大语言模型（LLM）引入循环，迭代地优化候选方程。该流水线首先使用自适应库拟合基准 SINDy 模型，随后进入 LLM 引导的优化循环。在每次迭代中，当前的最佳方程、误差指标和领域特定约束被汇总到 LLM 的提示词中，由其建议新的方程结构。这些候选方程根据定义的符号形式进行解析，并在训练和测试数据上进行评估。该流水线将基于模拟的误差作为主要指标，同时评估与真实值（ground truth）的结构相似性，包括函数形式、关键项、耦合关系和定性行为的匹配。如果测试误差低于阈值（NRMSE < 0.1）或达到最大迭代次数（10 次），迭代停止准则将提前结束优化。最后，选择最佳模型，并在 63 个动力系统数据集（ODEBench）以及用于沸腾核反应堆的 March-Leuba 模型上评估该 LLM 辅助的 SINDy。结果与经典 SINDy 进行了对比，表明在复杂动力学情况下，LLM 循环一致地提高了符号恢复能力，与基准 SINDy 相比，具有更高的方程结构相似性和更低的测试均方根误差（RMSE）。这项工作证明了 LLM 可以有效地引导 SINDy 在方程空间中的搜索，将数据驱动的误差反馈与受领域启发的符号推理相结合，从而发现不仅准确而且在结构上具有可解释性的控制方程。

## Abstract
Sparse Identification of Nonlinear Dynamics (SINDy) is a powerful method for discovering parsimonious governing equations from data, but it often requires expert tuning of candidate libraries. We propose an LLM-aided SINDy pipeline that iteratively refines candidate equations using a large language model (LLM) in the loop through in-context learning. The pipeline begins with a baseline SINDy model fit using an adaptive library and then enters a LLM-guided refinement cycle. At each iteration, the current best equations, error metrics, and domain-specific constraints are summarized in a prompt to the LLM, which suggests new equation structures. These candidate equations are parsed against a defined symbolic form and evaluated on training and test data. The pipeline uses simulation-based error as a primary metric, but also assesses structural similarity to ground truth, including matching functional forms, key terms, couplings, qualitative behavior. An iterative stopping criterion ends refinement early if test error falls below a threshold (NRMSE < 0.1) or if a maximum of 10 iterations is reached. Finally, the best model is selected, and we evaluate this LLM-aided SINDy on 63 dynamical system datasets (ODEBench) and march leuba model for boiling nuclear reactor. The results are compared against classical SINDy and show the LLM-loop consistently improves symbolic recovery with higher equation similarity to ground truth and lower test RMSE than baseline SINDy for cases with complex dynamics. This work demonstrates that an LLM can effectively guide SINDy's search through equation space, integrating data-driven error feedback with domain-inspired symbolic reasoning to discover governing equations that are not only accurate but also structurally interpretable.

---

## 论文详细总结（自动生成）

这是一份关于论文《In-Context System Identification for Nonlinear Dynamics Using Large Language Models》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
*   **研究背景**：从观测数据中发现控制方程（如常微分方程 ODE）是动力系统建模的核心。**SINDy**（非线性动力学稀疏辨识）是该领域的代表性方法，它通过在预设的候选函数库中进行稀疏回归来提取方程。
*   **核心问题**：传统的 SINDy 高度依赖于人工设计的候选库。如果真实的物理项不在库中，模型就会失效；此外，阈值选择和库的调整通常需要专家经验。
*   **整体含义**：本文提出了一种将大语言模型（LLM）引入循环的自动化流水线，利用 LLM 的科学知识和推理能力来动态建议、精炼候选方程，从而解决 SINDy 在处理复杂未知动力学时的“库错配”问题。

### 2. 论文提出的方法论
该方法构建了一个**LLM-in-the-loop**的迭代优化框架，主要包含以下步骤：
*   **基准拟合与特征提取**：首先运行标准 SINDy 获得基准模型，并计算状态变量的统计特征（如量程、是否振荡、是否饱和等），将其转化为自然语言描述。
*   **提示词工程（Prompt Construction）**：将当前方程、误差指标（NRMSE）、数据特征、领域先验（如变量含义）以及失败的尝试记录汇总成 Prompt 发送给 LLM。
*   **受限候选生成**：LLM 充当“假设生成器”，建议新的函数形式。为了保证可解释性和可计算性，要求生成的方程必须符合特定的符号语法，且在参数上保持线性（Linear-in-parameters）。
*   **验证与拟合循环**：
    1.  **语法解析**：自动检查 LLM 输出是否符合数学规范。
    2.  **稀疏回归**：使用 LLM 建议的新库重新拟合系数。
    3.  **仿真评估**：通过数值积分进行前向模拟（Rollout），计算测试集上的轨迹误差，这是评估模型好坏的核心指标。
*   **多目标评分与停止**：使用结合了预测精度、符号复杂度和结构先验惩罚的综合评分函数 $J$。当 $NRMSE < 0.1$ 或达到 10 次迭代时停止。

### 3. 实验设计
*   **数据集**：
    *   **ODEBench**：包含 63 个不同的 ODE 系统（涵盖 1D 到 4D），包括线性振荡器、捕食者-猎物模型、反应动力学及混沌系统（如 Lorenz 吸引子）。
    *   **March-Leuba 模型**：一个复杂的 7 维核反应堆（沸腾水反应堆 BWR）稳定性模型，具有强耦合和非线性反馈特性。
*   **Benchmark（基准）**：经典的 **SINDy** 方法（使用固定且广泛的候选库，如多项式和三角函数）。
*   **对比指标**：预测精度（$R^2$、NRMSE）和结构恢复质量（是否找回了真实的物理项）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/推理的总时长。
*   **实现细节**：由于核心逻辑是调用 LLM API 并结合数值积分器（ODE Solver），其主要的计算开销在于 LLM 的推理调用和多次前向仿真评估，而非传统的深度学习模型训练。

### 5. 实验数量与充分性
*   **实验规模**：在 63 个标准基准任务上进行了测试，这在符号回归领域属于较大规模且具有代表性的评估。
*   **充分性**：实验涵盖了从简单线性到复杂混沌、从低维到高维的多种动力学场景，并加入了一个真实的核工程案例，证明了方法的通用性。
*   **客观性**：通过与固定库的 SINDy 进行直接对比，并使用自动化指标（NRMSE）和定性结构分级，评估过程较为客观。

### 6. 论文的主要结论与发现
*   **精度提升**：在 63 个系统中，LLM 辅助方法在 48 个系统中达到了停止准则（NRMSE < 0.1），而基准 SINDy 仅为 30 个。
*   **误差降低**：中位数误差从基准 SINDy 的 62% 大幅下降至 **3.4%**。
*   **结构恢复**：LLM 能够识别出基准库中缺失的项（如指数项、复杂的双线性耦合），成功恢复真实物理结构的比例从 26/63 提升至 **36/63**。
*   **稳定性**：基准 SINDy 在 5 个案例中出现仿真发散（无限误差），而 LLM 辅助流水线通过迭代修正，在所有案例中都找到了稳定的模型。

### 7. 优点（亮点）
*   **自动化专家直觉**：成功将建模专家“观察误差 -> 假设新项 -> 验证修改”的思考过程算法化。
*   **打破固定库限制**：利用 LLM 的上下文学习能力，动态扩展函数空间，解决了传统 SINDy 最核心的痛点。
*   **闭环反馈**：引入了仿真轨迹误差作为反馈信号，而不仅仅是导数拟合误差，这确保了发现的方程在长期预测中是稳定的。
*   **可解释性**：保留了 SINDy 的稀疏符号形式，生成的模型对人类科学家完全透明。

### 8. 不足与局限
*   **LLM 偏差风险**：LLM 可能会倾向于它在训练数据中见过的常见方程形式，对于完全新颖、违背直觉的物理规律可能存在搜索偏见。
*   **计算成本**：多次调用 LLM 和进行数值仿真迭代比单次 SINDy 拟合耗时更长。
*   **复杂组合爆炸**：对于极高维系统或极其复杂的非线性组合（如文中提到的 `cot(y)cos(x)`），如果 LLM 不能一次性给出正确的组合原语，搜索空间仍然巨大。
*   **噪声敏感性**：虽然论文在无噪声数据上表现优异，但在高噪声现实数据下的鲁棒性仍需进一步验证。

（完）
