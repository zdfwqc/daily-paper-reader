---
title: Knowledge-Informed Kernel State Reconstruction for Interpretable Dynamical System Discovery
title_zh: 知识引导的核状态重构用于可解释动力系统发现
authors: "Luca Muscarnera, Silas Ruhrberg Estévez, Samuel Holt, Evgeny Saveliev, Mihaela van der Schaar"
date: 2026-01-29
pdf: "https://arxiv.org/pdf/2601.22328v1"
tags: ["query:sr"]
score: 10.0
evidence: 从噪声数据中恢复控制方程的符号发现框架
tldr: 针对从噪声和部分观测数据中发现动力系统控制方程的难题，本文提出MAAT框架。该方法利用知识感知的核状态重构技术，在再生核希尔伯特空间中建模，并将非负性、守恒律等物理先验直接融入重构目标。MAAT能处理异构采样数据，生成物理一致的状态估计及其解析导数，为符号回归提供了可靠接口。在12个基准测试中，该方法显著降低了状态和导数的估计误差，提升了系统发现的准确性。
selection_source: fresh_fetch
motivation: 现有的动力系统发现方法在面对噪声、部分观测或异构采样数据时，往往难以兼顾物理可解释性与重构精度。
method: 提出MAAT框架，通过在核空间中整合结构化和语义先验（如守恒律），实现物理一致的状态重构与解析导数计算。
result: 在12个科学基准测试和多种噪声环境下，MAAT在轨迹和导数估计的均方误差上均显著优于现有强基准方法。
conclusion: 该研究证明了将领域知识融入核重构过程能有效弥合碎片化传感器数据与符号回归之间的鸿沟，增强了动力系统发现的鲁棒性。
---

## 摘要
从数据中恢复控制方程是科学发现的核心，然而现有方法在噪声、部分观测下往往失效，或者依赖于掩盖机制的黑盒潜动力学。我们提出了 MAAT（Model Aware Approximation of Trajectories，模型感知轨迹近似），这是一个基于知识引导的核状态重构的符号发现框架。MAAT 在再生核希尔伯特空间中进行状态重构，并将结构和语义先验（如非负性、守恒律和特定领域的观测模型）直接纳入重构目标，同时能够处理异构采样和测量粒度。这产生了具有解析时间导数的平滑且物理一致的状态估计，为碎片化的传感器数据与符号回归之间提供了一个原则性的接口。在十二个不同的科学基准测试和多种噪声环境下，相比于强基线模型，MAAT 显著降低了下游符号回归所使用的轨迹和导数的状态估计均方误差（MSE）。

## Abstract
Recovering governing equations from data is central to scientific discovery, yet existing methods often break down under noisy, partial observations, or rely on black-box latent dynamics that obscure mechanism. We introduce MAAT (Model Aware Approximation of Trajectories), a framework for symbolic discovery built on knowledge-informed Kernel State Reconstruction. MAAT formulates state reconstruction in a reproducing kernel Hilbert space and directly incorporates structural and semantic priors such as non-negativity, conservation laws, and domain-specific observation models into the reconstruction objective, while accommodating heterogeneous sampling and measurement granularity. This yields smooth, physically consistent state estimates with analytic time derivatives, providing a principled interface between fragmented sensor data and symbolic regression. Across twelve diverse scientific benchmarks and multiple noise regimes, MAAT substantially reduces state-estimation MSE for trajectories and derivatives used by downstream symbolic regression relative to strong baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Knowledge-Informed Kernel State Reconstruction for Interpretable Dynamical System Discovery》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何从现实世界中**噪声大、采样稀疏、且仅能部分观测**的数据中，准确地恢复出描述动力系统的控制方程（常微分方程 ODEs）。
*   **研究背景**：
    *   传统的符号发现方法（如 SINDy）高度依赖于高质量的轨迹数据和准确的数值导数，但在实际应用（如临床、药理学）中，数据往往是碎片化的。
    *   现有的预处理方法（如样条平滑、高斯过程）通常对领域知识（如非负性、质量守恒）视而不见。
    *   黑盒模型（如 Neural ODEs）虽然能处理复杂数据，但其内部机制不可解释，无法满足科学发现对透明度的要求。
*   **整体含义**：论文提出了 **MAAT** 框架，旨在将状态重构视为一个“知识引导”的过程，通过在核空间中嵌入物理先验，为下游符号回归提供高质量、物理一致的输入。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将状态重构公式化为**再生核希尔伯特空间（RKHS）**中的风险最小化问题。MAAT 不仅仅是插值，而是在满足物理约束的前提下寻找最可能的轨迹。
*   **关键技术细节**：
    1.  **核状态重构 (KSR)**：使用高斯核（RBF）对每个状态变量建模。由于核函数是解析可微的，可以直接获得**解析时间导数**，避免了数值微分带来的噪声放大。
    2.  **复合损失函数**：
        *   **数据保真度**：同时拟合稀疏的直接测量（Anchor points）和高频的聚合观测（如总量测量）。
        *   **平滑度正则化**：通过 RKHS 范数控制轨迹的平滑性。
        *   **物理先验 (Rphys)**：直接在优化目标中加入非负性约束、导数幅度惩罚、质量守恒律或已知的动力学子结构。
    3.  **两阶段流程**：第一阶段通过 KSR 重构平滑轨迹和导数；第二阶段将重构数据输入符号回归引擎（如 SINDy 或 PySR）以发现显式方程。

### 3. 实验设计
*   **数据集/场景**：涵盖 12 个科学基准，包括：
    *   **药理学/生物学**：糖尿病模拟器（UVA/Padova）、肿瘤生长模型、中性粒细胞生命周期、EPO 药代动力学。
    *   **流行病学**：SEIR 和 SEIRH 模型、病毒动力学。
    *   **其他**：消费主义社会模型、Lotka-Volterra 等。
*   **噪声机制**：测试了三种噪声环境：各向同性高斯噪声、相关高斯噪声、重尾 Student-t 噪声。
*   **对比基准 (Benchmarks)**：
    *   **传统平滑**：三次样条（Cubic Spline）、Savitzky-Golay 滤波、TVRegDiff。
    *   **概率/滤波**：高斯过程（GP）、卡尔曼滤波（RTS 平滑）。
    *   **深度学习**：Neural ODEs、PINNs（物理信息神经网络）、Symbolic Neural ODEs。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **AMD Ryzen Threadripper PRO 3995WX (64核)** CPU 和 **NVIDIA RTX A4000 (16GB)** GPU 的工作站。
*   **计算效率**：
    *   MAAT 采用了基于 JAX 的 JIT 编译。
    *   虽然核方法通常涉及 $O(N^3)$ 复杂度，但 MAAT 通过一阶优化（Adam）避免了直接矩阵求逆，实际运行时间（约 44.7 秒）远快于 Neural ODEs（约 630 秒），略慢于简单的高斯过程。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 12 个数据集上进行了详尽测试，每个实验重复 10 次随机种子以确保统计显著性。
    *   针对不同噪声强度和类型进行了鲁棒性分析。
*   **消融实验**：专门验证了非负性约束、导数惩罚以及结构化先验对重构精度的贡献。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了重构误差（MSE），还对比了下游符号发现的准确性，并使用了统一的符号回归后端（PySR/SINDy）来保证对比的公平性。

### 6. 主要结论与发现
*   **精度提升**：在所有噪声环境下，MAAT 的状态估计 MSE 显著低于强基线模型，尤其是在处理异构观测（部分变量已知，部分变量仅知总和）时表现优异。
*   **导数质量**：MAAT 提供的解析导数比数值微分更稳定，这是成功发现符号方程的关键。
*   **先验的重要性**：在数据极度稀疏的情况下，引入物理先验（如非负性）能有效防止模型产生非物理的震荡，从而引导符号回归找到正确的函数形式。

### 7. 优点与亮点
*   **解析性**：利用核函数的特性获得闭式导数，从根本上解决了噪声放大问题。
*   **灵活性**：能够无缝融合不同频率、不同观测算子（线性映射）的异构数据。
*   **知识嵌入**：提供了一个原则性的框架，允许科学家将已知的生物/物理定律作为“软约束”注入模型，而不仅仅是依赖纯数据驱动。

### 8. 不足与局限
*   **先验依赖**：如果注入的物理先验是错误的（Misspecified），可能会导致重构结果偏离真实轨迹。
*   **扩展性限制**：尽管优化过程经过改进，但存储和计算核矩阵在处理超大规模时间序列（$N$ 极大）时仍可能面临 $O(N^2)$ 的内存压力。
*   **结构已知性**：目前假设状态变量的层级划分或部分结构是预先知道的，对于完全未知的复杂系统，如何自动发现子系统划分仍是挑战。

（完）
