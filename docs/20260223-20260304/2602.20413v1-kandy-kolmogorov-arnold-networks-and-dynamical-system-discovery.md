---
title: "KANDy: Kolmogorov-Arnold Networks and Dynamical System Discovery"
title_zh: KANDy：Kolmogorov-Arnold 网络与动力系统发现
authors: "Kevin Slote, Jeremie Fish, Erik Bollt"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20413v1"
tags: ["query:sr"]
score: 10.0
evidence: 通过用KAN取代稀疏回归来发现动力系统的控制方程
tldr: 本研究提出 KANDy，一种基于 Kolmogorov-Arnold 网络（KAN）的零深度、宽神经架构，旨在发现混沌及复杂动力系统的控制方程。KANDy 通过将 KAN 与方程发现相结合，替代了传统的稀疏回归方法，有效解决了稀疏性约束带来的局限性。该模型在离散、连续系统及混沌偏微分方程中表现出色，并能恢复 Hopf 纤维化的拓扑结构，为非线性动力系统提供了一种可解释且高效的数据驱动建模新途径。
selection_source: fresh_fetch
motivation: 旨在克服传统稀疏回归在动力系统方程发现中的局限性，并提升 KAN 在处理复杂混沌系统时的表现。
method: 引入 KANDy 架构，利用零深度且宽的 KAN 结构来显式学习并提取系统的控制方程。
result: 实验证明该模型能准确识别多种动力系统及偏微分方程的控制规律，并能保持吸引子的拓扑一致性。
conclusion: KANDy 是非线性动力系统建模中一种兼具解释力和鲁棒性的有效替代方案。
---

## 摘要
我们引入了动力学 Kolmogorov-Arnold 网络（KANDy），这是一种零深度、宽神经架构，能够发现混沌和复杂动力系统中的控制方程。在 Kolmogorov-Arnold 网络（KANs）的基础上，KANDy 通过用 KAN 替换稀疏回归来显式地学习控制方程。KANs 与稀疏回归的结合解决了 KANs 在应用于动力系统时方程发现的局限性，并克服了稀疏回归受限于稀疏性约束的情况。此外，我们展示了该模型在应用于 Hopf 纤维化（Hopf Fibration）时能够恢复拓扑结构，从而提高了与吸引子特性的相干性。我们将模型应用于离散和连续动力系统，以及混沌偏微分方程（PDEs）。这些结果使 KANDy 成为非线性动力系统数据驱动建模中一种具有解释性且有效的替代方案。

## Abstract
We introduce the Kolmogorov-Arnold Network for Dynamics (KANDy) as a zero-depth, wide neural architecture capable of discovering governing equations in chaotic and complex dynamical systems. Building on the foundation of Kolmogorov-Arnold Networks (KANs), KANDy explicitly learns governing equations by replacing sparse regression with a KAN. The synthesis of KANs and sparse regression addresses the limitations of equation discovery for KANs applied to dynamical systems and overcomes cases where sparse regression is hindered by sparsity constraints. Additionally, we show that our model, applied to the Hopf Fibration, recovers topological structure, thereby improving coherence with attractor properties. We apply our model to discrete and continuous dynamical systems, as well as to chaotic partial differential equations (PDEs). These results position KANDy as an interpretable and effective alternative for data-driven modeling of nonlinear dynamical systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《KANDy: Kolmogorov-Arnold Networks and Dynamical System Discovery》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在数据驱动的建模中，既能准确预测混沌动力系统的长期演化，又能显式地提取其背后的数学控制方程（Governing Equations）？
*   **研究动机**：
    *   **传统方法的局限**：稀疏回归（如 SINDy）依赖于方程的稀疏性假设，在处理非稀疏系统（如 Ikeda 映射、Holling 型项）时会失效；而深度学习模型（如 RNN、LSTM）虽预测力强，但缺乏可解释性。
    *   **KAN 的潜力与不足**：新兴的 Kolmogorov-Arnold 网络（KAN）具有良好的可解释性，但标准深度 KAN 在处理多变量非线性耦合（如 Lorenz 系统中的 $xy$ 项）时存在维度缩减困难，且难以直接提取全局闭式方程。
    *   **混沌挑战**：混沌系统对初始条件极度敏感，预测精度往往在 1-2 个李雅普诺夫时间（Lyapunov time）内迅速崩溃。

### 2. 方法论：KANDy 架构
*   **核心思想**：提出 **KANDy（Kolmogorov-Arnold Network for Dynamics）**，这是一种“**零深度、宽架构**”的 KAN。它放弃了增加网络深度，转而通过增加宽度和引入“提升（Lifting）”特征来捕捉动力学。
*   **关键技术细节**：
    *   **零深度宽架构**：等效于一种非线性回归，但利用 KAN 的可学习单变量样条函数（Splines）来替代固定基函数，增强了灵活性。
    *   **坐标提升（Lifting）**：将原始状态变量 $x$ 映射到更高维的特征空间 $\Phi(x)$（包含物理启发项，如多项式、三角函数等），使 KAN 在线性化空间中学习向量场。
    *   **复合损失函数**：结合了**导数监督（$L_{deriv}$）**和**轨迹展开（$L_{roll}$）**。通过 ODE 求解器进行反向传播，确保模型不仅在单步导数上准确，在多步预测（Rollout）中也能保持轨迹一致性。
    *   **符号提取程序**：开发了专门针对动力系统的符号化提取算法，通过对激活函数进行贪婪拟合和复杂度正则化，将学习到的样条函数转化为简洁的数学表达式。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **拓扑测试**：Hopf 纤维化（Hopf Fibration），验证模型对高维拓扑结构的恢复能力。
    *   **离散动力系统**：Hénon 映射、Ikeda 映射（非稀疏系统的典型代表）。
    *   **连续动力系统**：Lorenz 系统（经典混沌基准）。
    *   **偏微分方程（PDE）**：Kuramoto-Sivashinsky (KS) 方程（时空混沌）、无粘性 Burgers 方程（冲击波形成）。
*   **Benchmark 与对比**：
    *   对比了**标准深度 KAN**（证明深度在方程发现中反而有害）。
    *   对比了**稀疏回归（Sparse Regression）**（证明 KANDy 在非稀疏项上的优越性）。
    *   评估指标包括：方程发现准确率、NRMSE（归一化均方根误差）、吸引子几何保持度、李雅普诺夫时间内的预测稳定性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或总算力消耗。
*   **训练细节**：提到了一些超参数，例如 Lorenz 系统训练了 300 个 Epoch，使用 4 阶龙格-库塔（RK4）积分器，学习率为 $10^{-3}$。样条网格大小通常设为 7，网格更新频率为每 50 步一次。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低维 ODE 到高维 PDE，从离散映射到连续流，甚至包括了拓扑流形学习。
*   **充分性评价**：实验设计较为**充分且全面**。作者不仅展示了拟合精度，还重点分析了模型在训练分布外（Out-of-distribution）的泛化能力，以及在跨越李雅普诺夫时间后的吸引子几何保持情况。通过对 Ikeda 映射等“稀疏回归失效案例”的测试，有力证明了方法的必要性。

### 6. 主要结论与发现
*   **深度并非必要**：在动力系统方程发现中，增加 KAN 的深度会模糊全局方程结构，而“零深度+宽特征”能更直接地提取物理规律。
*   **突破稀疏限制**：KANDy 能够识别包含复杂分式或超越函数的非稀疏控制方程，这是传统 SINDy 类方法的短板。
*   **长期结构保持**：即使在确定性预测失效（超过李雅普诺夫时间）后，KANDy 仍能准确维持吸引子的几何形状和统计特性。
*   **拓扑一致性**：模型能成功恢复 Hopf 纤维化等复杂的拓扑不变性，证明其捕捉系统深层对称性的能力。

### 7. 优点与亮点
*   **可解释性极强**：直接输出闭式数学方程，而非黑箱模型。
*   **物理启发与数据驱动的融合**：通过“提升”技术，既利用了先验物理知识（库函数），又保留了 KAN 样条函数的非线性自适应能力。
*   **鲁棒性**：在处理包含冲击波（Burgers 方程）或非稀疏项的挑战性系统时表现稳健。

### 8. 不足与局限
*   **库函数依赖**：虽然比稀疏回归更灵活，但“提升”步骤仍依赖于预设的候选特征库，如果真实项完全不在库中，发现难度会增加。
*   **计算开销**：样条函数的优化和通过 ODE 求解器的轨迹展开训练比简单的线性回归或标准 MLP 更耗时。
*   **高维扩展性**：虽然处理了 PDE，但在极高维度的复杂系统（如全尺寸湍流）中，宽架构带来的参数爆炸问题尚待进一步验证。

（完）
