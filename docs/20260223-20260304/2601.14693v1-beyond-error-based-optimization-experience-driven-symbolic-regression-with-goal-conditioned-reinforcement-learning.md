---
title: "Beyond Error-Based Optimization: Experience-Driven Symbolic Regression with Goal-Conditioned Reinforcement Learning"
title_zh: 超越基于误差的优化：基于目标条件强化学习的经验驱动符号回归
authors: "Jianwen Sun, Xinrui Li, Fuqing Li, Xiaoxuan Shen"
date: 2026-01-21
pdf: "https://arxiv.org/pdf/2601.14693v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于经验驱动的目标条件强化学习符号回归
tldr: 针对符号回归中传统误差驱动搜索易陷入局部最优且结构模糊的问题，本文提出EGRL-SR框架。该框架将符号回归建模为目标条件强化学习问题，利用事后经验回放和动作价值网络从历史轨迹中学习通用的映射模式。通过设计全点满足的二元奖励函数和结构引导的启发式探索策略，EGRL-SR能够更稳健地搜索复杂数学表达式，在恢复率和鲁棒性上显著优于现有方法。
selection_source: fresh_fetch
motivation: 传统的基于误差的搜索方法在面对具有相似误差但结构迥异的候选表达式时，难以提供明确的搜索方向，导致收敛困难。
method: 提出一种基于目标条件强化学习的框架，结合事后经验回放、二元奖励函数及结构引导的启发式探索来优化动作价值网络。
result: 在公开基准测试中，EGRL-SR在表达式恢复率和鲁棒性方面均优于现有最先进方法，且能以更少的搜索预算恢复更复杂的表达式。
conclusion: 实验证明，通过经验驱动的动作价值网络引导搜索，能有效克服误差驱动优化的局限性，提升符号回归的搜索效率和准确性。
---

## 摘要
符号回归旨在自动识别紧凑且可解释的数学表达式，以建模输入和输出变量之间的函数关系。大多数现有的基于搜索的符号回归方法通常依赖拟合误差来指导搜索过程。然而，在广阔的表达式空间中，许多候选表达式可能表现出相似的误差值，但在结构上却存在巨大差异，这导致了模糊的搜索方向，并阻碍了向底层真实函数的收敛。为了应对这一挑战，我们提出了一个名为 EGRL-SR（经验驱动的目标条件强化学习符号回归）的新型框架。与传统的误差驱动方法不同，EGRL-SR 引入了一个新视角：利用精确的历史轨迹并优化动作价值网络，以主动引导搜索过程，从而实现更稳健的表达式搜索。具体而言，我们将符号回归建模为一个目标条件强化学习问题，并结合了事后经验回放（hindsight experience replay），使动作价值网络能够从多样的输入输出对中泛化出通用的映射模式。此外，我们设计了一个全点满足二元奖励函数，鼓励动作价值网络关注结构模式而非低误差表达式，并同时提出了一种结构引导的启发式探索策略，以增强搜索的多样性和空间覆盖率。在公共基准测试上的实验表明，EGRL-SR 在恢复率和稳健性方面始终优于现有最先进的方法，并且在相同的搜索预算下可以恢复更复杂的表达式。消融实验结果验证了动作价值网络能有效地引导搜索，且奖励函数和探索策略都发挥了关键作用。

## Abstract
Symbolic Regression aims to automatically identify compact and interpretable mathematical expressions that model the functional relationship between input and output variables. Most existing search-based symbolic regression methods typically rely on the fitting error to inform the search process. However, in the vast expression space, numerous candidate expressions may exhibit similar error values while differing substantially in structure, leading to ambiguous search directions and hindering convergence to the underlying true function. To address this challenge, we propose a novel framework named EGRL-SR (Experience-driven Goal-conditioned Reinforcement Learning for Symbolic Regression). In contrast to traditional error-driven approaches, EGRL-SR introduces a new perspective: leveraging precise historical trajectories and optimizing the action-value network to proactively guide the search process, thereby achieving a more robust expression search. Specifically, we formulate symbolic regression as a goal-conditioned reinforcement learning problem and incorporate hindsight experience replay, allowing the action-value network to generalize common mapping patterns from diverse input-output pairs. Moreover, we design an all-point satisfaction binary reward function that encourages the action-value network to focus on structural patterns rather than low-error expressions, and concurrently propose a structure-guided heuristic exploration strategy to enhance search diversity and space coverage. Experiments on public benchmarks show that EGRL-SR consistently outperforms state-of-the-art methods in recovery rate and robustness, and can recover more complex expressions under the same search budget. Ablation results validate that the action-value network effectively guides the search, with both the reward function and the exploration strategy playing critical roles.

---

## 论文详细总结（自动生成）

### 论文总结：超越基于误差的优化——基于目标条件强化学习的经验驱动符号回归 (EGRL-SR)

#### 1. 论文的核心问题与整体含义
*   **研究动机**：符号回归（SR）旨在从数据中发现简洁的数学表达式。传统方法（如遗传算法、强化学习）主要依赖**拟合误差**（如 MSE）来指导搜索。
*   **核心痛点**：在广阔的表达式空间中，存在大量“结构迥异但误差相似”的候选公式。单纯依赖误差引导会导致搜索方向模糊，容易陷入局部最优，生成结构复杂且不可解释的公式，难以恢复真实的底层函数。
*   **整体含义**：本文提出 **EGRL-SR** 框架，试图将搜索逻辑从“减小误差”转向“学习结构映射模式”，通过经验驱动的方式提高复杂表达式的恢复成功率。

#### 2. 论文提出的方法论
核心思想是将符号回归建模为**目标条件强化学习（GCRL）**问题，利用历史轨迹学习通用的数学映射规律。
*   **GC-MDP 建模**：
    *   **状态 (State)**：当前中间计算结果 $x_{now}$ 与目标输出 $y$ 的拼接。
    *   **动作 (Action)**：选择变量、一元算子或二元算子（采用后缀表达式生成策略）。
    *   **奖励函数 (APSR)**：设计了“全点满足二元奖励”，仅当表达式在所有样本点上均达到精度阈值时奖励为 1，否则为 0。这避免了相似低误差带来的误导。
*   **关键技术**：
    *   **事后经验回放 (HER)**：将失败的尝试（未达到目标 $y$ 但达到了某个中间值 $y'$）重新标记为成功的轨迹。这使得智能体能从大量失败中学习 $x$ 到不同 $y$ 的通用转换模式。
    *   **Double Dueling DQN**：使用离线强化学习算法优化动作价值网络，解耦状态价值与动作优势，缓解 Q 值过估计问题。
    *   **结构引导启发式探索 (SGHE)**：将搜索空间按结构属性（如嵌套深度、算子数量）划分为多个子空间，分配独立的价值网络进行并行探索，确保搜索的覆盖面。

#### 3. 实验设计
*   **数据集**：采用了符号回归领域公认的三个基准测试集：**Nguyen**、**Livermore** 和 **Keijzer**。
*   **Benchmark 划分**：按表达式长度（复杂度）对目标函数进行分层评估（从长度 $\le 8$ 到 $\ge 31$）。
*   **对比方法**：涵盖了四大类共 10 种最先进方法：
    *   **强化学习类**：DSR, DSO。
    *   **预训练/端到端类**：NeSymReS, End-to-End (E2E)。
    *   **遗传算法类**：GOMEA, PySR。
    *   **方程学习/蒙特卡洛类**：EQL-Div, DySymNet, TPSR。
*   **评估指标**：**精确恢复率 (Exact Recovery Rate)**，即是否能完全找回原始公式（含常数），而非仅看拟合精度。

#### 4. 资源与算力
*   **搜索预算**：每个算法分配统一的 160 万步搜索预算（EGRL-SR 分为 8 个搜索方向，每个方向 20 万步）。
*   **硬件说明**：论文正文及附录中**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。通常此类深度强化学习任务需要主流 GPU（如 RTX 3090 或 A100）支持。

#### 5. 实验数量与充分性
*   **实验规模**：对数十个目标函数进行了测试，每个函数进行 12 次独立重复实验以消除随机性影响。
*   **消融实验**：针对 HER（经验回放）、APSR（二元奖励）和 SGHE（探索策略）分别做了对比实验，验证了各组件的必要性。
*   **鲁棒性测试**：引入了不同水平的加性高斯噪声（0 到 0.1），测试算法在非理想数据下的表现。
*   **充分性评价**：实验设计较为全面，对比了当前主流的所有技术路径，且通过控制变量（如固定表达式长度）来排除搜索空间大小的干扰，具有较高的客观性和说服力。

#### 6. 论文的主要结论与发现
*   **性能领先**：EGRL-SR 在中高难度（长度 11-30）的表达式恢复上显著优于所有基线方法。
*   **结构学习的有效性**：动作价值网络确实学到了数学结构的映射模式，随着探索率 $\epsilon$ 的降低（即更多依赖网络引导），搜索效率（TtS 指标）明显提升。
*   **抗噪性强**：在噪声环境下，EGRL-SR 的恢复率下降速度远慢于基于误差优化的遗传算法（如 PySR），表现出极强的鲁棒性。
*   **二元奖励的必要性**：实验证明，传统的连续误差奖励（nRMSE）在复杂空间中会产生严重误导，而二元奖励配合 HER 能更有效地稳定训练。

#### 7. 优点（亮点）
*   **范式创新**：首次将目标条件强化学习（GCRL）引入符号回归，改变了过去三十年以误差为核心的搜索逻辑。
*   **解决稀疏奖励问题**：巧妙利用 HER 解决了符号回归中“只有找到正确公式才有奖励”的极度稀疏奖励难题。
*   **结构化探索**：SGHE 策略有效解决了强化学习在搜索初期容易陷入特定结构偏见的问题。

#### 8. 不足与局限
*   **常数处理**：论文承认目前对**复杂常数**（如非整数、无理数）的处理仍依赖于变量和算子的组合，这在处理含精细常数的物理公式时会显著增加搜索难度。
*   **计算开销**：虽然搜索步数固定，但维护多个价值网络和进行 HER 重放的计算复杂度可能高于简单的遗传算法。
*   **超长表达式瓶颈**：实验显示当表达式长度超过 31 时，所有方法（包括本文方法）的恢复率均降至 0%，说明超大规模空间的搜索仍是未解难题。

（完）
