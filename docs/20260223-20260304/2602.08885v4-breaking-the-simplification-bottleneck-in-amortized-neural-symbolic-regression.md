---
title: Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression
title_zh: 突破摊销神经符号回归中的简化瓶颈
authors: "Paul Saegert, Ullrich Köthe"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08885v4"
tags: ["query:sr"]
score: 10.0
evidence: 摊销神经符号回归与表达式简化
tldr: 符号回归（SR）旨在发现可解释的数学表达式，但摊销式神经符号回归在处理复杂科学问题时受限于表达式简化的效率瓶颈。本文提出 SimpliPy，一个比 SymPy 快 100 倍的规则化简化引擎，并构建了 Flash-ANSR 框架。该方法显著提升了训练和推理速度，支持更大规模的数据集训练和有效的测试集去重，在保持高精度的同时生成更简洁的表达式，性能可媲美最先进的遗传算法。
selection_source: fresh_fetch
motivation: 现有的摊销式符号回归方法受限于通用计算机代数系统极慢的简化速度，难以扩展到复杂的科学场景。
method: 提出了一种名为 SimpliPy 的高效规则化简化引擎，并基于此构建了 Flash-ANSR 框架，以实现大规模训练和高效的表达式规范化。
result: SimpliPy 实现了 100 倍的加速，使 Flash-ANSR 在基准测试中优于同类神经模型，并达到与 PySR 相当的精度且表达式更简洁。
conclusion: 通过突破表达式简化的计算瓶颈，摊销式神经符号回归能够在大规模数据集上实现更高效、更准确且更简洁的公式发现。
---

## 摘要
符号回归 (SR) 旨在发现能够准确描述观测数据的可解释解析表达式。摊销 SR 有望比主流的遗传编程 SR 方法更高效，但目前在扩展到现实科学复杂度方面仍面临挑战。我们发现，一个关键障碍是缺乏将等效表达式快速约简为简洁规范形式的方法。摊销 SR 曾通过 SymPy 等通用计算机代数系统 (CAS) 来解决这一问题，但高昂的计算成本严重限制了训练和推理速度。我们提出了 SimpliPy，这是一个基于规则的简化引擎，在质量相当的情况下，其速度比 SymPy 快 100 倍。这使得摊销 SR 得到了实质性的改进，包括对更大训练集的扩展性、对每个表达式 token 预算的更高效利用，以及针对等效测试表达式的系统性训练集去污染。我们在 Flash-ANSR 框架中展示了这些优势，该框架在 FastSRB 基准测试中取得了比摊销基准模型（NeSymReS、E2E）更好的准确率。此外，它的性能与最先进的直接优化方法 (PySR) 相当，同时随着推理预算的增加，它能恢复出更简洁而非更复杂的表达式。

## Abstract
Symbolic regression (SR) aims to discover interpretable analytical expressions that accurately describe observed data. Amortized SR promises to be much more efficient than the predominant genetic programming SR methods, but currently struggles to scale to realistic scientific complexity. We find that a key obstacle is the lack of a fast reduction of equivalent expressions to a concise normalized form. Amortized SR has addressed this by general-purpose Computer Algebra Systems (CAS) like SymPy, but the high computational cost severely limits training and inference speed. We propose SimpliPy, a rule-based simplification engine achieving a 100-fold speed-up over SymPy at comparable quality. This enables substantial improvements in amortized SR, including scalability to much larger training sets, more efficient use of the per-expression token budget, and systematic training set decontamination with respect to equivalent test expressions. We demonstrate these advantages in our Flash-ANSR framework, which achieves much better accuracy than amortized baselines (NeSymReS, E2E) on the FastSRB benchmark. Moreover, it performs on par with state-of-the-art direct optimization (PySR) while recovering more concise instead of more complex expressions with increasing inference budget.

---

## 论文详细总结（自动生成）

这是一份关于论文《Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：符号回归（SR）旨在从观测数据中发现简洁、可解释的数学公式。传统的遗传编程（GP）方法（如 PySR）虽然精确但每次搜索都需从零开始，计算成本高。
*   **核心问题**：新兴的“摊销式（Amortized）”神经符号回归通过预训练 Transformer 模型来加速推理，但面临**简化瓶颈**。为了保证训练质量，必须将复杂的表达式（如 $x+x$）简化为规范形式（$2x$）。目前主流方法依赖 SymPy 等通用计算机代数系统（CAS），其解析速度极慢，导致大规模训练（如数亿级样本）在时间上不可行，限制了模型的扩展性和泛化能力。

### 2. 论文提出的方法论
论文提出了 **Flash-ANSR** 框架，其核心由以下技术驱动：
*   **SimpliPy 引擎**：这是论文的核心贡献。它将代数简化转化为高效的**哈希查找和模式匹配**。
    *   **离线规则发现**：利用 Kruskal 算法变体，在有限的变量和长度范围内预先搜索最小规则集。
    *   **在线模式匹配**：在训练/推理时，直接在 Token 序列上进行快速替换和抵消，无需构建复杂的对象树。
*   **Flash-ANSR 架构**：
    *   **编码器**：采用改进的 Set Transformer，引入了 Masked RMSSetNorm 以处理变长数据集。
    *   **解码器**：基于预归一化（Pre-norm）的 Transformer Decoder，结合 FlashAttention 和旋转位置编码（RoPE）。
*   **实时数据生成与去污染**：
    *   利用 SimpliPy 的高速特性，在训练过程中实时生成并简化表达式（共训练了 5.12 亿个样本）。
    *   **严格去污染**：通过数值和符号双重校验，确保训练集中不包含任何与测试集等效的表达式，保证了实验的严谨性。

### 3. 实验设计
*   **Benchmark**：使用了 **FastSRB** 基准测试，该测试基于物理驱动的数据域（SRSD），比传统的随机区间更具科学意义。
*   **对比方法**：
    *   **摊销类基准**：NeSymReS、E2E（均为基于 Transformer 的模型）。
    *   **直接优化类基准**：PySR（目前最先进的遗传编程方法）。
*   **评估维度**：
    *   **推理时间-恢复率 Pareto 前沿**：分析在不同计算预算下模型的表现。
    *   **数据稀疏性**：改变样本点数量 $M$（从 1 到 2048）。
    *   **噪声鲁棒性**：加入不同比例的加性高斯噪声。

### 4. 资源与算力
论文详细列出了不同规模模型的训练资源（见表 3）：
*   **模型规模**：提供 3M、20M、120M、1B 四种参数规模。
*   **硬件与时长**：
    *   **3M**：RTX 2080Ti，耗时 112 小时。
    *   **20M**：RTX 4090，耗时 56 小时。
    *   **120M**：A100，耗时 340 小时。
    *   **1B**：H200，耗时 657 小时。
*   **实现细节**：使用 PyTorch 2.9，开启自动混合精度（bf16）和 `torch.compile` 优化。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常充分。除了主基准测试外，还进行了 1000 次自助法（Bootstrapping）以计算置信区间。
*   **消融实验**：针对训练先验（常数分布范围）进行了消融研究，证明了更广的常数采样范围对泛化至关重要。
*   **客观性**：论文特别强调了“去污染”和“推理时间标准化”，避免了以往研究中因训练集包含测试题目或忽略计算成本而导致的性能虚高，实验环境公平、客观。

### 6. 主要结论与发现
*   **性能突破**：Flash-ANSR 彻底碾压了之前的神经符号回归模型（NeSymReS, E2E）。其 120M 和 1B 版本在精度上达到了与 PySR 相当的水平。
*   **简洁性反转（Parsimony Inversion）**：这是一个重要发现。随着推理预算（采样数）增加，PySR 倾向于通过增加公式复杂度来降低误差，而 Flash-ANSR 则倾向于找到**更简洁、更接近真值**的公式。
*   **SimpliPy 效率**：在保持与 SymPy 相当的简化质量前提下，实现了 **100 倍的速度提升**，且避免了 SymPy 在处理复杂公式时偶尔出现的长度反增或超时问题。
*   **相位变迁**：在数据稀疏性实验中观察到了类似“深度双重下降”的现象，在极少数据点时模型会出现“复杂度峰值”。

### 7. 优点
*   **工程突破**：SimpliPy 解决了困扰该领域多年的计算瓶颈，使得大规模在线训练成为可能。
*   **科学严谨性**：建立了严格的去污染协议和基于物理域的评估标准，为后续研究树立了榜样。
*   **模型特性**：模型展现出良好的缩放效应（Scaling Laws），且生成的公式比遗传算法更具“自然感”和简洁性。

### 8. 不足与局限
*   **噪声敏感性**：由于模型目前仅在无噪声数据上训练，当面对高噪声（$\eta \ge 10^{-2}$）时，性能下降比 PySR 更快。模型会将噪声误认为高频信号并试图用复杂的结构去解释它。
*   **分布偏移**：虽然在 FastSRB 上表现良好，但对于完全超出训练先验的函数形式，摊销模型仍可能面临泛化挑战。
*   **计算开销**：尽管推理很快，但 1B 参数模型的预训练成本依然巨大（数百小时的顶级 GPU 算力）。

（完）
