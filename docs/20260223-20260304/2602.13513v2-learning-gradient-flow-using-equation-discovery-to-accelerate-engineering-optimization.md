---
title: "Learning Gradient Flow: Using Equation Discovery to Accelerate Engineering Optimization"
title_zh: 学习梯度流：利用方程发现加速工程优化
authors: "Grant Norman, Conor Rowan, Kurt Maute, Alireza Doostan"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13513v2"
tags: ["query:sr"]
score: 9.0
evidence: 工程动力系统的数据驱动方程发现
tldr: 本研究针对工程优化中目标函数及其梯度计算昂贵的问题，提出了一种基于数据驱动方程发现的“学习梯度流”（LGF）优化器。该方法通过分析梯度下降、牛顿法或ADAM的优化轨迹数据，学习其连续时间动力学并构建代理模型。LGF支持全维或降维空间的变量多项式建模，能有效预测优化路径。在逆问题、拓扑优化及科学机器学习等任务中的实验表明，该方法在减少昂贵函数评估的同时，显著提升了收敛效率。
selection_source: fresh_fetch
motivation: 旨在降低复杂工程优化问题中频繁评估目标函数及其梯度所带来的巨大计算成本。
method: 引入LGF优化器，利用方程发现技术从优化轨迹中提取动力学特征，构建连续时间梯度流的代理模型。
result: 在结构拓扑优化、逆问题和前向求解等多个标准工程案例中，LGF显著加快了优化过程的收敛速度。
conclusion: 学习梯度流方法能有效捕捉优化轨迹的关键特征，是加速高成本工程优化问题的有力工具。
---

## 摘要
在这项工作中，我们研究了利用动力系统的数据驱动方程发现技术，来建模和预测无约束优化问题的连续时间动力学。为了避免对目标函数及其梯度进行昂贵的评估，我们利用优化变量的轨迹数据来学习与梯度下降、牛顿法和 ADAM 优化相关的连续时间动力学。随后，将发现的梯度流作为原始优化问题的代理模型进行求解。为此，我们引入了学习梯度流（LGF）优化器，该优化器能够在优化过程中以用户定义的时间间隔，在全维或降维空间中构建变多项式阶数的代理模型。我们在工程力学和科学机器学习的几个标准问题上证明了该方法的有效性，包括两个反问题、结构拓扑优化以及两个具有不同离散化的正向求解问题。我们的结果表明，学习到的梯度流通过捕捉优化轨迹的关键特征，同时避免对目标及其梯度进行昂贵的评估，可以显著加速收敛。

## Abstract
In this work, we investigate the use of data-driven equation discovery for dynamical systems to model and forecast continuous-time dynamics of unconstrained optimization problems. To avoid expensive evaluations of the objective function and its gradient, we leverage trajectory data on the optimization variables to learn the continuous-time dynamics associated with gradient descent, Newton's method, and ADAM optimization. The discovered gradient flows are then solved as a surrogate for the original optimization problem. To this end, we introduce the Learned Gradient Flow (LGF) optimizer, which is equipped to build surrogate models of variable polynomial order in full- or reduced-dimensional spaces at user-defined intervals in the optimization process. We demonstrate the efficacy of this approach on several standard problems from engineering mechanics and scientific machine learning, including two inverse problems, structural topology optimization, and two forward solves with different discretizations. Our results suggest that the learned gradient flows can significantly expedite convergence by capturing critical features of the optimization trajectory while avoiding expensive evaluations of the objective and its gradient.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Gradient Flow: Using Equation Discovery to Accelerate Engineering Optimization》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在工程设计、逆问题求解和科学机器学习中，优化过程通常需要成百上千次迭代。每次迭代都需要评估目标函数及其梯度，这往往涉及昂贵的偏微分方程（PDE）数值模拟（如有限元分析）。
*   **核心问题**：如何利用已有的优化轨迹数据，在不进行昂贵梯度计算的情况下，预测优化变量的未来演化路径？
*   **整体含义**：论文将优化算法（如梯度下降、牛顿法）视为连续时间动力系统（梯度流）的离散化。通过**方程发现（Equation Discovery）**技术，从初期的优化轨迹中学习出描述该动力系统的常微分方程（ODE），并将其作为代理模型来“外推”后续步骤，从而显著减少昂贵的物理仿真次数。

### 2. 方法论
*   **核心思想**：引入**学习梯度流（Learned Gradient Flow, LGF）**框架。它不直接模拟物理过程，而是模拟“优化器在参数空间移动”的规律。
*   **关键技术细节**：
    *   **SINDy 算法**：使用稀疏动力学非线性识别（SINDy）技术。给定优化变量轨迹 $a(t)$，构建一个包含多项式项的候选函数库 $\phi(a)$，通过稀疏回归求解系数矩阵 $\Xi$，使得 $\dot{a} \approx \Phi(a)\Xi$。
    *   **优化器适配**：
        *   **梯度下降/牛顿法**：直接学习状态变量 $a$ 的时间导数。
        *   **ADAM 优化器**：由于 ADAM 具有动量和二阶矩（历史依赖），LGF 将其建模为一个包含状态、动量和方差的扩展 ODE 系统，仅对其中最昂贵的梯度项进行 SINDy 建模。
    *   **降维技术（SVD）**：针对高维问题（如拓扑优化），先通过奇异值分解（SVD）将状态投影到低维潜空间 $r \ll n$，在潜空间学习动力学，再映射回原空间。
    *   **周期性重训练**：采用“收集数据（$K$ 步）— 学习模型 — 代理预测（$M-K$ 步）”的循环策略，以应对优化景观的非线性变化。

### 3. 实验设计
*   **实验场景**：
    1.  **1D 动态逆问题**：估计加热棒的材料导热系数（2个变量）。
    2.  **3D 结构拓扑优化**：最小化悬臂梁的柔度（13,824个变量，高维挑战）。
    3.  **非线性热传导前向求解**：使用牛顿法求解变分能量最小化问题。
    4.  **全波形反演（FWI）**：地球物理中的非凸优化问题，使用 ADAM 适配。
    5.  **Deep Ritz 方法**：利用神经网络求解 3D 弹性力学问题（涉及随机梯度/小批量数据）。
*   **Benchmark（基准）**：标准的梯度下降（GD）、牛顿法（Newton）和 ADAM 优化器。
*   **对比指标**：收敛速度、梯度评估次数的节省比例、最终损失函数值、参数恢复准确度。

### 4. 资源与算力
*   **算力说明**：文中未明确列出具体的 GPU 型号（如 A100/RTX 4090）或具体的训练总时长。
*   **实现工具**：算法基于 **PyTorch** 开发，ODE 积分使用了 `torchdiffeq` 库。
*   **计算效率评价**：论文重点在于**减少函数评估次数**（Function Evaluations）。例如，在拓扑优化中，LGF 可以在潜空间（维度仅为 2）运行，其计算开销远小于求解万级自由度的有限元方程。

### 5. 实验数量与充分性
*   **实验数量**：共进行了 5 组具有代表性的工程和机器学习实验。
*   **充分性评价**：
    *   **多样性**：涵盖了线性与非线性物理、谱方法与有限元离散化、确定性与随机优化。
    *   **深度**：每组实验都对比了不同程度的加速比（如 33% 到 250%），并展示了当加速过快（外推过远）时模型失效的情况。
    *   **客观性**：实验不仅展示了成功案例，还诚实地记录了代理模型偏离真实轨迹的失效点，体现了科学的客观性。

### 6. 主要结论与发现
*   **显著加速**：LGF 在保持高精度的前提下，可减少 33% 至 250% 的梯度评估次数。
*   **低维本质**：即使是上万维的拓扑优化问题，其优化轨迹往往也存在于极低维（如 $r=2$）的流形上，这证明了降维 LGF 的有效性。
*   **通用性**：该方法成功适配了包括 ADAM 在内的复杂现代优化器。
*   **稳定性阈值**：代理模型的预测步长（$M$）存在上限，超过该上限会导致优化过程发散或损失增加。

### 7. 优点
*   **非侵入性**：不需要修改原有的物理求解器，只需记录优化轨迹即可学习。
*   **在线学习**：无需离线预训练，在优化过程中实时构建代理模型，能够适应当前问题的特定景观。
*   **数学完备性**：为优化算法提供了清晰的连续时间动力学解释，并将 SINDy 成功引入优化加速领域。

### 8. 不足与局限
*   **超参数敏感性**：历史窗口大小 $K$ 和预测步长 $M$ 的选择目前依赖经验，缺乏自动调节机制。
*   **约束处理**：目前主要针对无约束优化或通过惩罚函数转化的无约束问题，难以直接处理复杂的硬约束（如投影步骤会引入轨迹的不连续性）。
*   **外推风险**：如果优化景观发生剧烈变化（如进入高度非凸区域），基于过去轨迹学习的线性/低阶多项式模型可能会失效。
*   **内存开销**：对于极大规模问题，存储优化历史轨迹矩阵 $A$ 可能会带来内存压力（类似于 L-BFGS 的限制）。

（完）
